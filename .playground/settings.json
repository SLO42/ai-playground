{
	"name": "ai-playground",
	"description": "",
	"branch": "feature/real-data",
	"agentConfig": {
		"topology": "hierarchical-mesh",
		"maxAgents": 6,
		"memoryBackend": "hybrid (HNSW + SQLite)",
		"consensus": "raft"
	},
	"build": {
		"dev": {
			"command": "npm run dev",
			"label": "Vite + SvelteKit"
		},
		"build": {
			"command": "npm run build",
			"label": "Production build"
		},
		"test": {
			"command": "npm test",
			"label": "Vitest"
		}
	}
}