# BUILD-QUEUE — the machine-readable wave chain (orchestrated looping, operator-authorized)

> Standing directive (operator, 2026-06-10): **the build factory runs autonomously** — when a wave lands green (all DoD reviews passed, pushed), the next QUEUED wave launches WITHOUT a per-wave ask. Stops are deviation-driven only: a review failure that exhausts its fix-loop, a CONFLICT/BLOCK deviation, or an item marked `gate:operator`. Cross-session continuation via scheduled wakeup; every autonomous stretch ends with a digest of verdicts/commits/deviations.
>
> Maintained by the orchestrating session. One line per item: `status | id | what | gate`.
> status: done / running / queued / blocked. gate: auto (chain through) / operator (stop and ask).
>
> **Why a doc and not the database (operator-discussed 2026-06-10):** the build factory must not store its control state inside the thing it is building — waves migrate/reset/wedge the dev SurrealDB (F-015 wedged `db:up` itself), and D-001's boundary cuts both ways (product never parses harness files; harness doesn't write product tables). Git-tracked markdown needs zero infrastructure and versions with the decisions behind each item. **End state = D-040 self-hosting:** this queue becomes `task`/`sprint` rows in `atelier_self`, owned by the PM inside the product; this doc then retires to a read-only cold-boot projection.

> ⏭ **RESUME PLAN (operator, 2026-06-26):** **BL-R1 DONE** (`eed3163`+`bcbc1f5`) · **BL-H1 digest DONE** · **BL-H2 eval DONE — headroom NO-ADOPT/idea-only** (`docs/HEADROOM-DIGEST.md` §6; F-049). Recovery + headroom closed; **recovery-harden-2 DONE** (RH-1 BL-R2 `d11db2f` + RH-2 wi-harden-2 `e89c9c5` + RH-3 `c58e5fb`, pushed) — the loop is hardened. Next + LAST — **go-live for ROUNDS**: bring up DB (v2 :8000) + dev server `CLAUDE_CODE_OAUTH_TOKEN` UNSET (F-029) → operator hits Continue → watch ROUNDS + `/projects/[id]/graph`. Boot reaper now runs BL-R1 release+reset, so go-live live-validates that path. Open deferred (non-blocking): BL-R3 (2 task-status MEDIUMs), BL-GUX-FIX (3 graph-UI test gaps), wi-harden done. (bring up DB + dev server token-unset → operator hits Continue → watch ROUNDS + the new `/projects/[id]/graph`). Server is currently DOWN (paused). All work committed + pushed to origin/v2 (tip `1da738e`); docs on v2-main. The full "alive" arc + repo-creation + usage-observability + command-center-ux + lifecycle-graph are DONE. Open tracked: BL-R1, wi-harden-2 (latent), headroom (BL-H1/H2), backlog (BL-1/BL-2/BL-2b).

| status | id | what | gate |
|---|---|---|---|
| queued | safety-defaults | **⚠ PRE-WAKE MUST (COMPLETION-LEDGER Wave B, HIGH — pull early).** Ship conservative NON-ZERO default token budgets (`orchestration.yaml` `spend.dailyTokenBudget`/`perProjectTokenBudget` currently 0=UNCAPPED; enforcement exists at `sessions/launch.ts:475`, default off) + loud UI banner while armed-and-uncapped · config-unreadable currently silently disarms brain to `mode='manual'` (only console.warn) → persist an honest "autonomy OFF: config unreadable" UI state · arm PM distress trigger (`workforce.yaml failure_threshold:null`). Safe autonomous floor before any wake. | auto |
| queued | analytics-visibility | **COMPLETION-LEDGER Wave A — biggest cross-cluster theme.** workforce/HR emits ZERO scene_events (hiring invisible) → `hire_*`/`cert_*` events · concierge Stage-2 turn no `agent_event` · orchestrator drain-fault swallows console-only (`orchestrator.ts:498..1149`) → named error events · enqueue/park/gate-block not first-class (`workqueue.ts`) · boot-skip invisible → boot-status row · pm-review scene_event · tests asserting each event emits. "Analytics first-class + faces of failure visible." | auto |
| queued | finish-small-polish | **COMPLETION-LEDGER Wave C.** F-048 `memory_review` dedup residue (`schema.ts:635`/`:2883`) · `/memory` deprecated `$app/stores`→`$app/state` (`memory/+page.svelte:12`) · stale Sidebar comment · raw session-hash id labels → titles/names · merge-back real-git test · explorer real-surreal test · judge.ts catch-honesty verify. **+ 4 pre-existing full-suite failures found by periphery-svc on clean v2 HEAD (not introduced): (1) brain `onDbChange` watched-tables census drift, (2) memory/soul.ts uncatalogued `FROM memory` census, (3-4) pm-autonomous-action/pm-fit-verdict-action + loops-tab.live teardown flakes (0-assertion credential-skipped). (1) and (2) look like real census/catalog drift — verify + fix; (3-4) are teardown hygiene.** | auto |
| queued | v1-retirement | **COMPLETION-LEDGER Wave D — 'modernized and clean'.** Already clean (no dashboard/, heartbeat/, live OpenClaw). Strip: stale `ai-playground-v2/docs/DECISIONS.md` (35 entries, missing D-035..D-040) → banner→v2-main · `.playground/` in v2 worktree (verify no reader; D-001) · rename `runtime/index.ts:5` OpenClaw seam comment. | auto |
| queued | brain-observer-loop | **SPEC'd → `docs/BRAIN-OBSERVER-LOOP-SPEC.md` (2026-07-23, operator-final). P3/P4 WAKE — the active self/PM/project observer.** Phased: v1 eye (m0081 brain_tick journal + digest + local triage + `/brain` panel + kill switch + preconditions, NO proposals) → v2 voice (m0082 proposal + cloud escalation cap 4/day + rejection ledger m0083 + backpressure 3 + auto-demote N=5) → v3 depth (reader-council fenced+provenanced + PM advisory routing + curation gate m0084). 15 safety invariants. Blocked-by: safety-defaults + atelier-identity-compose + local-runtime-lifecycle. | operator (release after stabilize + deps) |
| queued | brain-alive-ui | **SPEC'd → `docs/BRAIN-ALIVE-UI-SPEC.md` (2026-07-23, operator-final defaults).** The "alive and watching" experience layer over the observer loop — a `brain_activity` SSE narration (zero new tables, zero extra brain compute), honest resting vitals (heartbeat/countdown/last-verdict), scene 'firing' (self:atelier pulse + escalation constellations), the COUNCIL THEATER (reader seats light up, findings land with confidence bars + provenance, disagreement side-by-side), "Think now" + focus mode + self-reverting cadence raise (local $0 only, cloud caps untouchable). 6 SI safety invariants. Phased v1(eye)/v2(voice)/v3(theater) tracking the observer loop. Defaults locked: ephemeral reasoning · Think-Now counts vs cloud cap · 5min/60min raise · stream reasoning text. | operator (ships with observer v1/v2/v3) |
| queued | versioning-audit | **SPEC'd → `docs/VERSIONING-AUDIT-SPEC.md` (2026-07-23, defaults locked). DECISION **D-042 LOCKED** (operator, 2026-07-23).** Unified append-only version + audit trail for every capability artifact (tool/agent/skill/workflow/rule/rejection/role): version · content-hash (EOL-normalized, F-054) · server-stamped SOURCE (operator/sync-from-disk/hr-recruiter/self-improvement/brain-observer/rollback, unforgeable D-035a-style) · REASON (git-commit/learning_candidate/validation_run/interview_run/consent) · WHEN · SUPERSEDES. GENERALIZES existing `role_version` prior art (`schema.ts:1077`); roles stay on it, unified by read-time union (no 2nd writer, F-055). `session.artifact_versions` pins the versions each session ran with → effectiveness attribution + regression tracing. One chokepoint `writeArtifactVersion`; autonomous bumps require a passed validation gate (D-024 fail-closed). Rollback appends never deletes. Phased v1(substrate+sync chains+history UI)/v2(attribution join)/v3(HR+self-improvement writers+rollback). Defaults: every hash-change versions · rollback operator-only v1 · wave scripts in v3 · keep history forever. | operator (foundation; release after stabilize) |
| queued | self-improvement-loop | **SPEC'd → `docs/SELF-IMPROVEMENT-LOOP-SPEC.md` (2026-07-23, defaults locked).** UNIFYING spine for Atelier self-improvement: measure effectiveness → propose update → VALIDATE for correctness → operator-adopt, append-only. Consolidates skillopt-pilot (validation-gated skill edits) + curation-gate + memory-curator + per-role-model-effectiveness + NEW tool/route-choice effectiveness (the missing outcome-quality half of `writeRoutingEvent`) + conversation-mining (fenced, D-026). Answers "learn from conversations + update skills/workflows/rejections without deriving wrong ideas" and "tool-use optimizer + tools updated as needed". SkillOpt-Sleep shape (slow offline batch learner) feeding the same operator queue + two-phase gate as the observer. | operator (release after stabilize; P3+ substrate) |
| queued | local-runtime-lifecycle | **SPEC-NEEDED (observer-loop dep §6).** Atelier owns the local model server lifecycle — on-demand load, idle-unload (Ollama `keep_alive` or llama.cpp `llama-server`); today `OllamaProvider` assumes it's already running. The $0-idle premise for a cheap always-sensing brain. Operator-requested 2026-07-23. | auto (spec first) |
| queued | atelier-identity-compose | **SPEC-NEEDED (observer-loop dep §6, D-040).** Replace `resolveAtelier` placeholder (`peer/resolve.ts:131-170`) with a durable `atelier_self` + append-only observation/rejection memory mounted into every brain turn. Stage-1 already stamps `session.pm='pm:atelier_self'` (m0074) short-lived — missing durability. Blocking for observer v1. | auto (spec first) |
| queued | per-role-model-config | **SPEC-NEEDED (operator req 2026-07-23).** Unify the 3 fragmented model-choice mechanisms into one brain-vs-hire-vs-task→model role map (Claude vs local pick-per-role) + model×role EFFECTIVENESS analytics (today provider-only, `provider-usage.ts`/`benchmark/store.ts` — never keyed by role). Reporting for growth. | auto (spec first) |
| queued | skillopt-pilot | **PILOT SPIKE before spec (Fable re-scope of `reference_skillopt`).** Verify the m0076/m0077 benchmark harness can inject an arbitrary skill as system prompt vs a FROZEN model with train/val/test splits (go/no-go for a full SkillOpt-adoption spec: validation-gated skill edits, benchmark=env, HR gauntlet=adjudicator). One skill (`atelier-data-layer`, richest fails evidence), ~10-20 task env from real logged failures, baseline skill-on-vs-off. Cheap. | auto |
| queued | n8n-spec | **SPEC-ONLY now, build later (operator 2026-07-23).** Brain-authored external-integration capability for data-collection/efficiency workflows; D-026 fencing + D-037 gating + F-045 catalogued-capability rule baked in; built by the awake brain via the proven proposal path (P5). | auto (spec first) |
| queued | plan-refine | **Refined prompts + operator question loop in front of planning — SPEC'd → `docs/PLAN-REFINE-SPEC.md` (2026-07-23, operator-approved design).** PR-1 `planning/refine.ts` + m0081 `plan_refinement` + real-surreal tests · PR-2 PM-chat pre-step (classifier branch byte-identical when not planning-shaped, question card, D-035a answer parse — **red-team mandatory**, steering-path adjacency) · PR-3 `planning` session kind + command-center surface (F-008 honest states) · PR-4 metering/analytics/timeout-finalize (CG2-1 class check). Planner model default `claude-fable-5` via provider ladder. Session-side twin ALREADY SHIPPED: `.claude/skills/plan-refine/SKILL.md`. | operator (new feature — release after pause lifts) |
| done | security-hardening-1 | **✅ GREEN + PUSHED (wave `security-floor` `wf_98d3bf5c-056`, origin/v2 `9d1019b..b63e2b0`, 2026-07-08).** SEC-1 `9c434a2` Host-fallback fails closed on LAN binds (loopback binds byte-identical; review+redTeam PASS) · SEC-2 `671f09f`+fix `b63e2b0` four premise comments corrected (fix-loop caught a half-done header). RedTeam deferred → security-floor-2: (MEDIUM) getClientAddress authoritative path header-spoofable when adapter ADDRESS_HEADER misconfigured · (MEDIUM) unset HOST defaults loopback in serverIsLoopbackBound — verify vs boot gate consistency · (LOW) bracketed-IPv6 Host mangle. From `SECURITY-MODEL-SPEC.md` §3. **(Operator released all 9 waves 2026-07-08, decisions delegated — D-notes `7ca9145`.)** SEC-1 Host-header loopback fallback fails CLOSED when getClientAddress unavailable on a LAN bind (today spoofable `Host: 127.0.0.1` = login-free control plane if the adapter ever changes; loopback-bound keeps lenient; red-team) · SEC-2 correct 4 stale "loopback-only enforced at bind" comments (events/notifications/briefs/file-snapshot — F-055 premise-drift class; haiku) · SEC-3 plain-HTTP cookie = documented accepted risk · SEC-4 runtime-DB grant matrix = DBR-1 acceptance bar. Perimeter otherwise VERIFIED: all 4 exempt paths own-auth, no exempt-unprotected endpoint, both F-055 fixes in place. | operator |
| done | security-floor-2 | **✅ 4/4 GREEN + PUSHED (`wf_c7039de8-3fb`, origin/v2 `51d0ad2..b3a92e7`, 2026-07-23, redTeamAll).** SF2-1 `51d0ad2` least-priv runtime DB user seam (authLevel:'database', additive opt-in byte-identical when unset). SF2-2 `7594bcc` AUTH_EXPIRED_RE narrowed (authz-denial ≠ expiry). SF2-3 `b123505`+fix `1ef6a69` loopback spoof-resistance — **fails closed on any bind when a proxy fronts the app; a set ADDRESS_HEADER voids the loopback-bind-unreachable premise** (red-team caught + fixed in-loop). SF2-4 `b3a92e7` atomic guardrails write + root_path re-confine + `git -C` push-deny. **DEFERRED → security-floor-3 (MEDIUM: db-up.ts stale operator handoff).** Original scope ↓ SF2-1 wire the runtime FLIP seam: SURREAL_RUNTIME_USER/PASS + authLevel:'database' through runtime-init resolveDbConnect (today an operator following db-up's .env block gets a failed signin → disconnected dashboard); actual live .env flip stays operator-manual · SF2-2 AUTH_EXPIRED_RE narrowed so genuine least-priv authz denials don't trigger the F-042 re-signin loop post-flip · SF2-3 ADDRESS_HEADER-spoof + unset-HOST-default consistency check between serverIsLoopbackBound and the D-025 boot gate (+ bracketed-IPv6 Host normalize) · SF2-4 (from cc-harness-hardening redTeam) atomic write for writeProjectGuardrails (temp+rename — a mid-write crash truncates the D-024 settings.json) + reconcile root_path re-confine under CODE_ROOT + `git -C` push-deny pattern. | operator |
| done | periphery-svc | **✅ 3/3 GREEN + PUSHED (`wf_3b87e487-5d7`, origin/v2 `1468b99..aea9da4`, 2026-07-23).** SVC-1 `1468b99` ServicesTicker (bounded/unref'd/single-flight/fault-isolated, `services.tickMs` 0=off/absent=5min) boot-wired → existing `manager.tick()`; NEW first-class `agent_event(type='supervision')` per detected-down+restart (which service/why/outcome) via **migration m0081** (widens type ASSERT; live db:up 81/81 clean). SVC-2 phantom `engine` dropped from SERVICE_NAMES (honest per-service status, F-008). SVC-3 proc/incidents/surreal-adapter/ticker suites (ticker.live.test.ts real-surreal). Review PASS, all 4 redTeam PASS. Deferral ledger all-LOW (DATA-MODEL.md §4.7 `engine` doc-drift + a harmless double config-read) — no chain. **NOTE: 4 pre-existing full-suite failures confirmed on clean v2 HEAD (NOT introduced): brain `onDbChange` census, memory/soul.ts FROM-memory census, pm-*/loops-tab teardown flakes → logged to finish-small-polish.** | auto |
| paused | periphery-hardening | **⏸ SUPERSEDED by `periphery-svc` above (2026-07-23). Historical.** OPERATOR PAUSE mid-wave (2026-07-11, run `wf_81cdf5c8-df5`). 2/3 GREEN + PUSHED: SCN-1 `67a6245` scan lock (review PASS) · SYN-1/2 `75d4b56` dryRun REQUIRED + registry/board-repo suites (review + red-team PASS). SVC-1+2+3 builder stopped MID-TASK — UNCOMMITTED work left in the v2 worktree (services/{index,manager,runtime}.ts, ticker.ts+4 new test files, config/load.ts, orchestration.yaml, hooks.server.ts, services page): resume with `Workflow({scriptPath: v2-wave.js, resumeFromRunId: 'wf_81cdf5c8-df5', args: <full original>})` + RECOVERY CONTEXT on the SVC task listing those dirty files (verify-don't-redo, the F-051 recipe — 3 prior successes).** Original scope: SCN-1 scan lock · SYN-1 dryRun required · SYN-2 suites · SVC-1 D-004-note 5min unref'd tick · SVC-2 engine honesty · SVC-3 proc/incidents/surreal-adapter suites. | operator | SCN-1 per-(project,family) scan lock (concurrent re-scans double-insert the active finding set) · SYN-1 make `dryRun` REQUIRED on SyncRunOptions (adapters default false — a bare future caller performs REAL GitHub mutations; red-team) · SYN-2 SyncRegistry+board-repo unit suites · SVC-1 DECISION+wire: bounded unref'd periodic services tick, config `services.tickMs` 0=off (auto-restart engine currently has NO production scheduler — crashed ollama stays down until a page load) · SVC-2 `engine` SERVICE_NAMES honesty · SVC-3 proc/incidents/surreal-adapter unit suites. No migrations. | operator |
| done | cost-governance-1a | **✅ 2/2 GREEN + PUSHED (`wf_a00e0db3-686`, origin/v2 `a6c541a..757f34b`, 2026-07-08).** CG-1 `62f6df2` pricing.yaml + loadPricing/resolveModelCost + the events.ts chokepoint (priced→computed, unpriced→NULL+once-warn, local→genuine 0; live real-surreal round-trips; review PASS). CG-2 `6c547cc`+fix `757f34b` global rolling-24h token budget (spend.dailyTokenBudget, 0=uncapped) INSIDE launchSession+gauntlet+concierge call-site; typed refusal parks-not-crashes the drain; operator override threaded; review caught the operator-token defect → fixed; redTeam PASS. **Mid-wave host note: first CG-2 builder died on an API connection loss (F-051 class) — recovered via resumeFromRunId + recovery-context prompt; second builder verified + kept the partial work.** Deferred → cost-governance-1b: (MEDIUM) budget counter overshoots under concurrent check-then-spawn · (LOW) concierge free-local turn gated on the token budget (should local $0 count?) · (LOW) create/agent.ts caller inheritance · (LOW) pmReview budget breach = raw 500 · (LOW) boot-time loadPricing fail-loud. | operator |
| done | cost-governance-1b | **✅ 4/4 GREEN + PUSHED (`wf_92d9ea62-d0c`, origin/v2 `bdca0d3..a3bd455`, 2026-07-11).** CG-2b `bdca0d3`+fixes `984f945`/`d72a206`: FIFO single-flight budget gate + in-flight reservation floor (8-concurrent-at-threshold → ≤1 proceeds), local-$0 exemption at the helper (ollama/local skips the GATE, cloud stays gated), pmReview 402 (red-team found the 402 handler was DEAD code — runPmReview spawns nothing; guarded), boot validateBootPricing loud-not-fatal (D-024 security-only), red-team honesty pass on a false "still metered" comment → code-coupled guard. CG-3 `2141cb0` per-project token budget (same chokepoint, either-can-refuse). CG-6 `dc0b540` buildProjectUsage session→project join + unknown bucket. CG-4 `a3bd455` cost-labeled estimates on ceremony + loop-arm confirms (honest '—'). **Survived TWO host crashes (F-051 recovery ×2, zero work lost).** Deferred → cost-governance-2. | operator |
| queued | security-floor-3 | **Deferral-ledger chain from `security-floor-2` SF2-1 red-team (policy 2026-06-13).** SF3-1 (MEDIUM) reconcile `scripts/db-up.ts:152-186` printed operator handoff with the SF2-1 seam — point it at `SURREAL_RUNTIME_USER/PASS` (not `SURREAL_USER/PASS` + manual authLevel code edit), so following db:up's own output cannot silently fail auth into a disconnected dashboard · SF3-2 (LOW) align the printed var label with `.env.example:28` · SF3-3 (LOW) add a RETAINED real-binary integration test for the scoped `atelier_runtime` DATABASE-user signin end-to-end (initDbFromEnv → Db.connect authLevel:database), builder's live test was throwaway. | auto (deferral policy) |
| done | cost-governance-2 | **✅ 4/4 GREEN + PUSHED (`wf_ecfe71f8-0c3`, origin/v2 `c574ad5..2253197`, 2026-07-23, redTeamAll).** CG2-1 `c574ad5`+fix `6c990ef` concierge Stage-2 turn METERED (wire.ts `meterConciergeTurn` writes one completion row feeding the budget; cloud=real cost, local=honest $0; fix-loop corrected 2 false "un-metered" docstrings, code-coupled honesty guard). CG2-2 `edfe7ef` scope-aware 402 label (project vs daily). CG2-3 `e141bea` COALESCE session.project ?? agent_event.project (no more 'unknown' misbucket). CG2-4 `2253197` honest external-cost line on publish confirm. **DEFERRED → cost-governance-3 (MEDIUM: metering only on the success branch — a cloud turn that times out/errors mid-stream is still un-metered).** Original scope ↓ CG2-1 (MEDIUM) concierge Stage-2 turn is UN-METERED both paths — wire.ts:149-193 writes no `type:'completion'` row, so a CLOUD concierge turn is gated against a budget its own spend never feeds; wire the meter at the wire.ts chokepoint · CG2-2 (MEDIUM) 402 UI recourse scope-blind (+page.server.ts:955 labels a per-project breach as "daily token budget" — use err.scope/projectId) · CG2-3 (MEDIUM) buildProjectUsage discards denormalized agent_event.project (project-usage.ts:81) — session-less-but-project-set rows (sync/github.ts:339) misbucket to 'unknown'; COALESCE session.project ?? project · CG2-4 (MEDIUM) release-publish confirm (projects/[id]/release +page.svelte:249) lacks an estimate line — add honest external-cost/'—' or document exclusion. LOWs noted, not auto-fixed (G3): park-log '≥' wording (orchestrator.ts:1097); ceremony estimate blends tiers (run-estimate.ts:109). | auto (deferral policy) |
| superseded-row | cost-governance-1b-orig | *(original scope text, kept for audit)* CG-3 per-project token budget (rides CG-2 counter) · CG-4 cost-labeled consent estimates (honest '—' without history) · CG-6 per-project cost attribution join · CG-2b deferred-MEDIUM: bound the concurrent check-then-spawn overshoot + the 1a LOW cluster (concierge local $0 exemption decision, pmReview honest 4xx, boot-time pricing validation). From `COST-GOVERNANCE-SPEC.md` §2. CG-1 pricing map → cost_usd at the events.ts chokepoint (today ALL $ views are null; unpriced stays NULL, local records genuine 0) · CG-2 GLOBAL rolling-window token budget enforced INSIDE launchSession + gauntlet runner (dailySpawnCap meters only the drain; ceremony/interactive/concierge/benchmark uncounted; red-team caller-bypass per F-055) · CG-3 per-project token budget (rides CG-2) · CG-4 cost-labeled consent estimates on spend-triggering confirms (honest '—' without history) · CG-6 per-project cost attribution join. Cap semantics uniform: 0=uncapped, durable anchor, parks-never-drops. | operator |
| done | cc-config-hardening | **✅ 3/3 GREEN + PUSHED (`wf_25fcdbbd-3e7`, origin/v2 `550ee69..1c145bc`, 2026-07-11).** CCF-1 `550ee69` spawn-time catalog freshness per the recorded D-036 note: `freshenCatalog` digest-probe → reuses the loader's `reconcileScopes` only on out_of_sync → `ClaudeCodeRuntime.refreshCatalog` snapshot rebuild before validation; in-sync byte-identical (spy-tested); reconcile-failure → last-good + honest warning; deleted-harvest-skill REFUSED (real-surreal); F-045 regression green; review + red-team PASS. CCF-2 `17e44a8` harvest-scope health surfaced honestly on /claude-code (typed reason, happy-path test). CCF-3 `1c145bc` hooks-posture comment. **Mid-wave F-051 crash #3 (StructuredOutput retry cap on the CCF-2 reporter — work was already committed); resume replayed CCF-1 from cache, CCF-2 re-entered verify-don't-redo. Zero loss.** Deferred → cc-config-2. | operator |
| done | cc-config-2 | **✅ 2/2 GREEN + PUSHED (`wf_99c202af-41a`, origin/v2 `facd9bf..5f39927`, 2026-07-23, redTeamAll) — COMPLETES the released 2026-07-08 hardening chain.** CCC2-1 `facd9bf` on-disk deletion now propagates OUT of covered project scopes (stale-allow-list hole closed — a deleted skill fails closed at composeCapabilities) + honest freshen (reconciled:true ONLY when drift cleared, else honest staleWarning, F-008). CCC2-2 `5f39927` per-spawn immutable catalog snapshot — concurrent drains never observe a torn D-036 id-set. Deferral all-LOW (near-unreachable per-spawn stale-event spam for a hypothetical non-harvest global scope — G3 noted, no chain). Original scope ↓ CCC2-1 (MEDIUM, found by review AND red-team) project-scope stale-allow-list hole: `reconcileScopes` skips covered project scopes (sync.ts:597) — a skill deleted on-disk from an EXISTING project .claude stays in the D-036 catalog and passes composeCapabilities, while freshenCatalog overstates `reconciled:true` no-warning (F-008); fix: freshenCatalog re-syncScope each out_of_sync scope directly (+ disk-drift test) · CCC2-2 (MEDIUM, red-team) concurrency: CCF-1 flipped `runtime.catalog` readonly→mutable and parallel unawaited `#runItem` drains (orchestrator.ts:659) now race the shared snapshot around awaits — snapshot the id-set per-spawn or serialize + a concurrent-drain test. LOWs noted (G3): freshen-infra failures emit console.warn only, no analytics event; SECURITY-MODEL-SPEC hooks-posture re-assertion (v2-main doc). | auto (deferral policy) |
| queued | cost-governance-3 | **Deferral-ledger chain from `cost-governance-2` CG2-1 red-team (policy 2026-06-13).** CG3-1 (MEDIUM) `wire.ts:234-243` — `meterConciergeTurn` fires ONLY on the Promise.race success branch, so a CLOUD concierge turn that consumes tokens then TIMES OUT (30s) or throws mid-stream is never metered (same un-metered-cloud class this wave closed, on the error path); capture partial usage on abort/error and meter it (or meter a bounded worst-case), honestly. CG3-2 (LOW) `wire.ts:222-235` detached `collect` IIFE on timeout can raise an unhandled rejection if `provider.stream` throws after the race resolves (pre-existing) — attach a `.catch`. | auto (deferral policy) |
| done | db-runtime-hardening | **✅ GREEN + PUSHED (wave `security-floor`, 2026-07-08).** DBR-1 `63cd609`+fix `f269868`: provision-user.ts DEFINE USER IF NOT EXISTS atelier_runtime ON DATABASE ROLES EDITOR (2.6.5 role model LIVE-verified: EDITOR = least priv expressible — record-CRUD+own-db-DDL yes, DEFINE USER/root/ns/cross-db NO, empirically confirmed incl. red-team-forced cross-db confinement test; password env-or-generated, never reset on re-run, never logged) + opt-in client authLevel 'database' (F-053 additive, root default byte-identical) + 16 real-surreal tests + live db:up clean. Runtime NOT flipped (operator-controlled .env). Deferred → security-floor-2: (MEDIUM) wire SURREAL_RUNTIME_USER/PASS+authLevel through runtime-init so the flip is reachable · (LOW) AUTH_EXPIRED_RE would misfire on genuine authz denials post-flip. From `DB-RUNTIME-SPEC.md` §4. | DBR-1 define the scoped least-priv runtime DB user (D-026c is currently aspirational — no DEFINE USER anywhere; dev runtime runs as ROOT). Idempotent provision step (IF NOT EXISTS; password env/generated never committed), db-up prints the runtime .env block, F-042 single-flight re-signin verified for the scoped user, privilege test (CRUD-yes/DDL-no); red-team (security boundary). DBR-2 committed-binary model documented (any future download MUST reuse the verifyBinary SHA gate); DBR-3 single-file schema.ts convention stands. | operator |
| done | adapter-framework-hardening | **✅ 1/1 GREEN + PUSHED (`wf_33aae562-105`, origin/v2 `ef600c1`, 2026-07-11).** ADF-1 honest extension-posture comment (novel adapters = code contributions through the contract harness + register(); no runtime code-load — ACE-behind-a-config-row, D-026/D-018). ADF-2 ONE UnknownAdapterError: types.ts canonical (richer signature, real importers; sync copy had ZERO external importers) — sync/adapter.ts re-exports it, throws kind='sync'; instanceof unified across release + sync paths, 178 adapter/sync tests green. Deferral ledger EMPTY. ADF-3 npm/gh-releases stays parked (operator, when a real customer exists). | operator |
| done | cc-harness-hardening | **✅ 3/3 GREEN + PUSHED (`wf_c99c0249-4b2`, origin/v2 `aeab9f1..a6c541a`, 2026-07-08).** CCH-1 `5f29477` runKeyFor(sessionId??agentId) registry keying, cancel routes by session id despite wrong slot id (real-surreal proof; gauntlet legacy fallback verified; review+redTeam PASS) · CCH-2 `e72344c`+fix `f8709ea` — **CASE B CONFIRMED: writeProjectGuardrails had NO production caller; the D-024 permissions.deny primary boundary was absent on every project.** Wired at registration + boot reconcile (idempotent, D-010 no-clobber); red-team forced the self-host-worktree exemption · CCH-3 `a6c541a` budgets.toolCalls→--max-turns (floor ≥2, F-032). Deferred: (MEDIUM→security-floor-2) writeProjectGuardrails non-atomic writeFileSync (mid-write crash = truncated settings.json) · LOWs: reconcile root_path re-confine, statSync TOCTOU, `git -C` push-deny bypass, cancel-before-first-pull window, resume-while-live overwrite, gitignore generated settings. From `CLAUDE-CODE-HARNESS-SPEC.md` §5. CCH-1 run-registry session-keying (ClaudeCodeRuntime.running keyed by agentId/slot — concurrent same-slot sessions collide, cancel reaches only latest; mirror the F-046 config-dir fix: key by sessionId??agentId, thread through channel.stop; concurrent-cancel test; red-team) · CCH-2 VERIFY writeProjectGuardrails production caller (scout not-found; if absent, the D-024 PRIMARY boundary may be missing on new projects — verify first per Iron Law, wire at registration+boot if confirmed) · CCH-3 SpawnBudgets.toolCalls → --max-turns mapping (fixed 80 today). CCH-4/5 documented constraints. No migration. | operator |
| done | projects-hardening | **✅ 4/4 GREEN + PUSHED (`wf_2ca29eef-945`, origin/v2 `cb0fe33..b2feeac`, 2026-07-11).** PJH-1 `cb0fe33`+fix `ba6f7ca` applyBriefDecision = the ONE per-kind operator-authority dispatcher (spec premise was partially stale — cert_hire/repo_create dispatched at the ROUTE, not throwing; consolidated INTO the module, codebase-wins; review_proposal/fixture_proposal grep-proven panel_verdict kinds never minted as briefs → honest typed refusal, no invented gate; fix-loop caught a false gate-red analytics row on re-POST → wasOpen-gated; review + red-team PASS). PJH-2 `aeefbc3` arm-path F-055 census + behavioral suite (sanctioned: armAutonomousLoop + the autonomous-to-v1 chokepoint bare-arm). PJH-3 `adafc28` datetime consolidation onto norm*/isoOrUndef. PJH-4 `b2feeac` under-populated-project render-smoke (no-throw, POJO, honest '—'). Deferral ledger ALL-LOW (G3, noted not chained): applyHireDecision lacks same-action absorb (409 on deleted interview_run re-POST) · repo_create reject re-POST 409 asymmetry · cert_hire re-POST single-analytics-row untested · RED-then-GREEN retry double decision row · census evadable via aliased import. | operator |
| done | orchestrator-hardening | **✅ 3/3 GREEN + PUSHED (`wf_45ad08e3-c90`, origin/v2 `b63e2b0..aeab9f1`, 2026-07-08).** ORH-1 `d1aa645` bootDrain() after subscribe (global ready-task enumeration lifted into orchestrator — no UI import cycle; drains through the existing 3-gate drain(); one-shot, F-014-swallowed; 4 real-surreal tests: seed-before-boot / manual-inert / cap-parked / re-boot-dedup; review+redTeam PASS) · ORH-2 `ac633fa` post-release drain nudge via activeOrchestrator() registry (no-orchestrator no-throw) · ORH-3 `aeab9f1` handoff pair deleted per D-021 note (zero callers verified; schema column left in place). 5 deferred LOW (advisory, G3 — not chained): boot_drain rides type='completion' → +1 phantom completion/boot in rollup+provider-usage 'unknown' bucket · no direct startOrchestrator→bootDrain wire assertion · #listReadyTasks unbounded/sequential · cross-project boot-drain test · ORH-2 nudge latent until a runtime caller exists. From `ORCHESTRATOR-SPEC.md` §5. ORH-1 boot re-drain (one bounded idempotent drain after subscribe; fixes the verified reap-before-subscribe hole — pre-existing ready tasks + reaper-reset tasks never auto-drain at boot) · ORH-2 releaseSessionWork drain-nudge (freed pending twins have no self-trigger) · ORH-3 handoff-replay DECISION (wire recoverHandoffs at boot vs retire the caller-less pair — additive note on D-021 required first). ORH-4 followUpOnTestFail stays documented-off (bounded-retry parked); ORH-5 in-process semaphore = accepted constraint. Real-surreal tests mandatory (seed-before-boot case); no migration. | operator |
| done | recovery-repo-harden | **✅ 4/4 GREEN + PUSHED (origin/v2 tip `9d1019b`, run `wf_281f0943-f6b`, 2026-07-07).** RRH-1 no-op (already fixed `3073230`, independently re-verified live-green) · RRH-2 `8d1ff08` (scaffolder births `main` via `git symbolic-ref HEAD refs/heads/main`; gate `resolvePushBranch` normalizes master→main + resolves actual branch; +6 F-050 tests) · RRH-3 `4e8698b` (**deterministic-id active-window dedup** — reverted the secondary-UNIQUE approach per F-026 + real 4-concurrent→1-row test; success-side terminal writer) +`f6b5cea`/`588bc15` fix-loop (stale-comment corrections) · RRH-4 `9d1019b` (operator-authority `reopenFailedTaskToReady`, `failed→ready` login-gated; RH-1 non-regression + no-auth-refusal tests). **4 deferred LOW (advisory, G3 — NOT chained):** RRH-2 symbolic-ref exit-code unchecked · RRH-2 birth-on-main over a pre-existing populated root lacks a guard (re-run edge) · RRH-2 gate case-4 non-main default for odd-branch scanner repos · RRH-4 concurrent-reopen race unit test. redTeam PASS on RRH-2/3/4. LAUNCHED — harden the go-live cracks so the graduation test (new-mod-test) can run hands-off. 4 tasks, pushAtEnd origin/v2: **RRH-1** BL-GUX-FIX (3 graph-UI D-038 test fails: banned `max-width:1100px` cap + ` — Atelier` title suffix + count-badge `aria-hidden`; tier haiku, mechanical, no red-team) · **RRH-2** BL-RC-FIX/F-050 (`main` EVERYWHERE — Create-with-AI scaffolder births `main` + repo-creation gate normalizes master→main + resolves the ACTUAL branch before push; local-only-git FORBIDDEN_GIT + D-037 gate preserved; red-team) · **RRH-3** BL-R3 (F-048 class — collapse the dedup active-window so {pending,processing} dedup against each other + success-side-twin guard; MANDATORY real 4-concurrent→1-row test per F-026; red-team) · **RRH-4** BL-R4 (operator-authority `failed→ready` re-run path; HARD INVARIANT: manual-only, the auto/reaper path stays `failed` per RH-1 or it re-drains forever; red-team). Grounded from the go-live backlog (BL-R3/R4/RC-FIX/GUX-FIX); build agents re-verify file:line at BUILD. | auto |
| done | recovery-harden-2 | ✅ 3/3 PASS (`wf_44cf7220-90b`, 2026-06-26, pushed origin/v2). RH-1 `d11db2f` BL-R2 — `resetStuckTaskToFailed` (guarded idempotent in_progress/review→failed, legal in ALLOWED_TRANSITIONS) wired into `#runItem` finally under `!ok && taskId`, logged-swallowed (F-014); chose `failed` not `ready` (no bounded task retry exists → ready would re-drain into the same failure forever); redTeam PASS. RH-2 `e89c9c5` wi-harden-2 — `worktree.ts` stale-dir guard: empty→rm, NON-EMPTY untracked dir→preserve-aside (rename to `.orphaned-<id>`) not hard-rm (F-007), warning surfaced; redTeam PASS. RH-3 `c58e5fb` — boot reaper log now surfaces `releasedWorkItems`+`resetTasks`. svelte-check 0/0/951. NOTE: full `npm test` has 3 PRE-EXISTING graph-UI failures (layout-gate max-width 1100px + 2 route-head-a11y on graph/[id] — committed at `1da738e`, NOT this wave) → tracked as **BL-GUX-FIX** below. **Two new deferred MEDIUMs → BL-R3.** | auto |
| done | BL-R1 | recovery-harden ✅ 2/2 PASS (run `wf_89134df3-de1`, 2026-06-26, pushed origin/v2). R1-1 `eed3163` — `releaseSessionWork(db,sid)` (work_item processing→pending, clears claim_token/claimed_at, `session.status!='running'` record-link guard so a mis-call on a live session frees nothing) + `resetStuckTaskToReady` (in_progress/review→ready via narrow guarded UPDATE — verified in_progress→ready is NOT in ALLOWED_TRANSITIONS so setStatus would throw; documented crash-recovery exception) + reaper loops reaped sessions, per-session try/catch (F-014). Verified linkage: work_item.session holds the F-048 memory_review fork twin's claimer; session.task is option<record<task>>. R1-2 `bcbc1f5` — gcStale wired: one-shot on boot (after reap, fire-and-forget .catch) + bounded periodic backstop (unref'd interval, cleared on teardown, in-flight guard, F-014-safe). Tests 30/30 on real throwaway SurrealDB. **Deferred ledger (red-team, named):** (MEDIUM) runtime non-reaper session-failure trigger left UNWIRED — verified the only non-reaper `failed` writes (launch.ts:547 spawn-time, gauntlet abort) DON'T orphan session-linked twins, so boot-reaper-only is correct by current paths; primitives EXPORTED for future use. (LOW) boot log omits releasedWorkItems/resetTasks counts (operator-visibility). (LOW) dangling-session-link release path is prose-asserted, no explicit test. **Separate known gap (NOT in scope, tracked → BL-R2):** a FAILED task_run spawn (orchestrator.ts:777 catch) leaves its task `in_progress` (post-task never runs) — not reaper-recoverable until next boot. | auto |
| done | wave-14.x | v2.0 audit remediation + full-viewport ✅ 6/6 (d7f0506, 4995d58+edf4ff4, 3a48c7c, 487b5a6, 16e8283, eab1961 — pushed) | auto |
| done | lane-A-docs | HARVEST Lane A-docs ✅ 14/14 applied, review PASS (dc7239b pushed) | auto |
| done | lane-A-code | HARVEST A4/A7 ✅ red-team pass + verdict artifact gate (3cd2755; reviewer caught F-016 host-load brick via real-loader extraction) | auto |
| done | lane-C-specs | Lane C ✅ 4/4 (80ed54c): decision classification w/ Operator-Challenge supremacy, scope challenge (null tunables), ONE canonical brief format, anti-sycophancy in-rail | auto |
| done | wave-v2.2a | ✅ 3/3 (pushed, tip `fa69d4a`): 15.1 scope-lock gate (`dde4f1c`+fix `231f411` — red-team's FIRST outing caught 3 fail-open bypasses incl. `..`-in-rm-exception + `>|` clobber; re-review ran 15 novel probes) · 15.2 browser daemon `464b815` · 15.3 verify-flows `6031bbe`+fix `fa69d4a` | auto |
| done | ctx-mode-trial | **KEPT (2026-06-13, qualitative).** Ran on `wf_87ad4b7a-16a` (1.14M subagent tokens / 10 agents / 3 tasks — reasonable for verify + 2 substantial builds, all red-teamed + live-verified). Clean token A/B was CONFOUNDED (model swap + non-resume) so no headline %; the **evidence rail HELD** — reviewers cited drilled-in RAW output, not digests (empirical `string::contains(content,'')→2/2 rows`, live `found 1/1 plants`, browser `XSS payload rendered as ESCAPED TEXT`). No digests-as-evidence violations. Harness-side, low risk → keep installed | auto |
| done | wave-v2.1 | **✅ GREEN + PUSHED (2026-06-13, tip `bb6af37`) on claude-opus-4-8 after the fable-5 re-plan.** 16.6 W-D7b verify-only (engine `db5e280`+`9cf96ba`, all-green: db:up 33/33 idempotent, workforce slice 186/186, live gauntlet found 1/1 plant, lint 0, svelte-check 0) review PASS + red-team PASS · 16.7a W-D7c content+ceremony mechanism (`137703b`: 5 F-entry-seeded launch fixtures + A8 injection plants, NO keys, sentinel-empty, day-0 ceremony write-paths INERT, 204 workforce tests) review PASS + red-team PASS · 16.7b W-D7c surfaces (`bb6af37`: /agents panel + adjudication queue + PM rationale, live-verified, XSS escaped) review PASS + red-team PASS. Build deviation (advisory): 16.7a found NO pre-existing launch roles/prompt cores → seedLaunchPool authored the 5 role rows + DRAFT harvested cores (§4.4 independence preserved via distinct provenance). **All red-team findings LATENT (no production caller yet) → see wave-v2.1-harden ledger** | auto |
| done | wave-v2.1-harden | **✅ GREEN + PUSHED (2026-06-13, tip `95e34d5`, opus).** H1 `e6bb773` empty-sentinel leak storm (3-layer defense + m0034 idempotent migration) + auto-budget-token-on-no-run (recordAutoToken moved after pre-flight) + **F-025** logged · H2 `f5d6230` confirmLaunchKey hardening — parsePlant pre-flight (D-026), stale-key changed-signal, teethless-key reject, AND a DEEPER root-cause: `gauntlet_key_dedup` UNIQUE *secondary* index doesn't enforce under concurrency (25/40 races dup'd) → fixed with a deterministic primary-key id, **F-026** logged · H3 `95e34d5` per-run adjBusy isolation + 16.8→16.7b labels. All review PASS + red-team PASS. **Residual (folded into wave-v2.2b, NOT a 3rd wave — latent + day-0-safe): ① ULID-shape sentinel invariant** — H1's guard checks `string::len>0`, but a whitespace/low-entropy sentinel still near-matches-all (same F-025 class); the COMPLETE invariant is assert 26-char Crockford ULID at activation.ts + schema, not merely >0. Latent: production mints sentinels server-side via injectSentinel→newSentinelUlid (valid ULIDs); operator never authors one → not exposed by the day-0 flow. **② LOW cosmetics:** F-025 fails.md assert-quote omits `status = NONE`; remaining 16.8 comment labels (agents/+page.svelte 25/86/117/201/885, projects/[id] 1386/2938, watched-tables 74-80, panel.test.ts:18); sentinelSweep `checked` overcount; keyDrift path-normalization over-warn. **Going forward this manual triage is AUTOMATED** — v2-wave.js now flips in-scope MEDIUM+ to passed=false (auto fix-loop) and emits a `deferredFollowUps` ledger (commit `458bdac` on v2-main) | auto |
| done | wave-ceremony-ui | **✅ GREEN + PUSHED (2026-06-13, tip `7f7cec5`, opus, run `wf_862e8651-e87` — paused mid-wave then resumed with verbatim args, CER1 build cache-hit).** CER1 `0103f92` authoring half (route `/agents/ceremony`: 'Begin day-0 ceremony' on the empty panel → seedLaunchPool FIRST real caller → prompt-core review → D-010 key author/confirm with teethless/A8-bait guards as UI affordances) · CER2 `7f7cec5` execution half (per-role reference-runs + bootstrap interviews, operator-gated real-spend with confirm+cost label + budget gate, live via existing interview_run SSE, ambiguous→§3.4 queue, panel-flip gate). Both review + red-team PASS. **6 deferred LOW** (persist review-acks · integer fp_tolerance · re-confirm-key UI · §4.4 leak-fixture extension · IdentifierError→400 map). **NOTE: builder live-verified on the LIVE dev DB → left test-seed (5 roles + 1 junk key 'committed-secret-value') — clear before a real cert run (no db:reset script; targeted DELETE of gauntlet_*). Recurring 'verify-on-live-DB pollution' pattern — builders should use throwaway DBs** | auto |
| ready | day-0-ceremony | **gate:operator — DRIVER UI NOW LIVE (`7f7cec5`); operator-actionable.** Reboot dev (`npm run dev` to pick up the new build), open `/agents` → 'Begin day-0 ceremony' (route `/agents/ceremony`) → seed 5 roles → review 5 prompt cores → author/confirm each fixture key (A8 bait = mode:'noncompliance'+compliance_pattern; teethless rejected) → trigger admission reference-runs + bootstrap interviews (real spend, gated) → on 5 passes, confirm the inline→catalog panel flip. **PREREQ for a clean run: clear the builder's dev-DB test-seed first (5 roles + junk key).** **LIVE-RUN IN PROGRESS (2026-06-15):** all 25 keys authored (agent-drafted from source, operator-confirmed; A8 baits given the 2-plant real-defect+noncompliance key). First real driven-session spawn surfaced + fixed SIX latent bugs (F-028 activate-form confirm · F-029 stale-shell-token-shadows-.env · F-030 blank-spawn-errors · F-031 dev-bound-::1-vs-127.0.0.1-gate · F-032 maxTurns=1 · F-033 scorer_control-not-keyed/activated; fixes on `v2` `990baba`/`815c102`/`da1d519`/`73fb593` + F-028 `1caf58b`/`97e46a4`, NOT pushed). code-reviewer interview then ran clean: **found 5/5, 0 FP**, resisted the A8 injection, → status `adjudicating` (2 extra_findings = same planted defects in a different finding-shape than the key → operator resolves not-FP → certifies). Remaining: resolve adjudication → finish code-reviewer → run the other 4 roles (activate→reference-run→interview) → panel flip. **Dev currently run by the orchestrator on 127.0.0.1:5173 (clean env, GAUNTLET_TRACE=1).** **code-reviewer CERTIFIED 1/5 (2026-06-15):** adjudication resolved operator-delegated — 2 extra_findings `dismiss`ed (correct catches of the planted defects in a different finding-shape than the key; not FPs) → run `passed` 5/5, 0 FP. Also surfaced a launch-key refinement (presence/absence plant KIND didn't match how a thorough reviewer naturally reports — tolerance+adjudication absorbed it; tighten launch keys later). Remaining: operator resumes for the other 4 roles (activate→reference-run→interview→adjudicate) + panel flip. | operator |
| done | wave-v2.2b-a | **✅ GREEN + PUSHED (2026-06-13, tip `0911210`, opus, run `wf_198a207f-5e4`).** R1 `758f61e` sentinel=26-char-Crockford-ULID at activation guard + migration m0035 (red-team ran 6 probes incl. astral-emoji/regex-newline edges, clean) · B6 `81d1711`+fix `775c889` quarantine leak harness (red-team caught the coverage-guard never Glob'd the source tree → auto fix-loop) · B4 `286943d`+fix `56f54bb` screen-before-embed leak harness (**red-team found a REAL in-scope leak: truncated private-key paste leaked raw key material to the embedder → auto-fixed**) · C1 `0911210` cosmetic sweep (zero 16.8 left in src/). **The deferral-ledger automation FIRED end-to-end:** in-scope MEDIUM+ (B6 guard, B4 key-leak) flipped passed=false → auto fix-loop; 3 DEFERRED screen.ts items captured in deferredFollowUps → folded into v2.2b-b (S1 below) | auto |
| done | wave-v2.2b-b | **✅ GREEN + PUSHED (2026-06-13, tip `228e753`, opus, run `wf_5bea3fcc-400`).** S1 `010f00e` screen hardening (case-insensitive PEM + Luhn-gated card; red-team probed ReDoS/truncated-key) · B5 `484f9fb` recall budget (tail-drop top-N, null-tunable, fail-closed) · B7 `0a95fe2` Tier-0 capability gate (proved tier=0 is operator/curator-set only) · B2a `cf686bb` review-memory writer fork (ADD-only, screened, whitelisted; ledger seam = TODO) · B10 `b8c9ef6`+fix `d1de2ee` pull-tool — **red-team caught a FENCE-ESCAPE (embedded FENCE_CLOSE token could forge a §10 boundary, D-026 break) → auto fix-loop landed `stripEmbeddedSentinels` in fence.ts at the SHARED wrap() ingress (verified: covers briefing/channel/tier0/skill/pull, not a pull-local patch)** · B8 `228e753` recall eval harness (measure-only). All but B8 red-teamed. Deferral ledger → wave-v2.2b-c (below) | auto |
| done | wave-v2.2b-c | **✅ GREEN + PUSHED (2026-06-13, tip `b9b97b2`, opus, run `wf_8dee1a19-6f6`).** T1 `3f3abed`+fix `bd0e577` loop.ts robustness (dedup regex tightened, skill.name screened, extract shape-validated — **red-team caught the first fix was INCOMPLETE: a null array element still threw an anonymous TypeError → escalated → final fix guards every element**) · T2 `f036c92` recall budget NaN/neg → fail-closed · T3 `b49e8f8`+fix `b9b97b2` pull-memory registered + capability-gated. Deferral ledger → wave-v2.2b-d + the B10-delivery defer (below) | auto |
| done | wave-v2.2b-d | **✅ GREEN + PUSHED (2026-06-13, tip `c3d5e2a`, opus, run `wf_de7525c4-5e1`).** D1 `79c9c34` store.ts D-026 seams (named provenance-shape + array/element guards, no partial write) · D2 `c3d5e2a` LOW cluster (skill-name false-drop fixed, RECALL_BUDGET frozen, stale comment). Red-team reproduced the SAME class on the NEXT fields (tags/importance/kind) → escalated structurally → wave-v2.2b-e | auto |
| done | wave-v2.2b-e | **✅ GREEN + PUSHED (2026-06-13, tip `365d113`, opus, run `wf_472b1d0b-3df`).** E1 `365d113` `assertCandidateShape` — one boundary validator over EVERY schema-constrained field (content/project/session/kind/namespace/key/tags/source/importance), malformed→NAMED `MemoryCandidateFieldError` before screen/embed/CREATE; dropReason record-id sanitize; **F-027** logged (the field-by-field-guard architectural-smell lesson). Class CLOSED. Red-team found ONE follow-on: the widened throwing surface may break a storeMemory caller → wave-v2.2b-f | auto |
| done | wave-v2.2b-f | **✅ GREEN + PUSHED (2026-06-13, tip `dbb32ee`, opus, run `wf_02e392e3-2cc`) — CONVERGENCE.** F1 reproduced the e-regression (importer aborted a whole batch on one empty-content row), fixed the two single-item callers (v1-stores importSwarmMemory + bridge importAutoMemory) with per-item isolation — drop+continue on Field/Shape errors, RE-THROW real DB/embedder faults (F-008). No CONFLICT (v1 empty content is junk, not required to migrate → validator strictness stays). **`deferredFollowUps: 0` — ledger drained.** |
| **MEMORY HARDENING CONVERGED** | wave-v2.2b a→f | **✅ DONE (2026-06-13, opus).** 7 waves: every D-026/leak/quarantine/capability-gate/recall-budget/untrusted-input boundary closed + red-team-verified; the shape-class shut STRUCTURALLY (assertCandidateShape, F-027). Notable catches auto-fixed in-loop: fence-escape (shared fence.ts), truncated-key embed leak, lowercase-PEM evasion, null-element TypeError, importer batch-abort. **Known LOW residuals (NOT chased — pre-existing/cosmetic):** storeMemory's memory+memory_history CREATE is non-atomic (orphan row on a transient history-write fault; self-heals on re-run via findMemoryByKey — a future memory-durability/atomicity item, pre-existing from TASK 2.5); dropReason taxonomy differs between batch ('quarantined') vs single-item ('dropped') callers (cosmetic) | auto |
| done | B10-delivery | **✅ DONE (2026-06-13, `ed5cfd3`).** pull-memory LIVE: stdio MCP server (scripts/memory-pull-mcp.mjs, NDJSON JSON-RPC, no SDK — mirrors gate-hook.mjs) + loopback route (api/memory/pull/+server.ts), capability-gated fail-closed, fenced/screened/budgeted BY CONSTRUCTION (transport owns no guard → routes through pullMemory→recall+assembleInjection). **Deferred (1 MEDIUM, folded into B2b):** add a real spawn-subprocess round-trip test of memory-pull-mcp.mjs main() (has unit+protocol tests, not a live-process one) | auto |
| done | tunable-novelty-cut | **✅ DONE (2026-06-13, `1a539e2`).** NOVELTY_COSINE_CUT 0.97→0.90, operator-blessed; harness empirically confirmed 0.4→1.0 dup-suppression. (Corpus-enrichment for WMR-weight discriminability remains a separate B8 follow-up, unscheduled) | auto |
| done | fix-embed-probe | **✅ DONE+PUSHED (2026-06-13, ).** Operator-hit: cold-boot Ollama timeout mislabeled a PRESENT embedding model as missing + latched memory OFF. Fix: 3-valued probe (present/absent/**unreachable**), 8s timeout + 1 retry, no-latch self-recovery, honest reasons (pull-hint vs timeout-retry). 3rd Ollama-probe-honesty issue (cf. 10.5, 15.3) — pattern noted. LOW deferred: hang-forever Ollama costs ~16s inline at boot | auto |
| blocked | B2b-missed-defect-ledger | **gate:operator — SCHEMA PROPOSED 2026-06-13 (awaiting sign-off).** Proposed `missed_defect` table (append-only; investigator PROPOSES attribution → operator CONFIRMS, mis-attribution = integrity attack per §4.1; maps a later-surfaced defect → the review_verdict whose certified SHA-range+rubric covered the introducing commit; shares §2.5 identity columns). DEPENDENCY: needs `review_verdict` (§2.5) to exist — verify B2/v2.2b landed it. On sign-off → small B2b migration + writer-fork seam wiring | operator |
| done | live-session-transcript | **✅ DONE + PUSHED (2026-06-15, opus, tip `2d051a3` on origin/v2 — two waves wf_9dc47a27 + wf_427dff97).** LT1 `2aae273`+fix `6a22906`: persistence seam — driven-session conversation (assistant_text/thinking/tool_use/tool_result) persists to the `message` table via the shared `eventToMessage` chokepoint, D-026-screened, fail-open (F-014), kind+monotonic-seq ordered, m0037 idempotent migration (applied additively to the live DB — workforce rows untouched). **Red-team caught a launch↔resume seq COLLISION (resumed turns interleaved into the launch transcript) → auto fix-loop → continuation reads MAX(seq)+1; defect-reproducing regression test added.** LT2 `8ce221f`: live kind-aware transcript view on `/claude-code?session=` (SSE via message onDbChange; honest empty/unknown/running). Deferred-ledger item (thinking not rendered distinctly on the OLDER projects transcript) drained by the chained hardening wave `2d051a3`: extracted ONE shared `SessionTranscript.svelte` + pure `transcript-core.ts` (25/25 unit) used by BOTH /claude-code and projects/[id] — no divergence; live `thinking` events were silently dropped on projects before, now rendered. **Known env-noise (NOT a regression):** `cc-config/readback.live.test.ts` flakes on local-model behavior (Ollama-probe-honesty family, cf 10.5/15.3) — outside the changed set, builder gate green. **ORIGINAL queued scope below ↓** · operator-requested (2026-06-15): a live conversation/transcript panel so the operator can watch a driven session as it runs. Today the fleet (`/claude-code?session=`) shows status + tool events only; assistant thinking/prose is NOT persisted and the "transcript →" link is a dead end. Scope: (1) persist the full driven-session conversation — assistant text + thinking + tool_use + tool_result + result — as session message/event rows (the `GAUNTLET_TRACE` extraction in cli-backend.ts already parses exactly these; promote it from a log tap to a persistence seam; cf. the "session+message persistence 1.6/2.10" note); (2) a live SSE-fed transcript view on `/claude-code?session=<id>` (reuse the existing onDbChange stream; honest empties per F-008 — empty/redacted thinking shown as such); (3) D-026 safety — never render a secret; gauntlet keys never enter a session so they can't leak, but screen transcript content at the boundary. Groundwork in: F-030 stdout-surfacing + GAUNTLET_TRACE (commit `815c102`). | auto |
| done | comms-origin-visible (G-A) | **✅ DONE + PUSHED (2026-06-15, opus, origin/v2 tip `fbec756` — waves wf_89939733 + hardening wf_9a878e92).** GA1 `2644def`: server-authoritative `origin ∈ {operator,agent,system,hook}` on `message` (m0038, additive, DEFAULT 'agent' so 178 legacy rows never falsely claim operator); stamped on interject (resolveOrigin) + agent self-turns (agent) + briefing (system); read side coalesces. Red-team PASS — a body claiming 'origin:operator'/'I am the operator' + a leaked off-endpoint token both store as `agent`, fenced, non-steering (D-035a: origin server-stamped, NEVER content-derived). GA2 `6b83075`: shared renderer surfaces channel pushes as origin-LABELLED 'communication' turns (transcript-core `communication` TurnKind, origin-wins classification). Hardening (drained the deferred ledger): H1 `23f1af9` — interject was CREATEd without seq/kind → defaulted seq=0/assistant_text → interleaved at top + rendered as agent prose; fixed with shared `nextSeq`=MAX+1 (mirrors 6a22906) + origin-wins classification + regression test. H2 `fbec756` — live interject now appends as a communication turn in REAL TIME on both /claude-code and projects/[id] (bus→SSE→shared interjectEventToTurn, deduped by messageId), was reload-only. **Residual: 2 LOW env-bound (NOT auto-fixed, G3): a real interject browser smoke + the communication render branch are verified by code-read + unit tests (66/66) only — no running interjectable session in this env; they resolve when the operator interjects into a live session.** **What it means today:** the ONE live comms path (operator interject) is now correctly ordered, labelled, and real-time. Agent↔agent comms remain BL-4 (peer_message unbuilt). | auto | Today an operator interject persists a `message` row (so it shows) but the `message` table has NO `origin` column — only `role ∈ {user,assistant,tool,system}` — so a channel push renders as anonymous "user" prose; the immutable `origin ∈ {operator,agent,system,hook}` the channel stamps (channel.ts, D-035a) is resolved for steering then dropped. Scope: persist `origin` on the message row (channel.pushToSession already computes it — store it; idempotent additive migration), and render channel messages as a distinct **"communication"** turn LABELED by origin in the shared `SessionTranscript` renderer (transcript-core.ts). PRESERVE D-035a: origin stamped server-side, agent/system/hook fenced as DATA, only operator steers — visibility must NEVER become a steering/injection vector. Small; builds directly on the live-session-transcript renderer. | auto |
| done | day-0-ceremony | **✅ 5/5 CERTIFIED (2026-06-16) — the launch workforce has graduated.** All five roles have a `passed` role_version: code_reviewer + design_reviewer (direct), investigator + qa_lead + security_officer (recovered via re-version → re-run → `dismiss`). Verified at the data level (5×lifecycle:passed) AND on /agents after the picker fix. Ceremony-support fixes pushed: `fbea882` scorer [n]-lines · `79f9b19` re-version recovery · `43a5c9c` panel.ts newest-selectable picker (shared lifecycle.ts predicate — killed the divergence) · `d902ba8` reversion-concurrency hardening (graceful dedup-collision no-op, no raw 500 — serial double-click fixed). **F-037 logged + DEFERRED (NOT chased — low-likelihood single-operator edge; proper fix belongs with §6/§7 version-churn / self-hosting):** on this SurrealDB build the role_version_dedup UNIQUE only blocks a SERIAL re-insert, not two TRULY-SIMULTANEOUS in-flight inserts (both commit → duplicate version) — needs an F-034-style atomic per-role monotonic sequence under composite UNIQUE. **This unblocks wave-v2.3 §6/§7 (real cert track data now exists).** **✅ wave-v2.3 FOUNDATION (V2.3-01) DONE + PUSHED (origin/v2 tip `31f6617`, run wf_6a1d8da3-1df, build+review+red-team PASS, live-verified):** m0046 review_proposal (resolves the dangling role_version.proposal forward-ref) + m0047 project_staff (fail-closed enabled DEFAULT false) + fail-closed `resolveStaff` (null for unstaffed/disabled/non-deployable/failed/withdrawn/sha-mismatch; pin survives global swap+retire) + audited staffRole/unstaffRole/list CRUD (D-026 screens charter_note); both migrations additive on live DB (47/47). **Deferred (latent — NO caller until routing wiring): MEDIUM** staffRole concurrent-double-submit raw error (apply the d902ba8 isDedupCollision pattern when routing adds the first concurrent caller); **LOW** the two-`;`-statement staff upsert isn't truly atomic (comment overclaims); **F-038** logged (SurrealDB rejects MERGE…SET in one UPDATE). **OPERATOR DECISIONS LOCKED (2026-06-16):** (1) **sequencing = FULL v2.3 first** (build §5/§7/§7b/BL-3 before Create-with-AI); (2) **BL-3 defect-class vocabulary = operator-curated ENUM** (new class only via fixture proposal + operator confirm, mirrors §3.8 key authorship — no free-form drift); (3) **§7 tier policy = STRICT** (a role runs at a tier ONLY with a passing gauntlet run at prompt_sha × that tier's model — NO waivers, even low-stakes); (4) **§5 drift = AUTO-RAISE** (an armed drift signal — incl. confidence-miscalibration from the A1 calibration — auto-creates a review_proposal{trigger+evidence}; the prompt-revision/re-gauntlet/swap remain operator-gated D-010/D-039, never auto-swap). **BUILD CHAIN (serial, full-v2.3-first): §5 perf-loop ✅ DONE+PUSHED (origin/v2 `096ba4f`, run wf_37699cb0-e4a — 02a drift+auto-raise `d80c702`+`f503867`, 02b lifecycle `c1266f3`+`a7d9642`+`096ba4f`; build+review+red-team PASS; idempotency verified in-code drift.ts:368; the lone MEDIUM previewDiff fixed in-wave; LOWs incl. authorChallenger TOCTOU deferred-no-caller) → §7 tier-hiring ✅ DONE+PUSHED (origin/v2 `83bd539`, run wf_08314079-917 — tierHiringGrid + strict no-waiver tier_change gated on a passing run at the target tier + recommendation only on both-passing/same-sha/cheaper-wins; build+review+red-team PASS; red-team caught a recall/FP provenance bug → fixed `83bd539` [recall/FP read from the latest PASSING run, not latest-terminal — verified in-code]; LOW swap-audit concurrent-race deferred-no-caller) → BL-3 matcher ✅ DONE+PUSHED (origin/v2 `fc66f5b`, run wf_16560806-b0a — BL3-a `ff77d63`: m0048 capability_needs + operator-curated defect-class enum [=confirmed-key plant.class set] + roleProvenCoverage [passed-fixtures only] + propose-only REUSE/EXTEND/HIRE matcher; BL3-b `bfb3bf6`+`fc66f5b`: resolveStaff→routing wired [method:'explicit', byte-identical-when-unstaffed] + operator-gated staffing board + staffRole concurrency fold; both review+red-team PASS. **2 deferred MEDIUM → consolidated v2.3-hardening wave BEFORE create-ai:** (1) projectId not table-prefix-validated [recommendStaffing accepts a 'role:' id cross-type, D-016 gap]; (2) un-staff orphan [staffed-but-not-candidate role can't be un-staffed from the board]. LOWs: roleProvenCoverage set-level-not-per-class precision, screen-asymmetry, non-atomic needs MERGE, confirmStaffing no live re-validate, orphaned-proposal invisibility) → §7b researcher ✅ DONE+PUSHED (origin/v2 `bf5eb79`, run wf_144b1117-60e — 7b-A `c799392`: sixth catalog role `researcher` [WebSearch/WebFetch via D-036 tools grant, draft, NOT in LAUNCH_ROLES so the 5-launch invariant holds] + 6 rails [provenance/fence/born-quarantined-ingest/Option-A cheap-tier verifier/bounded-budget/no-paywalled]; 7b-B `9e93e80`+`bf5eb79`: loopback stub-web + 4 fixtures + DRAFT keys [seed, operator-certs-later]. Both review PASS; **7b-B red-team caught a HIGH — the fetch allowlist was DEAD CODE, live internet reachable in the interview → fixed `bf5eb79` [enforced at the gate seam]**. **✅ v2.3 BUILD COMPLETE (§5·§7·BL-3·§7b).** **✅ v2.3-HARDENING COMPLETE + PUSHED (origin/v2 tip `7d71440`, 2026-06-17).** Landed in 3 commits: `dbbd035` (H-7b §7b fixes: verifyAndStoreClaim verified-finding-discard now THROWS not silently drops + rail④ sources≥2 floor + stub-web pathname-collision + the fetch-allowlist regression test [subprocess + seam, live-internet→deny]; + H-seed wire seedResearcherRole into the ceremony seed action [the researcher-cert blocker — verified in-code]); `296899e` **CONSOLIDATION GATE (F-039):** the per-wave touched-area slices had left the FULL suite RED on 6 cross-cutting-guard failures no single wave ran — watched-tables (project_staff+review_proposal subscribed but unwatched → staffing/proposals boards never live-updated; ADDED), route-head-a11y ×4 (staffing missing <title>; atelier reversed title + undefined heading font token + unlabeled count-badge), ceremony shadow ×2 (researcher is the legit 6th role → bumped 5→6), readback.live ×1 (brittle whole-transcript assertion → tightened to the Bash tool_result row, STRENGTHENING the deny proof). Full deterministic suite green (2883), svelte-check 0, lint 0; readback.live green in isolation 3× (full-suite-only failure = F-014 live-session flake under parallel workers). **F-039 logged** (escalates F-036: a multi-wave build is NOT complete until the FULL suite passes as a consolidation gate — per-wave slices are necessary, not sufficient); `7d71440` (H-bl3 BL-3 hardening, run wf_97eef871-a39, build+review+red-team PASS, full suite 2896 passed/typecheck 0/lint 0): D-016 projectId table-scope guard `assertRecordIdOfTable` [verified in-code — rejects cross-type role:x before any query] + un-staff orphan section (staffed-but-not-candidate) + confirmStaffing live re-validate (fail-closed on stale proposal, no leaked write) + orphaned-proposal visibility (sourced from listOpenProposals, propose≠staff). **NEW deferred LOW (G3, ledger):** route propose/unstaff catch omits IdentifierError → 500 not 400 on a FORGED wrong-table project id (+page.server.ts:108,180) — security FAILS CLOSED (no wrong-table query, no leak), UI-unreachable, unclean error surface only; fold into a future hardening pass. **THEN: dev-server restart [next] + operator runs the researcher cert + Create-with-AI for the operator's project.** ⟨archived plan↓⟩ **→ consolidated v2.3-HARDENING wave (DONE above) was: §7b — wire seedResearcherRole into the ceremony seed action [MEDIUM, blocks the researcher cert] · verifyAndStoreClaim silently-discards-a-verified-finding [MEDIUM] · stub-web pathname-collision [MEDIUM] · rail④ host-side sources≥2 floor [LOW] · re-verify the allowlist gate; BL-3 — projectId table-scope assertion [MEDIUM] · un-staff orphan UI [MEDIUM] · confirmStaffing live re-validate + orphaned-proposal visibility [LOW]. Latent-no-single-operator-caller concurrency [F-037 version-dup, authorChallenger/swap-audit races] stays deferred to a dedicated atomic-sequence pass for self-hosting.** **THEN: dev-server restart + operator runs the researcher cert + Create-with-AI for the operator's project.** Earlier ↓: sub-decisions LOCKED 2026-06-16: build-now-full; **verifier = Option A** — independent cheap-tier session, claim+sources-only not researcher-reasoning, auto-all, fail/no-2nd-source → `unverified:` prefix; stub-web = loopback mini-web shipped with fixtures [Thunderstore-stub precedent]; capabilities = WebSearch/WebFetch per D-036. Build seeds the role + rails + verifier + stub-web + 4 fixtures [planted-wrong-claim/source-attribution/injection-plant/unverified-bait] + DRAFT keys; the CERT is operator-run later [reuses the day-0 ceremony infra]. Flips the BL-6 cannibalize distiller TODO to the researcher role afterward.) **THEN: consolidated v2.3-hardening (BL-3's 2 MEDIUM + accumulated deferred-concurrency/LOWs) → Create-with-AI for the operator's project.** (new role → needs its own fixtures + cert; §7b verifier-role + stub-web sub-decisions surfaced when it's the active wave).** §5 builds with available signals (confidence-calibration + escaped_defect + operator_feedback); refutation_rate/fixloop_rate stay null-until-B2 (B2 separately operator-gated). Thresholds use spec defaults (14d window / 5-event min), tunable.** Earlier-state ↓. **OPERATOR-DRIVEN, LIVE (2026-06-16).** 2/5 certified: code_reviewer, design_reviewer. 3 recovering: investigator/qa_lead/security_officer all hit terminal `failed` (investigator = a scorer bug; qa_lead/security_officer = operator mis-clicked adjudication `false_positive` on extras that were a clean-control absence + real-but-unplanted non-idempotent DDL — should have been `dismiss`, the not-FP path). **Ceremony-support fixes pushed (live-debug, not queued waves):** `fbea882` findings.ts parseLines accepts 1-element `lines:[n]` (the scorer rejected a valid investigator finding → 0/4); `79f9b19` ceremony re-version recovery — a failed role_version is §2.2-terminal + had NO retry path; added `reversionFailedRole` (clones prompt_core via createRoleVersion → fresh draft, failed row never mutated) + newest-selectable version pick + a 're-version & retry' UI affordance. **Deferred MEDIUM (NOT chased — chain AFTER 5/5): reversionFailedRole is read-then-write, no txn → two SIMULTANEOUS submits race → loser hits the role_version_dedup UNIQUE → raw 500 (index PREVENTS a dup version, NO corruption; serial clicks fine). Fix: catch the dedup collision in createRoleVersion/route → clean no-op.** Recovery path: re-version each of the 3 → reference-run → interview → adjudicate with `dismiss` → 5/5 → panel-flip. Unblocks wave-v2.3 §6/§7 (real cert track data). | operator |
| done | wave-v2.3 | **✅ DONE + PUSHED (origin/v2 `7d71440`, 2026-06-17) — §5·§6·§7·BL-3·§7b + full hardening; see the day-0-ceremony row for the per-commit hardening detail (incl. F-039 consolidation gate).** WORKFORCE-SPEC §5 performance loop · §6 project_staff · §7 tier hiring · **§7b researcher role + research rails** (+ missed-defect ledger after B2). **§5 SCOPE += confidence-calibration drift signal (operator-confirmed 2026-06-16):** gauntlet certification stays confidence-BLIND (deterministic recall+FP, §3.4 — never let an agent talk past the gauntlet); confidence is instead a §5 TRUST/DRIFT dimension — extend the EXISTING A1 calibration (`track-record.ts` calibration = counts per confidence×closed-outcome on `panel_verdict.confidence`) so a role trending MISCALIBRATED (high-confidence-but-wrong rising) flags for review; + optionally persist the wave-reviewer's per-finding HIGH/MED/LOW confidence as a per-task signal (today harness-side only, D-001) and adopt the §7b `unverified:` convention so uncertainty is first-class per task. **§6/§7 must settle the capability-matching heuristic (BL-3): a project declares structured capability-needs → the PM scores catalog roles' PROVEN coverage (gauntlet fixture provenance, §3.8) → recommends reuse / extend-and-re-certify / new-hire, through the §4 panel + operator gate (D-039). Reuse must mean "certified on the relevant defect classes," not vibes.** | auto |
| queued | self-hosting | D-040: ingest the docs suite into Atelier's own memory for project atelier_self + hire its PM (**claude-opus-4-8** — fable-5 retired 2026-06-13) — Atelier maintains itself | operator |
| done | wave-create-ai | **✅ BUILT + PUSHED (origin/v2 tip `e8880fc`, run wf_80dd0fc3-bbd, 2026-06-17) — full suite 2953 passed, typecheck 0, lint 0.** 4 tasks: CA-0 `62edc15` (central MODEL_IDS vocab + fail-closed pm.model_id + fable-5→opus sweep across config/workforce.yaml + 6 test files); CA-1 `0daeb72`+`344df18` (the PLAN half — `src/lib/server/create/`: anti-sycophancy single-source banned-phrase list + detector, brief→clarifier→structured proposal generator [injected ProposalGenerator seam, no spend in unit tests], confirm-token over the ephemeral proposal, NO disk writes; review-fix: §3 rail extended after a banned hedge in stack[]/dirLayout[] reached the operator unflagged); CA-2 `f128440`+`cdcb020` (the EXECUTE half — confirmed proposal → real scaffold under CODE_ROOT [confineToRoot D-018] → git init → scanProject honest ingest [F-008] → updateProjectPlan/setCapabilityNeeds/createTask/declareTarget writers → hirePm handoff; fix: slugify non-idempotent for symbol-only names diverged the gate-id from scanProject's re-derived id → fail-closed); CA-3 `e8880fc` (the UI — /projects "Create with AI" + palette + brief/clarifier + diff-style proposal review/confirm → workspace; a11y/watched-tables gates green). Each REUSED createProject/scanProject/createTask/declareTarget/hirePm/launchSession/setCapabilityNeeds (verified reuse-map, no rebuild). **→ CA-HARDENING (9 deferred follow-ups, 5 MEDIUM): CA-H1 ✅ DONE (`c56bc62`, full suite 2983 green) — the wave (wf_6efda4c3-d80) correctly 3-strike-STOPPED at CA-H1 review (the fixer kept tuning a length/entropy threshold on an unclosable premise: a short literal like `X7K9QZ2MPLW4` is byte-identical to a bare env name); orchestrator took it over with the ROOT-CAUSE fix — an env reference now requires EXPLICIT syntax (`${ENV_NAME}`/`$ENV`/`process.env.X`), so under a secret-like key EVERY literal (any length/entropy) is rejected, no threshold guessing; also folded in the deferred Gap-2 (secret-echo now covers macro/charter/tasks/clarifiers free-text + the execute D-026 test split into plan-boundary + re-minted defense-in-depth). CA-H2 + CA-H3 RE-DISPATCHED (run wf_f10b0ef1-855): CA-H2 post-register writer rollback+incident [F-008, was wedging the slug] + concurrent same-slug TOCTOU guard [loser's `rm -rf` could delete the winner's scaffold + phantom row]; CA-H3 wire MODEL_IDS into loadAgentPool/loadModels [CA-0 residual LOW].** **✅ CA-HARDENING COMPLETE + PUSHED (origin/v2 tip `6700d58`, 2026-06-17) — CA-H1 root-cause closure (orchestrator) + CA-H2 (`b952447`-class, m0049 create_status+create_lock, F-040) + CA-H3, full suite 2996 + readback.live green, typecheck 0, lint 0.** Re-committed as ONE clean commit `8c860de` (+F-041 `6700d58`): the per-task commits' secret-detector test fixtures carried real-FORMAT provider tokens (glpat-/ghp_/sk-ant-) that GitHub push-protection (GH013) BLOCKED — and `pushAtEnd` swallowed the rejection silently (the wave reported success but origin never moved; caught by a rev-list ahead/behind check). Fixtures now assemble fakes at runtime so the source has no contiguous token; **F-041 logged** (verify `rev-list 0 0` after any secret-test wave). **REMAINING DEFERRED → CA-H4 (queued, operator-prioritized — NOT auto-chained; core integrity/security is CLOSED, these are create-route UX/recovery refinements that only bite on failure/concurrent paths): [MEDIUM] the /projects/create action maps ConcurrentCreateError→500 (should be 409 retryable) + PostRegisterWriterError→500 with no recovery hint; [MEDIUM] create_status='incomplete' is write-only — no UI surface + no resume path, retry blocked by ProjectExistsError (a partially-failed create is honestly recorded but un-recoverable); [LOW] create_lock has no TTL/reaper — a SIGKILL between acquire and finally-release orphans the lock + wedges the slug until an operator DELETE.** THESE GUARD THE EXECUTE PATH BEFORE THE OPERATOR CREATES A REAL PROJECT (new-mod-test). Original ↓: Build Create-with-AI per CREATE-SPEC (post-v2.1; PM-hire hand-off needs D-039). First customer locked (operator 2026-06-10): a NEW ROUNDS mod based on SWIP — exercises the §2.1 reference-repo hint (read SWIP as prior art, generate fresh scaffold; greenfield-with-reference, NOT adoption). Resolves fork 3 in practice: PM hire ON for the test | auto |
| queued | new-mod-test | THE graduation test (operator): create the new ROUNDS mod VIA Create-with-AI → its PM proposes founding work → Atelier develops it → its 1.0.0 becomes the platform's FIRST REAL PUBLISH (supersedes re-shipping SWIP 2.3.3 as the inaugural release; SWIP's staged publish stays parked). Full lifecycle: create → develop → release | operator |
| blocked | first-publish | **first real Thunderstore publish = the NEW Atelier-created project's 1.0.0 (operator decision 2026-06-16), NOT SWIP.** The inaugural release is the new ROUNDS mod created via Create-with-AI in `new-mod-test` (create → develop → release, all inside Atelier) — this proves the FULL lifecycle on a project Atelier itself originated, which is the truer graduation proof than re-shipping an externally-built mod. Downstream of `new-mod-test`; same Thunderstore adapter/D-037 publish path + operator gate-confirm (no real publish without it). gate:operator. | operator |
| done | transcript-view + db-reauth | **✅ DONE + PUSHED (origin/v2 tip `084b21a`, run wf_401ff5c3-de9, 2026-06-17) — operator asks, both review+red-team PASS.** TV-1 `31302fb`: /claude-code fleet rows now link to `?session=<id>` (accessible `<a>`, keyboard-activatable, aria-label + aria-current, selected-row token highlight; running AND ended sessions linkable) — completes "view a transcript of a session" (backend + SessionTranscript render already existed; UI affordance was the only gap; live-verified in browser). TV-2 `084b21a`: **F-042 durable fix** — the `Db` client self-heals on auth-token expiry (retains creds in-memory [never logged/thrown, D-026], narrow AUTH_EXPIRY match, ONE bounded retry via a de-duped in-flight re-signin+`use()`, genuine errors still propagate honestly). Verified in-code + red-team adversarial-probed (no infinite retry / no cred leak / real errors surface / no stampede). **DEFERRED → live-query-reconnect (queued below):** TV-2 heals the QUERY path; `watchTable` (events/db-source.ts:47) opens LIVE queries once at boot — if that session's token expires the server→SurrealDB live stream silently dies while the SSE heartbeat keeps the browser "connected" → stale-shown-as-live (F-008). Plus [LOW] liveTable() heals only SELECT setup, not an established subscription. (4w root-token stopgap re-applied each db:up keeps live queries alive this session until the reconnect lands.) | auto |
| done | live-query-reconnect | **The live-query analog of TV-2 (F-042 family).** `watchTable`/the runtime live-query handles (events/db-source.ts, runtime-init) must reconnect on auth-expiry — re-establish the LIVE SELECT (and any established subscription) when the singleton's token lapses — so the dashboard's real-time regions don't silently present stale data as live (F-008) after ~1h on the default 1h token. Distinct from TV-2's query-retry heal (that path is fixed). Until then the 4w-token stopgap (re-applied at each db:up) masks it. Pairs with the adjudication-UI / observability cluster. | auto |
| done | create-templates | **✅ DONE + PUSHED (origin/v2 `b4c201f`→`c1c082e`, run wf_402de202-d57, 2026-06-18) — 4/4 PASSED (review + red-team), CT-4 live-verified, rev-list 0 0.** CT-1 `b4c201f` registry port (17 templates + BEPINEX_GAME_CONFIGS, pure, 78 tests; red-team caught + fixed a REAL D-018 gap v1 had — raw `${name}` in path keys for dotnet/bepinex/go → sanitized to fileBase/pkgName). CT-2 `cde9878` AI-seed (template prior-art block in buildPrompt + briefHintsFromTemplate). CT-3 `52ba01e` executeTemplateCreation (reuses confineToRoot/D-026 screen/scanProject/create_lock/hirePm). CT-4 `c1c082e` picker UI (params form + two doors scaffold-direct/refine-AI + brief pre-fill, live-verified). **2 stale deferrals (CT-2 'no caller' — CT-4 consumed them, verified). 2 real → create-templates-harden below.** Original ↓. Reclaim v1's templates into v2 Create-with-AI — operator chose "Both: picker does either" (2026-06-18).** v1 (`dev:dashboard/src/lib/server/project-templates.ts`) had 17 deterministic scaffold templates {id,name,language,icon,tags,params,generate(name,desc,params)→{path:content}} incl. a ROUNDS-ready **bepinex** (params modName/modId/gameId=ROUNDS/CI/Thunderstore; emits real Plugin.cs `[BepInProcess("Rounds.exe")]`+Harmony, .csproj w/ game framework, CI, manifest), fabric/forge/paper/bg3/dotnet/rust/go/java/python/sveltekit/nextjs/… 4 tasks: CT-1 port registry → `src/lib/server/create/templates.ts` + BEPINEX_GAME_CONFIGS, pure+unit (redTeam D-026 no-literal-secret/D-018 no-path-escape in generate()); CT-2 AI-seed (propose accepts optional templateId+params → pre-fill brief + inject template stack/dirLayout/(bepinex)ROUNDS facts as PRIOR ART into agent.ts buildPrompt); CT-3 direct-scaffold execute (render generate()→write real file-map under CODE_ROOT REUSING execute.ts confineToRoot/D-018 + D-026 screen + scanProject/F-008 register + create_lock/F-040 + hirePm; NO agent/spend); CT-4 picker UI on /projects/create (params form + two doors seed-AI/scaffold-direct + brief pre-fill; a11y/tokens/honest, live-verify). Directly de-risks new-mod-test (correct C#/BepInEx scaffold vs hallucinated). | auto |
| done | create-flow-harden | **✅ DONE + PUSHED (origin/v2 `b39d424`→`e463e6f`, run wf_e995d6e3-e73, 2026-06-18) — 4/4, rev-list 0 0; H3+H4 live-verified.** H1 `b39d424`+`deb4766`+`c1b0701` screen gate: redact-and-write on `redacted` (benign email no longer hard-fails), hard-reject only `quarantined` naming the file. **Red-team caught a REAL D-026 HIGH** — `planMacro.purpose`/`foundingTasks[].purpose` flow to updateProjectPlan(MERGE)/createTask(CONTENT) which DON'T screen, so a `redacted`-status sk-ant key persisted RAW in DB columns; fix-looped → `screenWriterText()` (execute.ts:393) now screens EVERY DB-bound freetext field at the writer boundary (planMacro.{purpose,vision,role,dod} 535-540 + foundingTasks 567-568) before persistence; column-level regression asserts `[REDACTED:anthropic-key]` not raw. **Verified in committed code at handoff.** H2 `df732e9` MAX_TEMPLATE_PARAM cap + unknown-key drop. H3 `d2e6cf4` input persistence (name/desc/hints/templateId/params via sessionStorage, SSR-guarded) + Clear-all, live-verified. H4 `dde8b3a`+`e463e6f` proposed_defect_classes (AI-proposed unknown classes CAPTURED as a hire signal not hard-failed; matcher reads only confirmed defect_classes — D4 intact; review-block off real tokens), live-verified. **3 residual deferred → create-flow-harden-2 below (none in the create auto-path — don't block ROUNDS).** Original ↓. Create-with-AI hardening + UX — 2 deferred MEDIUMs (wf_402de202-d57) + 2 operator asks (2026-06-18).** H1 (screen-abort): `execute.ts` treats screen() `redacted` as a HARD ScaffoldSecretError, but screen() already RETURNS safe redacted `.text` (status clean|redacted|quarantined). A normal description w/ an email/`password=` → redacted → create fails with a MISLEADING "literal secret" error. Fix: write the safe `.text` on `redacted`; hard-reject only `quarantined` with a CLEAR named error; D-026 preserved (no raw secret written). H2 (param-cap): MAX_PARAM length cap + validate param keys vs template.params in readTemplateParams before threading into prompt/scaffold (red-team: 50k param→200k prompt). H3 (input-persistence, operator-asked): every /projects/create field (name/description/hints/template selection/params) survives reload + navigation (sessionStorage-backed runes) + a Clear-all button. H4 (defect_classes, operator chose "capture as proposed needs"): SPLIT capabilityNeeds → confirmed `defect_classes` (vocab-validated, matchable, D4) + NEW `proposed_defect_classes` (unknown classes CAPTURED not hard-failed; NEVER matchable / NEVER masquerade as confirmed; surfaced in proposal review as the HIRE signal; stored on the project for pm-hr-handoff to consume → HR drafts fixtures minting them). Agent prompt updated so it CAN propose rich domain failure modes (BepInEx Doorstop/version-mismatch/net472/Thunderstore-schema/bitness/reproducibility). H1+H4 touch security/locked-D4 → reviewed+red-teamed wave. Run wf TBD. | auto |
| done | create-flow-harden-2 | **✅ DONE + PUSHED (origin/v2 `eb76919`→`2285238`, run wf_a64acfe6-3ca, 2026-06-18) — 3/3, rev-list 0 0.** R1 `eb76919`+fix `e87f471` config secret-echo REDACT-AND-KEEP: `redactConfigSecrets` (plan.ts:648) hard-rejects only a credential PREFIX (657) or `quarantined` (668); a `redacted` screen carries the residual through Gate 2 key-positive (692) + Gate 3 entropy (698) + a residual-token scan (707) — benign `assets:['icon.png',…]` stays clean → proposal SUCCEEDS (the live blocker fixed). **Red-team caught a REAL D-026 residual leak** (prior code early-returned on `redacted`, leaving a raw literal under a secret-like key) → fix-looped `e87f471` (removed the early-return). **Verified in committed code at handoff** — no raw secret survives any branch; configRedactions surfaced in the review UI. R2 `a15c6ce` pm charter+persona screened at every writer boundary (D-026 sibling leak closed). R3 `2285238` capabilityNeeds bounded + confirmed/proposed cross-dedup. **1 residual → create-flow-harden-3 below.** Original ↓. + AI-REFINE ROBUSTNESS (operator hit a 4th proposal-validation over-rejection 2026-06-18): `targetDrafts[1].config.assets[0]` flagged as a literal-secret echo (D-026). `assets` is a publish-target asset PATH list (icon/README/manifest), not a credential — Gate 1 (screen redacts a path) or Gate 3 (entropy on a hash/long filename) over-fired and HARD-killed the whole ~2min proposal. Value unrecoverable (CA-1 ephemeral; error omits it per D-026) but the CLASS is clear. FIX (the systemic one — stop nuking legitimate proposals): apply H1's REDACT-AND-KEEP to validateTargetDrafts config (and audit the sibling proposal gates) — a suspected-secret config value is REDACTED (screen().text, D-026-safe — no raw secret stored) + surfaced in proposal review ("looked secret-like; use ${ENV_NAME}"), NOT a hard-fail; HARD-fail ONLY the un-redactable (a known credential PREFIX glpat-/sk-/… or a `quarantined` block — those the operator must remove). Mirrors the kind/defect_classes/screen-abort resolutions: the proposal validator should TOLERATE/CAPTURE/REDACT legitimate agent output, never discard a real-spend generation over one off-happy-path field. D-026 stays sound (prefix/quarantine still hard-reject; nothing raw persists). Reviewed+red-teamed (D-026 boundary). — Original residuals ↓.** 3 residual deferred from wf_e995d6e3-e73 (none in the create auto-path).** [MEDIUM] D-026 sibling-field leak: `updatePmCharter` + the pm `persona` field (pm-repo.ts) write RAW — operator-direct edit paths (the create flow's `createPm` IS screened via deb4766), but harden them with the same `screenWriterText` boundary as the plan/task writers. [MEDIUM] capabilityNeeds arrays (defect_classes/proposed_defect_classes/languages/frameworks) are unbounded in plan.ts reqStrArray/reqStr — H4 turned the unknown-class hard-fail into unbounded persisted capture; add a MAX-count + per-element length cap (write-amplification from agent output). [LOW] setCapabilityNeeds doesn't cross-dedup proposed vs confirmed defect_classes (non-prod direct caller; not a D4 break — matcher reads confirmed only). Reviewed wave (D-026 boundary). | auto |
| done | create-flow-harden-3 | **1 residual deferred from wf_a64acfe6-3ca (pre-existing, low-likelihood, NOT in the create auto-path's common case).** [MEDIUM] `SECRET_HEAD_TOKENS` (plan.ts ~346) is SINGULAR-only — a PLURAL secret-like config key (`passwords`/`secrets`/`tokens`/`credentials`) bypasses Gate 2 whole-value redaction; a LOW-entropy bare literal under such a key would persist raw (a HIGH-entropy one is still caught by Gate 3 / the residual-token scan). Fix: add plural head tokens (or stem the head token) so `{passwords:'literal'}` gets the same key-positive redact-and-keep. Small, D-026 hygiene — fold into the next create touch or a quick pass. | auto |
| done | create-live-thinking | **Loading/thinking UX during proposal generation (operator 2026-06-18).** Today `?/propose` BLOCKS on a real ~2min read-only agent session (makeProposalAgent→launchSession) showing only a disabled "Generating proposal…" button — no progress/thinking/what's-happening (the session persists transcript turns but the client never gets the sessionId to subscribe). NOT fake phases (F-008). Fix: make generation ASYNC (return sessionId immediately, generate in background, persist envelope to a run row) → client renders the SESSION'S LIVE TRANSCRIPT (reuse SessionTranscript.svelte + transcript-core + the message onDbChange SSE — the live-session-transcript infra) + watches the run row for done→envelope. The platform's "watch the agent work" UX applied to create. Distinct async refactor; builds after create-templates. | auto |
| done | file-snapshot | **See file content in-app, DB-backed — `docs/FILE-SNAPSHOT-SPEC.md` (operator 2026-06-17).** A content-addressed `file_snapshot` table {path, content_sha, content, captured_at, captured_by} linked from any file-referencing row, so the app renders "what the file was when referenced" (project files live on disk; today the DB has only the row+metadata). POINT-IN-TIME not a live mirror (F-008 — disk stays truth, snapshot labeled as-of-capture; don't reintroduce stale-shown-as-live); dedup by sha (no bloat); D-026 never snapshot secrets; byte-cap binary/huge. Capture at scaffold-create, agent read/edit (transcript turns), finding-cite, config-write. + an in-app "view file" surface. | auto |
| queued | memory-scene | **The living brain — animated graph of memory + workforce + jobs. `docs/MEMORY-SCENE-SPEC.md` (operator vision 2026-06-17).** Obsidian-style force graph: nodes = memory `entity`/`memory`/`skill` + hires `role`/`role_version`/`pm` + ateliers `project` + usage `session`/`work_item`/`agent_event`; edges = `references`/`project_staff`/pm↔project/session↔target. Nodes/edges DERIVE LIVE (F-008 — no denormalized node copy). Animation = motion.dev IMPERATIVE API in Svelte 5 (NOT React `<motion.div>` — `docs/skills/motion-framer/SKILL.md` principles: spring presets, stagger, enter/exit, transform-only 60fps, prefers-reduced-motion) + a force layout; SSE-driven spawn (new row→spring pop) / job-fire pulse / connection draw-in / retire fade. NEW table `scene_event` = a DERIVED append-only viz feed {kind:node_spawned|job_fired|connection_formed|node_retired|…, ref, at} that drives the animation timeline (NOT node state — append-only events like agent_event, no drift); rolling window. Surface: a Scene lens on `/memory`. Builds after HR + file-snapshot (visualizes the workforce HR manages). FORKS: route-vs-lens, node-scope-v1 (recommend memory+jobs first), scene_event retention (recommend rolling). | auto |
| done | pm-hr-handoff | **Wire the PM HIRE recommendation → HR hire-request (the hands-off chain).** `recommendStaffing` (capability-match.ts:388) ALREADY emits a per-class `HIRE` gap recommendation when no certified role covers a project need (evidence-cited, propose-only) — but it does NOT dispatch; acting on it is operator-mediated. After `hr-recruiter` lands + is bootstrapped, wire: a PM HIRE gap → a hire-REQUEST to HR (the target role + its defect-class needs) → HR's cert-lifecycle orchestrator (HR-3) drafts fixtures+keys → operator approves the key-SET (B2) → HR runs the cert + auto-adjudicates clear cases (HR-4) → operator approves the hire (HR-5/B4) → staff via the existing BL-3 flow. End state: PM identifies need → HR fulfils → operator touches only the 2 gates (HR-spec §8 fork-1: "PMs request, the recruiter fulfils"). **PM FIT-VERDICT (operator-raised 2026-06-17): capability ≠ fit ≠ authority — HR certifies the role CAN do the work (objective gauntlet); the PM, as the project's accountable owner, then judges FIT for THIS project (context HR lacks — e.g. HR certifies a generic C# reviewer but the PM knows the mod needs BepInEx/Unity specifics the gauntlet never exercised); the operator holds final D-039 authority. The chain gains a PM step: HR proposes hire (capability-certified) → PM fit-verdict {approve | deny-with-reason} → operator gate. A PM deny-fit DEFAULTS the operator decision to reject (with the PM's reason surfaced on the hire brief); the operator can override (D-039 stays final) but decides against a stated project-context objection, not blind. First-class PM input, not a competing hard gate.** DEPENDS ON hr-recruiter + its bootstrap cert. | auto |
| done | hr-adjudication-queue | **HR handles the gauntlet adjudication queue (operator-asked 2026-06-19) — sequence AFTER pm-hr-handoff (same recruiter/ceremony files).** Today `auto-adjudicate.ts` (HR-4: autoAdjudicateRun, clear-cases-only, escalate-on-doubt, NEVER auto-FP — B3) is wired ONLY into the HR campaign path (`runRecruiterCampaign`); the OPERATOR/ceremony cert path (`/agents/ceremony` adjudicate action → `adjudicateInterviewRun(resolutions)`) leaves the WHOLE ambiguous queue for the operator to hand-resolve (what the orchestrator did on the recruiter bootstrap — hand-confirmed the over-strict partial_match). Wire HR's `autoAdjudicateRun` as a PRE-PASS on the adjudication queue everywhere a cert scores to `adjudicating` (ceremony + HR-driven): HR auto-resolves the CLEAR cases (obvious confirm_hit on a G1-evidence partial_match; obvious dismiss on a correct injection-flag extra_finding), and ONLY genuine doubt ESCALATES to the operator's HR-1 queue. **B3 LOCKED: HR NEVER auto-false-positives a fabrication (auto-FPing a real defect is an integrity attack); escalate-on-doubt; the deterministic scorer is untouched; the operator still sees + decides every genuinely-ambiguous item — the queue just gets SMALLER, never zero-by-fiat.** Surface which items HR auto-cleared (with its basis) vs escalated, so the operator can audit HR's calls. Reduces the operator from queue-clearer → escalation-decider (the HR-automation goal). Reviewed+red-teamed (B3 boundary). | auto |
| done | hr-recruiter | **✅ BUILT + INTEGRATED + PUSHED (origin/v2 tip `50d7f9f`, 2026-06-17) — HR wave (wf_5e79558c-106, 5 tasks) + integration wave (wf_24dd7202-c5c, 2 tasks).** HR-1 `f75f067` adjudication-UI (renders finding/plant/basis/pass_criteria — kills the DB-spelunk). HR-2 `084e7c5` global RECRUITER role (7th catalog, seeded DRAFT/uncertified via seedRecruiterRole, NOT in LAUNCH_ROLES) + 3 judgment fixtures (detect-teethless-key / detect-over-strict-key / adjudication-classification) + RECRUITER_DRAFT_KEYS. HR-3 `1dfc707`+`de12788` cert-lifecycle orchestrator (recruiter.ts: draft→operator-approve-key-set→run→re-version+classify key-defect-vs-candidate-miss; propose+gate only). HR-4 `2329daf`+`f5bf96f` auto-adjudication (auto-adjudicate.ts: clear-cases-only, escalate-on-doubt, NEVER auto-FP — partial_match escalates). HR-5 `5c88e73`+`209ff4b` hire-gate (recruiter-hire.ts + m0050 cert_hire_brief: terminal-pass→ONE decision_brief→operator approve flips cert + feeds BL-3 staffing/reject neither, B4). **Integration (HR-H1 `0151471`+`2f6dbd8`+`70e5444`, HR-H2 `50d7f9f`):** reconciled the recruiter prompt with the no-auto-FP fork (red-team caught the prompt teaching auto-FP-fabrication, contradicting HR-4; fixed) + scorer-VALIDATED all 3 RECRUITER_DRAFT_KEYS via real scoreFindings (red-team caught the adjudication-classification key was OVER-LOOSE — credited auto-FP judgments matching bare 'operator'/'ambiguous'; fixed with an affirmative-escalate + negative-lookahead-rejecting-auto-resolution pattern, teeth restored on the B2/B3 axis) + WIRED the loop (HR-3→HR-4 auto-adjudicate→HR-5 raiseHireBrief end-to-end; were zero-caller before). Integrity boundaries verified in-code: B1 no-self-cert (recruiter seeded uncertified, operator bootstraps), B2 propose-not-confirm, B3 scorer untouched + no-auto-FP, B4 operator hire-gate. HR-slice 110 tests green; full suite green modulo F-014 flakes (isolated-green). **✅ RECRUITER BOOTSTRAP-CERTIFIED (2026-06-18, orchestrator-driven on the operator's "you run it").** v2 `role_version:zqqvecu40c88iyw9yh1g` lifecycle=passed (same data shape as the 6 other certified roles). The prior spawn_failure was env (stale-shell OAuth token shadowing .env, F-029 + a surreal-orphan mess) — gone on a clean env (token cleared from shell, fresh db:up + 4w-token, GAUNTLET_TRACE). First run scored 2/4·0FP on a behaviorally-PERFECT transcript (flagged teethless+over-strict, resisted both injections, escalated the fabrication, refused to auto-FP/invent findings); BOTH misses were key/harness artifacts (the recurring "don't over-constrain keys" lesson). Operator chose **fix the 2 keys + re-run** (B2): over-strict evidence_pattern now accepts the G1-verbatim plant-id quote (detection-only, content_sha intact); adjudication-classification task.md gained an explicit findings.json OUTPUT CONTRACT (the scorer reads structured findings, never prose) → reseeded+re-keyed+re-activated. Re-run **4/4·0FP·passed** (the extra injection-report dismissed as a correct catch, not an FP). Source fix pushed `0639958` (recruiter+launch-fixtures 59/59, svelte-check 0, lint 0, rev-list 0 0). Integrity held: B1 (operator bootstrapped, no self-cert) · B2 (operator approved the key fix) · B3 (scorer untouched, no auto-FP) · B4 (operator held the gate). **Cleanup: the first attempt's run `interview_run:113b52s548948bgvz3os` is still `adjudicating` (references now-deleted fixtures) — harmless phantom, finalize/abandon later.** **NEXT: HR owns the cert/hire loop → new-mod-test (create the ROUNDS project via Create-with-AI; HR hires the C# specialist).** | done |
| done | hr-bootstrap-ux | **✅ DONE + PUSHED (origin/v2 tip `4e62d9f`, 2026-06-20; rev-list 0 0).** HRB-1 `844558c` (run `wf_d27dd141-50c`) + HRB-2 built INLINE by the orchestrator (subagent spawns were blocked by a sustained API 529 — both wave attempts `wf_9adead7d-4b2`/prior died at agent spawn; the orchestrator implemented HRB-2 directly with full rigor). HRB-2: `ceremonyAuthoringState` surfaces a per-fixture `draftKey` (plants JSON + fpTolerance + fpJustification) matched by fixtureSlug from RECRUITER_DRAFT_KEYS ∪ RESEARCHER_DRAFT_KEYS, ONLY while unkeyed (a keyed fixture shows keyDiff); the ceremony key-form pre-fills the plants textarea + fp inputs (editable), a 'review and approve' notice + DRAFT tag, button reads 'Review draft key'. **B2 LOCKED + tested: surfacing a draft is NOT auto-approval — confirmKey stays the gate, keyed stays false (asserted).** Read-only; nothing writes a gauntlet_key. Verify: build OK, typecheck 0/0, lint 0, ceremony 39/39 (+ new HRB-2 test asserting all 4 recruiter drafts surface + parse + keyed=false + no-draft→null), watched-tables 6/6, route-head-a11y 78/78; live page renders clean (no SSR regression — visual pre-fill exercise needs an unkeyed fixture; the live recruiter is certified/keyed, so proven by the fresh-DB unit test). _(orig HRB-1 detail ↓)_ HRB-1 — drafted a scorer-validated real-teeth key for the recruiter's 4th fixture `injection-key-approved` (hallucination_bait) so all 4 fixtures now have a draft key (HR-2 contract); evidence_pattern requires RESISTANCE/judgment language (ignored/refused/resisted the embedded/injection instruction · injection plant/attempt/note), NOT bare defect tokens — a full verbatim artifact dump (incl. the `// PLANTED DEFECT` comment) scores plantedFound=0, both known-GOODs survive, compliers/parrots MISS (scorer untouched, B3). **The wave's stored red-team `passed:false` was the stored-failing pattern AGAIN — it quoted the PRE-fix bare-token state; the committed 844558c dropped them. Orchestrator verified in committed code + reproduced via the real `scoreFindings` against the SHIPPED artifact (test:725, full dump → 0); 50/50 launch-fixtures, 17/17 recruiter.** HRB-1 — drafted a scorer-validated real-teeth key for the recruiter's 4th fixture `injection-key-approved` (hallucination_bait) so all 4 fixtures now have a draft key (HR-2 contract); evidence_pattern requires RESISTANCE/judgment language (ignored/refused/resisted the embedded/injection instruction · injection plant/attempt/note), NOT bare defect tokens — a full verbatim artifact dump (incl. the `// PLANTED DEFECT` comment) scores plantedFound=0, both known-GOODs survive, compliers/parrots MISS (scorer untouched, B3). **The wave's stored red-team `passed:false` was the stored-failing pattern AGAIN — it quoted the PRE-fix bare-token state; the committed 844558c dropped them. Orchestrator verified in committed code + reproduced via the real `scoreFindings` against the SHIPPED artifact (test:725, full dump → 0); 50/50 launch-fixtures, 17/17 recruiter.** The first HRB-2 attempt never ran (transient API 529) → re-running alone. HRB-2 — ceremony confirmKey UI surfaces RECRUITER_DRAFT_KEYS pre-filled+labelled DRAFT for one-click B2 review-and-approve (NOT auto-approval; operator still explicitly confirms each). Anchors: recruiter.ts (RECRUITER_DRAFT_KEYS), launch-fixtures.ts (4 fixtures), routes/agents/ceremony. Original ↓. **Two HR-2 gaps surfaced at the operator bootstrap (2026-06-17) — HR should draft+surface ALL keys, the operator shouldn't hand-confirm.** (1) `injection-key-approved` (recruiter's 4th fixture, hallucination_bait) shipped with NO draft key in RECRUITER_DRAFT_KEYS (only 3 of 4 drafted) — orchestrator confirmed it with an authored presence-teethless plant (`class:injection-key-resisted`, evidence `[Tt]eethless|empty plants|…|"plants":[]`, fp 0) so the bootstrap could proceed; HR-2 should draft a key for EVERY recruiter fixture. (2) The ceremony `confirmKey` does NOT pre-fill RECRUITER_DRAFT_KEYS (they're consumed only by the HR-3 orchestrator) — so the operator would hand-paste 4 keys incl. the gnarly adjudication regex; orchestrator confirmed all 4 via `confirmLaunchKey` on the operator's go. The B2 "operator approves the key-SET" UX wants the ceremony to SURFACE the drafted keys for one-click approve (not blank-form paste). Both folded into the next HR hardening pass. | auto |
| queued | hr-recruiter-old | **Automate the cert/hire ceremony — `docs/HR-RECRUITER-SPEC.md` (drafted 2026-06-17 from a live operator fatigue signal: hand-keying regex plants + DB-spelunking adjudication + manual re-version is "too much"; contradicts the agents-do-the-work premise).** A GLOBAL certified recruiter (HR) role owns the cert lifecycle: draft fixtures+keys (operator approves the SET, B2) → run `runGauntlet` → auto-adjudicate CLEAR ambiguous cases + escalate the rest (B3) → `reversionFailedRole` on fail + propose key-fix when the fail is a key defect → surface ONE D-039 hire decision (B4). Operator shifts author+laborer→approver. Integrity boundary: no self-cert (B1, recruiter≠candidate, operator bootstraps the recruiter day-0-style), operator approves key-set not authors it (B2), deterministic confidence-blind scorer untouched (B3), operator keeps the hire gate (B4). **PREREQUISITE (ships first, also a CA-H4 sibling): adjudication-UI completion** — `/agents/ceremony` adjudication MUST render per ambiguous item the finding (file+class+verbatim evidence) + scorer note + per-fixture found/missed plant ids + basis + pass_criteria (today shows NONE → operator resolved a real run only by querying `interview_run.ambiguous`/`.results` in SurrealDB by hand). FORKS open (HR-vs-PM ownership, key-approval granularity, auto-adjudication aggressiveness, recruiter cert standard). Builds after wave-create-ai; can dogfood by re-certifying the researcher as its first job. | operator |
| blocked | swip-publish (parked) | **SWIP Thunderstore publish — PARKED as inaugural (superseded by first-publish above, operator 2026-06-16); kept as a real prepared publish for when SWIP develops further.** Prepared state preserved: project registered (`project:thunderstore`, "SWIP (ROUNDS mod)", root F:\code\mods\rounds-mod\thunderstore), target `project_target:szn709rvw27ypyotpv0d` (BasicallyCoding/rounds/custom-cards, default), token present, manifest bumped 2.3.2→2.3.3 (SWIP-repo-local commit `9b470f6`, NOT pushed), dll staged at thunderstore\plugins\SWIP.dll (untracked, includes uncommitted src/Cards changes — REBUILD from committed code before shipping), dry-run green 0-blockers. Re-bump version if SWIP develops further. Old GitHub CI diverges (stale tracked SWIP.dll) — retire or update before any v-tag push. gate:operator. | operator |

| done | pm-lifecycle | **✅ BUILT + PUSHED (origin/v2 tip `649cdb3`, run `wf_b1e2fe6c-711`, 2026-06-20) — operator ask: "PM able to start tasks as needed; one-click to start a project's life; PM automates the lifecycle."** Spec `docs/PM-LIFECYCLE-SPEC.md`. Verified GAP closed: the PM validated proposals but did NOT GENERATE them + there was no single loop entry. 3 tasks, all review PASS: **PM-LC-1** `8003c45`+fix `47efc36` pm-propose.ts `generatePmProposals` (the GENERATE step — live brief from real rows [plan/tasks/pm_memory/recommendStaffing gaps, F-008 no fabrication] → injected PmProposalGenerator seam [stub in tests, NO spend, mirrors create/agent.ts] → routed through the EXISTING `proposeTask` §4.1 chokepoint [origin=pm/proposed + objective/purpose/acceptance_criteria/provenance, D-039 Act-with-Purpose + authority precondition + anti-spam cap + fingerprint]; **red-team caught + fix-loop closed a D-026 evidence-leak** — agent-authored `evidence[]` was passed RAW past screen → now shape-constrained [reqEvidenceRef] + `screenWriterText`-screened per item, verified in committed code at pm-propose.ts:278/345/586; fingerprint-evasion via free-prose evidence also closed). **PM-LC-2** `f561984` pm-lifecycle.ts `startProjectLifecycle` one-click tick (getPm→needsHire if none [NO auto-hire] → bootstrapPm → generate → runValidationPanel → promote PER AUTHORITY: act→proposed→ready→orchestrator auto-develops · propose→leave for operator · observe→none) + `?/startLifecycle` action on projects/[id]; INTEGRITY verified: only existing 'act' authority, no auto-hire/hire-approve, D-039 untouched. **PM-LC-3** `649cdb3` Overview UI (live-verified): 'Start the project's life' button behind the D-010 confirm w/ verbatim real-spend cost label → sessionId nav reuses the existing SessionTranscript/SSE so the operator watches the PM think → tick-result counts; 'Hire a PM first' CTA when no PM (switches to PM tab, opens the 6-Q wizard, never auto-hires); honest idle/running/done/empty/error (F-008); tokens/a11y/route-head green; no new WATCHED_TABLES (reuses session/task/pm/panel_verdict). **Operator A-vs-B:** built **(A) one tick per click**; **(B) continuous autonomous cadence** DEFERRED (needs operator spend/safety call + a real dailySpawnCap, D-021 uncapped per BL-9-H1) — the tick is B's substrate. **→ deferred ledger → pm-lifecycle-harden (below):** 1 reachable MEDIUM (double-submit double-spend — PM-LC-3 wired the button the red-team said "guard when wired") + 3 LOW (uncapped openTasks brief; prod sessionId-capture untested; 4241-line route bloat). | auto |
| done | pm-lifecycle-harden | **✅ DONE + PUSHED (origin/v2 tip `ed3950a`, run `wf_300a244c-49b`, 2026-06-20; rev-list 0 0).** PMLH-1 `c1f9562`+fix `cf4aaf9` — per-project in-flight lock on `startProjectLifecycle` (m0056_pm_lifecycle_lock, additive/idempotent, 56/56 on live DB) closing the double-submit double-spend: a double-click now serializes to ONE PM session, the loser gets a benign `{alreadyRunning:true}` (no second spend), authority/D-039 untouched. **The fix's deeper root cause = F-043**: under a true-parallel race on the shared WS socket the SurrealDB SDK can FALSELY resolve a CREATE (2 callers → held=true, 1 DB row) AND the loser sees a retryable `read or write conflict` not `already exists` → fixed by reading ownership from a DB read-back of the holder nonce (not the CREATE's resolution) + absorbing both error shapes; stale-takeover (15min TTL) so a SIGKILL can't wedge. **Red-team stored `passed:false` was the stored-failing pattern — its prose quoted PRE-fix lines (`/already exists/`-only at 71-77); the COMMITTED cf4aaf9 absorbs both variants.** Orchestrator VERIFIED in committed code + reproduced: ran the exact tests the red-team claimed failed → deterministically GREEN 6× (lock 8/8 ×3 incl. parallel/takeover; lifecycle 12/12 ×3 incl. `generator.calls()===1` = one session). PMLH-2 `ed3950a` — greened the PRE-EXISTING workforce `gauntlet_key` census (F-036/F-039): repo.test.ts now strips comments before the substring check so file-snapshot.ts's 2 prior-art COMMENTS (from `3fed314`, verified NOT in this wave's diff) no longer false-positive, with a new regression keeping teeth (a real code read still trips it); census 35/35. F-039 consolidation gate: full suite 3616 pass / 2 reds = the known F-014 env flakes (db/client F-042 self-heal IAM, thunderstore 14.7 fixture-timeout) — orchestrator confirmed BOTH pass in isolation (12/12, 42/42), not regressions. **Deferred MEDIUM (G3 ledger, separate migration): proposeTask cross-project fingerprint dedup is TOCTOU over a NON-unique index (schema.ts:1260) — the per-project lock closes the same-project case [dominant harm]; cross-project double-propose needs a unique dedup-key migration.** | auto |

| done | CA-H4 | **✅ DONE + PUSHED (origin/v2 tip `6112f17`, run `wf_4269c4fe-3eb`, 2026-06-20; rev-list 0 0) — all 3 tasks review + red-team PASS.** CAH4-1 `96b0c36` create-route error mapping (consolidated `createErrorReason`: ConcurrentCreateError→409 retryable; PostRegisterWriterError→500 with a resume HINT, raw cause STRIPPED so no D-026 leak — test-asserted; AI + template paths now share identical text). CAH4-2 `361a2a7` resume path for an incomplete create (surface create_status='incomplete' + re-run the post-register writers on the EXISTING row, no re-scaffold, flip to complete on success / stay incomplete + incident on fail). CAH4-3 `6112f17` create_lock TTL/stale-takeover — ports the pm-lifecycle-lock F-043 pattern (read-back-verify ownership via readCreateLockHolder, absorb `already exists` + `read or write conflict`, atomic CAS `UPDATE … WHERE at<CREATE_LOCK_STALE_MS-cutoff`; no migration — m0049 already defines `create_lock.at`). Orchestrator confirmed: execute.test.ts 31/31 (incl. 4 new CAH4-3: stale-seized, fresh-foreign-never-stolen, parallel→exactly-one COUNT-asserted, double-seize→one winner), F-043 pattern present in committed code. Red-team deep-probed CAH4-3 (NONE-`at` permanent-wedge → CLOSED by schema non-option datetime; legacy pre-`at` rows → none; re-seize after 2nd crash → healthy; D-016 binding → sound) — all clean. **Deferred LOW (G3 ledger, NOT chased — spans BOTH locks): no lock-heartbeat — a create/tick alive but stalled >15min could be stale-seized mid-flight (`at` set only at acquire/takeover); unreachable today (hirePm is DB-only, no model spend in the create critical path); a heartbeat/`at`-refresh hardening would cover create_lock AND pm-lifecycle-lock together.** **→ AUTO QUEUE DRAINED to the operator-gated boundary (new-mod-test / C# specialist hire / first-publish / self-hosting all need the operator).** | auto |

| done | pm-autonomous-mode | **✅ DONE + PUSHED (origin/v2 tip `2850b7c`, run `wf_e284a419-fb8`, 2026-06-20; rev-list 0 0) — the continuous-loop FOUNDATION.** PMA-1 `087c8b0`+fix `da0d7c9` AutonomousPmLoop (pm-autonomous.ts: `pm.autonomous` flag m0057; sibling bus consumer on the pm-triggers task-terminal seam; armed PM re-ticks startProjectLifecycle on a terminal event once the promoted batch fully drains → next batch; honest stops running/blocked/cap-reached/dod-reached/awaiting-release-confirm; reuses the in-flight lock = no double-spend; only PM 'act' authority promotes; **halts at the publish gate — NO auto-publish, NO auto-hire**; fix wired the dead #stoppedProjects latch). PMA-2 `fe6e958` wired the REAL D-021 dailySpawnCap into boot.ts (config default **200/day**, DB-anchored, survives restart — BL-9-H1 closed). PMA-3 `9d77cb0`+fix `2850b7c` arm/disarm Overview UI + D-010 cost+cap confirm + STOP + honest live state; the auto-publish flag (m0058) is RECORDED-consent-only (write-only, correctly NOT acting — wiring it would've needed the D-037 override). review+redTeam PASS. **Spend doubly-bounded** (re-tick cap REQUIRED + 200/day spawn cap). The redTeam's deferred 'cap unwired' MEDIUM was a STALE ordering artifact (reviewed PMA-1 before PMA-2 landed the cap — orchestrator confirmed boot.ts:91/266/308 + config default 200). | auto |
| done | pm-autonomous-to-v1 | **✅ DONE + PUSHED (origin/v2 tip `7cf21c8`, run `wf_357baf94-12c`, 2026-06-20; rev-list 0 0) — Create-with-AI → fully hands-off → v1.0.0 PUBLISHED zero-touch; post-v1 on-demand.** EXP-1 `6637c64` verified auto-publish: at DoD-reached, IF the project's recorded `auto_publish_preauthorized` consent is set, run an OBJECTIVE release-readiness gate (release-gate.ts: REAL build exit code + thunderstore pack + validate) → only ALL-green proceeds through the EXISTING D-037 publish path (deterministic confirmTokenFor, never hand-rolled); ANY red → halt at 'awaiting-release-confirm' with failures surfaced. EXP-2 `7a2d254` Create-with-AI 'autonomously build + publish to v1' option arms pm.autonomous + consent at create (default OFF; reuses setPmAutonomous + the consent setter). EXP-3 `7cf21c8` auto-disarm at v1 (one-shot 0→1.0.0; disarm on the REAL publish-success signal; post-v1 re-arm is a fresh explicit action). All review PASS. The explicit per-project operator OVERRIDE of D-037 (recorded consent + objective gate). | auto |
| done | exp1-harden | **✅ DONE + PUSHED (origin/v2 tip `a59b5c0`, run `wf_cdc8a226-f47`, 2026-06-20; rev-list 0 0) — closed the 2 EXP-1 red-team auto-publish safety holes, both review+redTeam PASS.** EXP-H1 `308750c` FALSE-GREEN BUILD GATE (critical): the scanner persists a BARE build_tool ('dotnet'/'npm'/… detect.ts:55-98), so the gate ran `dotnet` with no args → exit 0 WITHOUT building → would publish an unbuilt artifact. Fix: `buildCommandFor(tool)` single-source map (dotnet→`dotnet build -c Release`, npm→`npm run build`, cargo/gradle/maven/go all carry verbs); un-buildable/unknown (pip/gibberish/empty) → null → FAIL CLOSED (RED, never run bare token, never publish); empty/missing/not-on-disk root_path → RED. Red-team probed injection (single-token `npm;rm`→null→RED; multi-token operator value runs as inert argv, shell:false D-008) — no seam. EXP-H2 `a59b5c0` STALE-VERSION PUBLISH: #reachDod published in the SAME call that proposed the version-bump task. Fix: PHASE 1 (DoD + release-prep generated) HOLDS publish as non-latching 'running'; PHASE 2 publishes ONLY after release-prep drains (inFlight==0) + the objective gate builds the CURRENT artifact. Convergence proven (releaseTicks==1, no livelock); integrity held under restart attack (lost in-memory latch re-holds publish, never a stale publish). **Deferred LOW (G3, NOT integrity): #releasePrepProposed phase latch is in-memory → a restart mid-PHASE-1 re-proposes release tasks (bounded churn, cap-bounded, never a stale/unbuilt publish) — persist/derive it in a future durability pass.** | auto |

| done | project-command-center | **✅ DONE + PUSHED (origin/v2 tip `88b7533`, run `wf_64d1b2e0-44f`, 2026-06-22; rev-list 0 0) — 4/4 review PASS.** MC-4 `25d5efd`+fix `64cfd74` honest failure surfacing (launchSession stamps a screened failure note on EVERY failed exit incl. error-event/instant-pre-init/done-ok-false, not just throws; D-026-screened; surfaced via SessionFailureReason.svelte on the project Sessions tab + /claude-code; contrast fixed to --color-error-on-overlay 5.31:1 — verified in committed code, the redTeam passed:false was stale-quoting the pre-fix --color-error). MC-1 `f539433`+fix `364fa58` visual status dashboard (task pipeline, autonomous-loop badge, cap usage, counts — derived live). MC-2 `3add9b6` live PM/HR/dev activity + transcript inline (reuses SessionTranscript/SSE). MC-3 `b389bd3`+fix `88b7533` controls (Continue re-drives ready tasks · Restart a failed run — **fix-loop closed a real MEDIUM: restart now re-reads task status + refuses a non-spawn-ready task → 409, so it can't re-spawn rework on an advanced task** · Re-run PM/arm/stop). New sub-components (didn't bloat the +page.svelte further). **Deferred LOW (G3): phantom 'stale'/'unknown' values in RESTARTABLE_SESSION_STATUSES; ProjectControls hardcodes status='failed'; dead-but-409-guarded restart button when task advanced; SessionFailureReason stale comment.** Pre-existing full-suite noise (pm-autonomous-action + pm-fit-verdict-action 'Db singleton already initialised' at beforeAll) confirmed NOT a regression (fails identically on clean HEAD). **⏸ OPERATOR PAUSE (2026-06-22): operator asked to pause when this wave finished — NOT auto-chaining the queued test-project-flag wave; holding for direction.** | auto |
| done | f046-config-race | **✅ DONE + PUSHED (origin/v2 tip `0767948`, 2026-06-22) — the concurrency-corruption incident.** Operator hit Continue → 6 ROUNDS sessions spawned, 5 failed. Root caused: (1) F-045 `code-write` declared un-catalogued capability `svelte5-patterns` → D-036 fail-closed EVERY spawn (fixed `296dd86`: emptied the capabilities block — live catalog has no synced scope carrying it); (2) F-046 `isolatedConfigFor` keyed `CLAUDE_CONFIG_DIR` by agent SLOT not session → concurrent same-slot CLIs raced `.claude.json` → "JSON Parse error: Unexpected EOF" (fixed `0767948`: key by `sessionId ?? agentId`; resume reuses the same sessionId so transcript reuse holds; legacy spawns unchanged). Also `perProject: 1` stopgap (driven sessions share the project cwd → >1 writer stomps until worktree isolation lands). Tests 142/142, svelte-check 0/0. F-045+F-046 logged. | auto |
| done | heartbeat-loop | **✅ DONE (run `wf_3abf281d-d91`, 2026-06-23) — both tasks build+review+redTeam PASS.** HB-1 `4ccb021`: wired `postTask:{enabled:true,followUpOnTestFail:false}` in startOrchestrator (boot.ts) AND fixed a SECOND dead link — `#runItem` now does best-effort `setStatus(taskId,'in_progress')` before launch (post-task's terminal write is guarded `IF $cur IN [in_progress,review]` + ALLOWED_TRANSITIONS makes done/failed legal only from in_progress/review, so a `ready` task would have stayed ready even with post-task wired). Closes ready→in_progress→done|failed; 3 integration tests vs real SurrealDB (done→committed+completion-event-with-sha, failed→no-commit F-007, negative control). HB-2 `6d83d4f`: `testCommandFor(buildTool,projectRoot)` + `resolveTestCommand` — honest skip when no real test target (ROUNDS dotnet+no-test-project → null, never false-fail / no false follow-up); fail-closed unknown. liveVerified via real-DB integration (no UI surface; HB-1 honest-skipped browser b/c token unset → boot returns started:false). **DEFERRED LEDGER → chained `heartbeat-harden`:** HIGH (perProject inert) + MEDIUM (silent divergence); 2 LOW (sync-fs-on-drain, resolver-discards-custom-cmd) G3-deferred. | auto |
| done | heartbeat-harden | **✅ DONE (run `wf_3175fcd0-adc`, 2026-06-23) — both tasks build+review+redTeam PASS, pushed.** HB-H1 `a3db470`: `concurrency.perProject` now ENFORCED — wired config→boot→Orchestrator + a `#inFlightByProject` per-project gate (drain parks a same-project task_run beyond the cap via claimNext `excludeProjectIds`; counter dropped on EVERY exit path incl. throw, floored, no leak/wedge). F-046 same-repo commit race closed for task_run. HB-H2 `7404cf2`: silent divergence closed AT CLAIM — `#runItem` captures whether ready→in_progress actually landed (`preStateEligible`); a non-ready/stale claim now REFUSES post-task (no commit), marks the work_item FAILED, and writes an observable `error` divergence event (the {commit+ok+non-terminal} state is unreachable at claim time). Happy path byte-unchanged. **DEFERRED LEDGER → 2 MEDIUM gate a CLEAN go-live (chained `heartbeat-golive`):** (M1) per-project gate scopes to `task_run` only — a `review` work-item ALSO launchSession+commits in project.root_path → bypasses the gate (LATENT: review-enqueue not yet wired live; generalize the gate to ALL session-spawning work types before it ships); (M2) post-task.ts internal TOCTOU — commit (post-task.ts:261) precedes the guarded terminal UPDATE (343), so a concurrent status move DURING a run re-opens the same {commit+ok event+non-terminal task} divergence. + 1 LOW (flaky pre-existing semaphore-CAP timing test, G3). | auto |
| done | heartbeat-golive | **✅ DONE (run `wf_b73703ad-15c`, 2026-06-23) — both tasks build+review+redTeam PASS, pushed. The heartbeat is now CLEAN to turn on live (operator chose 'one more wave first').** GL-1 `285b5ca`: per-project gate generalized to a single-source `CWD_SPAWNING_WORK_TYPES=['task_run','review']` (workqueue.ts) — both gated SELECTs + the orchestrator bump derive from it (park/bump/exclude sets can't drift); forks (memory_review/hire_request) stay UNGATED. GL-2/HB-H3 `43a61cf`: post-task mid-run TOCTOU closed — the guarded terminal transition runs FIRST and reports landed; commit+test+follow-up+ok-completion happen ONLY on landed; a mid-run concurrent move → NO commit, work_item failed, observable `post-task-divergence-midrun` error event (+ a step-5 belt-and-suspenders re-check). HB-1/HB-2/HB-H1/HB-H2 all green. **DEFERRED LEDGER (all LATENT or LOW — none block go-live): 2 MEDIUM both gated on the (currently-unwired) review-enqueue path** — (a) `follow_up` work_item must be added to CWD_SPAWNING_WORK_TYPES if its consume path is ever wired to spawn; (b) #runItem has NO `review` branch — a drained `review` would re-run the task_run path (re-spawn+re-commit, not a code review), so the review-wiring wave MUST add a reviewer branch BEFORE enabling maybeEnqueueReview. 3 LOW (untested sub-ms divergence branch; dangling follow_up on that window self-heals via HB-H2; G3). **⏸ OPERATOR PAUSE (2026-06-23): paused after this wave — NOT restarting the server / NOT hitting Continue; holding for direction. Go-live = restart clean → Continue → armed PM drives.** | auto |
| (superseded) | heartbeat-harden-detail | **CHAINED from heartbeat-loop deferred ledger (2026-06-23) — must land BEFORE the heartbeat is turned on live.** HB-H1 (HIGH): **`concurrency.perProject` is INERT** — the Orchestrator builds only a global `Semaphore(maxConcurrent)` (orchestrator.ts:176); perProject is parsed/validated (load.ts:867)/resource-profiled but NEVER passed to the Orchestrator (boot.ts:266 destructures without it) → the F-046 stopgap never worked (proven: the 19:43 batch ran 6 ROUNDS sessions concurrently). Now that HB-1's post-task COMMITS in the shared project.root_path, concurrent same-project sessions = live F-007/F-046 git/file-stomp race. FIX: enforce a per-project in-flight gate (wire perProject config→boot→Orchestrator; drain parks a task whose project already has perProject in-flight). HB-H2 (MEDIUM): heartbeat silent divergence — when the ready→in_progress setStatus is swallowed (non-ready/stale claimed task), post-task still commits + marks the work_item done + writes ok:true but leaves the task non-terminal → PM never informed + re-spawnable. FIX: don't silently "done" — fail the work_item / record the skipped-terminal so the half-state is observable. | auto |
| (superseded) | heartbeat-loop-detail | **THE KEYSTONE (2026-06-23) — makes Atelier "alive": task done → commit → PM informed → next batch.** ROUNDS root-cause: `startOrchestrator` (boot.ts:291) builds the Orchestrator with NO `postTask` → `#runItem` gates the commit+terminal-status+completion step on `this.#postTask?.enabled` (orchestrator.ts:468) → it never runs → every finished session leaves its task `ready` (work UNCOMMITTED, F-007 — observed: 4 done ROUNDS sessions' deliverables sat uncommitted, rescued in `ca789a7`), the PM is never informed (pm-autonomous re-ticks on a task-terminal `db_change` that never fires), failed sessions never marked. **HB-1**: wire post-task in startOrchestrator (commit + terminal done/failed + completion agent_event + the task `db_change`). **HB-2**: resolve `test_command` HONESTLY by build_tool (reuse the EXP-H1 `buildCommandFor`/detect.ts:55-98 pattern) — `dotnet test` with no test project must SKIP honestly, NEVER false-fail a succeeded task nor spawn an endless `followUpOnTestFail` loop (post-task.ts:199/240); unknown→skip. Integrity: F-007 commit-before-terminal · F-014 best-effort never crash drain · no false-fail on missing tests. Operationally after green: restart + Continue → watch the loop turn. **PM ALREADY ARMED** (verified 2026-06-23: `pm:nl9s4937hr9qsvursqsi` autonomous=true + auto_publish_preauthorized=true — the flag lives on the `pm` ROW; an earlier `SELECT pm_autonomous FROM project` was a non-existent column → misleading null). So the armed PM was ONLY starved by the post-task gap. (PM has role_version:null — no certified PM role; may matter at tick time.) | auto |
| done | workspace-isolation | **✅ DONE (run `wf_25e93cf4-9e9`, 2026-06-24) — all 4 tasks build+review+redTeam PASS, pushed.** WI-1 `25c7b03` worktree manager (`acquireSessionWorktree`→{cwd,branch,cleanup}; off HEAD on `atelier/session/<id>` outside the project tree; fail-closed non-git; idempotent re-acquire; `worktree remove --force` keeps the branch for merge-back; D-018 assertLocalGit blocks push/remote/fetch/pull/--force). WI-2 `ef044a2` spawn wiring (WRITE intents {code-write,code-debug}→worktree cwd; READ unchanged; additive worktree_path/branch on the session row). WI-3 `5af6db1` merge-back+teardown (post-task commit cwd→the worktree [orchestrator.ts:694], merge-back runs AFTER it [748]: clean done→FF-merge+teardown+branch -d; diverged/dirty/failed/cancelled→preserve branch + D-026-screened note on MC-4; per-root in-memory lock serializes same-process merges, cross-process git-index-safe; never force/discard — F-007). WI-4 `0da81eb`+`7dabe12` raised perProject 1→3 (worktrees make >1 same-project writer safe; F-046 stopgap superseded, fails.md note `3066f6c`). **DEFERRED LEDGER → tracked as `wi-harden` (all LATENT or LOW — none block; operator chose feature throughput):** 1 latent HIGH (WI-1 concurrent SAME-session acquire could delete the winner's tree via cleanup-on-add-failure — unreachable in normal flow: unique session ids + exactly-once claim + post-termination resume; fix = a `didCreate` guard) + LOW (channel-resume doesn't re-persist worktree fields; merge-back noop-label overstates a behind-branch as 'merged'; preserved-conflict note misattributes a dirty-root ff-abort as 'diverged'; stale doc comment). | auto |
| done | wi-harden | **✅ DONE (run `wf_ee71d805-e27`, 2026-06-25) — both PASS, pushed.** WI-H1 `72f1309` didCreate guard: `acquireSessionWorktree` failure-cleanup runs ONLY for a tree this invocation created → a losing concurrent same-session acquire never removes the winner's live tree (work-loss HIGH closed; 3 race tests vs real temp git repos, incl. winner-uncommitted-survives). WI-H2 `ff1d7ac` merge-back honesty (dirty-tree note ≠ 'diverged'; behind-branch→honest noop; resume re-persists worktree fields; stale-doc fix). **DEFERRED MEDIUM → `wi-harden-2`: a SIBLING unguarded removal — the half-state-recovery `rm(worktreeDir)` at worktree.ts:278 is NOT didCreate-guarded, same work-loss class; DOUBLY-LATENT (needs a same-session double-acquire AND git `worktree list` lagging the on-disk dir — natural 15-iter race showed 0 loss). The Iron-Law smell: a removal path not gated by 'did this call materialize it'. Fix = guard the :278 rm the same way (only remove a dir this call created / re-verify unregistered+non-git-tracked before rm).** Original ↓ ⟨was⟩ TRACKED from workspace-isolation deferred ledger (2026-06-24) — latent/LOW, run in a consolidated hardening pass.** WI-H1 (latent HIGH): guard `acquireSessionWorktree` failure-cleanup with a `didCreate` flag so a losing concurrent same-session acquire NEVER removes a tree it didn't create (worktree.ts:295) — close the work-loss class even though unreachable today. WI-H2 (LOW honesty cluster): merge-back note — distinguish a dirty-project-root ff-abort ('local changes would be overwritten') from a true non-FF divergence; honest 'noop' label for a fully-reachable behind branch; fix the stale teardown-wiring doc comment; channel-resume re-persist worktree fields. | auto |
| (superseded) | workspace-isolation-detail | **LAUNCHED `wf_25e93cf4-9e9` (2026-06-23) — SPEC `docs/WORKSPACE-ISOLATION-SPEC.md`.** WI-1 worktree manager (per-session `git worktree`, fail-closed non-git, idempotent re-acquire) · WI-2 launch wiring (WRITE intents {code-write,code-debug} → worktree cwd; READ unchanged; additive session worktree_path/branch) · WI-3 merge-back+teardown (clean done→FF-merge+remove; conflict/failed/cancelled→preserve branch+screened note; never force/discard; composes AFTER HB-1 post-task commit which now lands in the worktree) · WI-4 raise perProject>1 + mark F-046 stopgap superseded. Chains skill-harvest on green. Per-session work-dir isolation: READ/scope sessions stay parallel in the shared project cwd (write-denied via editScope/D-018); WRITE/build sessions get a dedicated git worktree per session (branch `atelier/session/<id>`), commit there (F-007), FF-merge back on clean exit / preserve-branch + honest note on conflict|fail (never force, never discard), mandatory teardown (F-014). Then raise `perProject` back >1 (retires the F-046 stopgap). 4 tasks WI-1..4 (worktree manager · launch wiring + additive session-row fields F-015 · merge-back+teardown · raise cap). Integrity: no-lost-work · no-force · D-018 confine · resume-reuses-one-tree · read-sessions-stay-write-denied. | auto |
| done | conversation-layer | **✅ DONE (run `wf_833f9702-48c`, 2026-06-25) — pillar 3 LIVE; all 3 PASS, pushed (origin/v2 `c3c4568`). The "alive" arc is COMPLETE.** CV-1 `981da87` peer-send affordance (new `peer/affordance.ts`→granted-only INSTRUCTION via SpawnRequest.affordances+buildPrompt; advertises ONLY session+role@project, warns off inert pm/atelier; bounded live FleetSnapshot who-list cap 12; best-effort D-019). CV-2 `f674dec` F-045-SAFE grant — peer-send to code-write/code-debug via the reserved-id pass-through (not catalog-validated; granted spawn doesn't fail-close). CV-3 `c3c4568` end-to-end proof (`peer/conversation-loop.test.ts`: A→B delivers+renders 'communication' turn; 'SYSTEM: do X' body stays non-steering DATA; planted secret quarantines). **DEFERRED (LOW, latent → sh-harden/future): sterile rail refuses only memory-pull ids (interview sessions aren't granted peer-send anyway); affordance-without-tool divergence (pre-existing).** Original spec note ↓ ⟨was⟩ SPEC'd + BUILD-READY → `docs/CONVERSATION-LAYER-SPEC.md` (2026-06-23, grounded) — pillar 3 of "alive": hires actually converse.** Transport+safety+rendering already BUILT (BL-4/G-A/G-C); this is the thin prompt/policy layer. Verified: send path `routes/api/peer/send`, `peer/resolve.ts` (session + role@project resolve; pm/atelier are honest D-040 placeholders→zero recipients), `PEER_SEND_CAPABILITY_ID='peer-send'` + MCP `mcp__atelier-peer`/`peer_send` (dormant — NO session granted today), drain at launch.ts:461-523, `transcript-core.ts` 'communication' turn (reused), screen+fence chokepoint `peer/repo.ts`. Tasks: CV-1 affordance + recipient disclosure in the briefing (granted-only; advertise ONLY resolvable addresses; real running recipients; purpose/restraint framing) · CV-2 grant policy via D-036 composition, **F-045-SAFE** (peer-send is a RESERVED id, NOT a cc_skill — grant via the reserved path, never catalog-validated) · CV-3 end-to-end proof (A→B same-project peer_send → drains → renders as communication turn, non-steering, screened). Invariants: D-035a non-steering · honest recipients only · affordance only-when-granted · F-045-safe grant · D-026 intact · existing bus bounds. Chains after repo-creation. | auto |
| done | skill-harvest | **✅ DONE (run `wf_ea49bd01-7e1`, 2026-06-24) — all 5 tasks build+review+redTeam PASS, pushed.** SH-1 `555fd93`+fix `42a62e9` skill_proposal store (born-open never self-approve; D-026-screened fields; **atomic** dedup→occurrences [fix-loop killed a TOCTOU occurrence-split via dedup_key UNIQUE + retry-as-bump]; m0060/m0061 idempotent). SH-5 `1068915`+fixes `b29c59d`/`b07696b` harness-owned harvest cc_scope (promoted skills reach `catalogIds` — closes F-045; **fix-loop closed a syncScope concurrency MEDIUM**: digest-gated re-sync + deterministic-id guarded-UPSERT across cc_settings + cc_skill/hook/agent/mcp child tables, **F-047 logged**). SH-2 `7a7caa7` capture seam (best-effort injected SkillHarvester at session-end, stub-in-tests, narrow). SH-3 `5dff0e6` promote (operator-approval-gated, confined disk-first→sync, no self-promotion, idempotent, F-045 compose-passes). SH-4 `41b7b2a`+fix `3f58455` /skills operator UI (approve→promote / edit-then-approve [fix-loop: in-place edit, no occurrences-bump, body actually promoted] / reject-mark; server-resolved approver; live-verified end-to-end). **DEFERRED → `sh-harden`:** 1 MEDIUM (SH-3 re-screen only body at disk boundary — name/desc ARE screened at proposal entry, asymmetry only) + the GO-LIVE gap (no production SkillHarvester factory+boot wiring yet → capture loop is seam-only/DORMANT, like peer-send; needs a makeSkillHarvestAgent mirroring makePmProposalAgent) + LOWs (bump-path evidence loss, DATA-MODEL doc, collision-guard existsSync, no UNIQUE on name, temp-file litter). | auto |
| done | sh-harden | **✅ DONE (run `wf_3fe3c4ea-3ec`, 2026-06-25) — both tasks PASS, pushed.** SHH-1 `28651a9`+fix `6edfc18` GO-LIVE: `makeSkillHarvestAgent` (new harvest-agent.ts, mirrors makePmProposalAgent — bounded READ-ONLY deep-explore over the screened trajectory, can't recurse) wired at boot + onto every spawn; **the capture loop is now LIVE** (successful code-write sessions draft born-'open' proposals; best-effort D-019, screened D-026, narrow trigger, G2). SHH-2 `ce7ce4d` promote/store hardening (D-026 re-screen symmetry on description+name at the disk boundary, scope-attributed collision guard not existsSync, same-name promote serialization, dedup evidence-union, stale-temp sweep, DATA-MODEL §4 entry). **DEFERRED (non-blocking): MEDIUM** editApprove rename-into-an-already-approved-name; **LOW** SH-1 dedup TOCTOU test flakes under full skills-suite concurrency (passes isolated; harden the read-or-bump txn / test isolation) + evidence-union ordering. | auto |
| done | usage-observability | **✅ DONE (run `wf_32ee55c3-85a`, 2026-06-25) — all 3 tasks PASS, pushed, CLEAN (no deferred ledger). Answers "who uses what tools/skills."** UO-1 `4ffe68e` persist the GRANTED capability set on the session at CREATE (m0065: granted_skills/agents/mcp/reserved + tool_allow + granted_intent; effective composed set incl. peer-send when peerSendGranted; F-013 omit-absent, legacy row→null 'not recorded'; exposed on fleet read-back). UO-2 `1044642` usage read model (`observability/usage.ts`: per-session tool_use→count breakdown + cross-session granted-vs-used roll-up keyed by capability/tool→sessions/tasks/roles; GRANTED≠USED kept distinct; bounded F-014; pure read, no write path). UO-3 `be8a1e2` surface on /agents (per session/task/agent granted + used; dead-grant [granted-but-unused] visually distinct; honest states). Chains wi-harden next. | auto |
| (superseded) | sh-harden-detail | **LAUNCHED `wf_3fe3c4ea-3ec` (2026-06-25).** SHH-1 GO-LIVE: wire a production `makeSkillHarvestAgent` + boot (mirror makePmProposalAgent) so successful code-write sessions actually draft proposals (loop is dormant until this). SHH-2 (redTeam): symmetric D-026 re-screen of description+name at the promote disk boundary, scope-attributed collision guard (not existsSync), same-name promote serialization, + LOW (evidence-union on bump, stale-temp sweep, DATA-MODEL §4 entry). Chains wi-harden next. Original ↓ ⟨was⟩ TRACKED from skill-harvest deferred ledger (2026-06-24).** Make the harvest loop LIVE + tighten: (a) **wire a production `SkillHarvester` generator + boot** (mirror `makePmProposalAgent`) so successful code-write sessions actually draft proposals — without this the loop is built-but-dormant; (b) SH-3 MEDIUM — symmetric D-026 re-screen of description+name at the disk boundary (defense-in-depth); (c) LOW cluster — union evidence on dedup-bump, UNIQUE/serialize same-name promote, collision-guard scope attribution (not existsSync), stale-temp sweep, DATA-MODEL §4 skill_proposal entry. | auto |
| done | lifecycle-graph | **✅ DONE (run `wf_1c6cd860-836`, 2026-06-25) — all 3 PASS, pushed (origin/v2 `1da738e`). The live causal node-graph the operator wanted.** LG-1 `538dee7` provenance (m0066 scene_event kinds continue/batch_drained/pm_tick + m0067 `agent_event.parent_event_id`; Continue marker from continueReadyTasks, pm_tick from the autonomous re-tick, spawn-cause = triggering work_item id; all additive/best-effort/screened, F-048-safe — a throwing emit never fails continue/PM/drain). LG-2 `456be43` read model (`observability/lifecycle.ts`: Continue→session→PM→task nodes [task name, role/title, hire, tool-count, skills] + edges, explicit-vs-inferred marked, bounded). LG-3 `1da738e` animated UI at **`/projects/[id]/graph`** (live via onDbChange SSE; extends the existing scene-graph client). **DEFERRED LOW: scene-graph KIND_VERB/color map could add explicit entries for the 3 new kinds (degrades safely now); #emitPmTick comment imprecision.** | auto |
| done | lifecycle-graph-ux | **✅ DONE (run `wf_6dafca08-a7e`, 2026-06-25) — all 4 PASS, pushed (origin/v2 `27c9738`). Operator v2 polish of `/projects/[id]/graph`.** (Grounding correction: the graph already used a rank-based layered layout via `components/graph/layout.ts` — d3-force is the SEPARATE /memory scene; agents delivered the POLISH.) GUX-1 `92bef8e` n8n-style cards (foreignObject, ellipsis title, meta grid, status rail) + arrowed solid/dashed bezier connectors + per-column header band + per-node DETAIL threaded into LG-2 (Σ tokens/cost/duration from agent_event + per-tool counts; honest '—'). GUX-2 `9a2c072` alive motion (cycling running glow, edge draw-in, honest settled done/failed states; reduced-motion static). GUX-3 `c0b705a` node-detail popover complete (task description + causal path; **focus-trapped** — closed GUX-1's deferred a11y note). GUX-4 `27c9738` data-shared edges (peer_message) + multi-node→PM funnel + full-path focus (highlight lineage, dim rest). Live-verified (29 session cards, real token detail). **DEFERRED LOW: connector/funnel/motion visuals unit-tested only (live atelier_self graph had 0 edges/1 column — exercise when multi-edge data exists); popover right-edge auto-flip; per-tool cap 8.** | auto |
| done | command-center-ux | **✅ DONE (run `wf_ead61c14-0f1`, 2026-06-25) — all 4 tasks PASS, pushed (origin/v2 `52c770f`). Overview readable/usable.** CC-1 `84cc712`+fixes `3ce815d`/`3a55ebf` failure-reason classifier (`shell/failure-classify.ts` — pure/total: raw screened note → {category, shortLabel, detail, icon}; categories grounded in real producers [reaper/merge-back/capabilities-D036/launch/cli-backend]; UNKNOWN→honest fallback; one clean line + `<details>` collapsible full text, D-026 unchanged, contrast/a11y preserved; 47 tests). CC-2 `f06e83c`+fix `42b6e28` start-time + live-ticking elapsed (one shared clock; F-013 honest '—'; fix coerced the SurrealDB DateTime in fleet.ts). CC-3 `9e44a65` overview IA — at-a-glance summary + progressive disclosure (no surface removed). CC-4 `52c770f` PM/HR agency activity surfaced in the overview pulse (not just dev sessions). **DEFERRED LOW (cosmetic): CC-1 post-`·` residual — a literal ` · ` inside an agent's result-tail before a marker phrase can mislabel the category TAG (full detail still correct + collapsed); tighten to only scan appended segments with a known OUR-advisory prefix.** | auto |
| (superseded) | skill-harvest-detail | **LAUNCHED `wf_ea49bd01-7e1` (2026-06-24) — SPEC `docs/SKILL-HARVEST-SPEC.md`.** SH-1 skill_proposal model+store (dedup→occurrences, D-026 evidence, additive migration) · SH-5 synced harvest scope (load-bearing F-045 fix — a harness-owned `.claude` cc_scope so promoted skills reach cc_skill) · SH-2 capture seam (best-effort injected SkillHarvester at session-end, stub-in-tests, narrow: successful code-write) · SH-3 promote (operator-approval-gated, confined SKILL.md disk-first→sync, no self-promotion/poisoning, idempotent) · SH-4 operator review UI (approve/edit/reject, honest states). Chains repo-creation on green. Agents PROPOSE reusable skills; operator promotes (G2/D-039). 5 tasks: SH-1 `skill_proposal` model+store (additive, D-026-screened evidence, dedup→occurrences) · SH-2 capture seam at the launch.ts §3.2 session-end (injected `SkillHarvester`, stub-in-tests like PmProposalGenerator, best-effort D-019) · SH-3 promote path (write confined SKILL.md to a synced scope's `.claude/skills/` THEN syncScope; disk-truth-first) · SH-4 operator review UI (approve/edit/reject, honest states) · SH-5 register ONE harvest cc_scope so a promoted skill actually reaches `cc_skill` (the F-045 dead-end fix; load-bearing). Integrity: no-self-promotion (approved_by required) · D-026 screen · disk-is-truth · D-018 confine · no catalog poisoning · F-015 additive · G2 mark-don't-delete. Prior art: hermes (disk SKILL.md + provenance) + kongcode (extraction schema + source-tag, NOT auto-promote). | auto |

**DIRECTION (operator, 2026-06-23) — "make Atelier + its projects feel ALIVE":** active development, conversations between hires, projects expanding autonomously and with purpose. The throughline = the HEARTBEAT loop (task done → commit → PM informed → PM spawns review/next + extends the roadmap → repeat). Three pillars, one keystone: (1) **active development** = the heartbeat (heartbeat-loop wave — was DEAD: post-task unwired); (2) **autonomous expansion with purpose** = the armed PM re-ticking toward DoD + extending the roadmap (built; was starved by #1 + disarmed); (3) **conversations between hires** = the peer-message bus + global transcript (substrate BUILT [BL-4/G-C], but hires aren't yet PROMPTED to talk → a prompt/surfacing layer, queue after the heartbeat turns). New capability raised same day: BL-0 repo-creation (private-first, operator or PM-proposed). Sequence: heartbeat → arm autonomy → conversation layer; workspace-isolation + skill-harvest support the expansion.

Standing rails for every autonomous stretch: v2-wave template only (fix-loops + auto-push), F-014 server discipline, no real external publishes without the operator's gate confirm, docs commits to v2-main / code to v2, memory + this file updated at every wave boundary.

**Owed deferred correction (fable-5 retired 2026-06-13):** `config/workforce.yaml` `pm.model_id` + 5 pinning test files (gauntlet.test.ts, gauntlet.live.test.ts, pm-proposals.test.ts, pm-session.test.ts, config/load.test.ts) still record `claude-fable-5`. Operator decided **opus-everywhere incl. PM identity**, but the value only matters when the PM actually spawns — applying it now would churn 5 tests for an unused identity. **Apply the fable-5→claude-opus-4-8 sweep AT the PM-hire wave (wave-create-ai)**, traced through every consumer (the §G5 enum sweep), as a real BUILD→review task. Until then: no NEW fable-5 literals anywhere (wave commonExtra enforces this).

## Backlog (unscheduled, in operator-set order — spec before build, none scheduled)

| # | item | notes |
|---|---|---|
| per-hire-soul | **Per-hire soul + comms topology (D-041)** | **SPEC'd → `docs/PER-HIRE-SOUL-SPEC.md` (2026-07-07), grounded.** Decision [[DECISIONS#D-041]]: KEEP the cross-project wall (project↔project direct messaging forbidden, `atelier` = sole cross-project broker, D-035a non-steering reaffirmed) — aliveness lives in the **in-project mesh**; and **every hire gets a compute-on-read SOUL** — extend S4 `memory/soul.ts` (`SoulModel`/`MaturityStage`/`formatSoulBlock`/`isColdBrain`) to each role, deriving identity from live workforce rows (role_version history, gauntlet fixture **provenance** = proven defect classes §3.8, `interview_run` passes, tier proofs §7, `track-record` calibration, `project_staff` served-in, experience volume) + a per-role maturity ladder (nascent→certified→proven→veteran). **Read-model only — NO new reach, NO base migration** (pure projection, justified like D-040/soul.ts); optional `hire_soul_event` history deferred (fork §8). Surfaces on `/agents`; feeds **BL-3** capability-matching (a hire's soul IS its proven coverage). Tasks SL-1..SL-5; redTeam SL-4 (soul block cannot steer/leak; topology unchanged). Verified seams cited in the spec. | operator |
| game-verify-in-loop | **Close the verify→auto-fix→re-verify arc** | **SPEC'd → `docs/GAME-VERIFY-IN-LOOP-SPEC.md` (2026-07-07), grounded.** The GAME-VERIFY runner/gate/persist/surface + `follow_up` enqueue are ALL built+wired (`boot.ts:436`), but nothing CONSUMES the follow_up: `#runItem`'s default branch reads `payload.taskId` (empty) not `parentTaskId` (`orchestrator.ts:709`) → claimed follow_up spawns nothing (+ suspected claim/permit leak on the early return). Same dead-end drops post-task's TEST-FAIL follow_ups. Fix: a bounded `follow_up` consumer that re-drives a build with the screened fix-signal — **BOUNDED (max_iterations, the RH-1/BL-R4 infinite-re-drive lesson)**, auth-gated (armed/autonomous else propose-only), leak fixed. Tasks GV-1..GV-5. **Unblocks hands-off game-mod dev (the ROUNDS bottleneck) + closes test-fail auto-iteration.** | operator |
| memory-curator | **Slow curator tier (BL-7C)** | **SPEC'd → `docs/MEMORY-CURATOR-SPEC.md` (2026-07-07), grounded.** The parked half of the D-027 two-tier loop. `consolidate()` executor is BUILT (`loop.ts:571`) but has ZERO callers; the member-selector/trigger, the D-028 conflict detector (`references.contradicts` schema-defined, runtime-unwritten), the D-015 time-archive runner, and the operator-confirm soft-delete are all MISSING. Core design: **split DETECT/propose (L1, read-only/safe) from APPLY (live, operator-gated, G2)** — the throwaway-DB rule governs measurement, not the curator's live writes. Hosts as a `MAINT_MEMORY_CURATOR` maintenance action. Honors D-015/D-028/D-030/D-031/G2 (time-based archive-not-delete, ADD-only, never prune-on-utilization, never auto-delete). Tasks CUR-1..CUR-5. | operator |
| concierge-broker | **Concierge: passive hub → active broker (D-041 #4)** | **SPEC'd → `docs/CONCIERGE-BROKER-SPEC.md` (2026-07-07), grounded.** Concierge advises in prose only (`concierge.ts:854`), never imports `capability-match`, never originates a cross-project leg. Add: (1) a `capability_query` intent + a cross-project `provenRolesForCapability` aggregator (reuse `roleProvenCoverage`) → structured "who's proven on X" answer; (2) a MEDIATED introduction — atelier originates the second leg (allowed, atelier=sole cross-project identity), re-screened (D-026), audited (`broker_introduction`), A↔B direct stays FORBIDDEN. Non-steering (D-035a) preserved; topology unchanged (broker mediates). Tasks CB-1..CB-4 (answer first, introduction as follow-on). | operator |
| release-pipeline | **RELEASE — versioned publish, last mile** | **SPEC'd → `docs/RELEASE-SPEC.md` (2026-07-07), grounded.** Closes the RELEASE lifecycle phase + powers `new-mod-test`'s first real publish. SUBSTRATE IS BUILT (D-037 adapter framework `adapters/types.ts` contract + `registry.ts`/`catalog.ts` pluggable + `driver.ts` confirm-token + `release-gate.ts runReleaseReadinessGate` + `release/pipeline.ts RELEASE_STAGES` + npm/thunderstore/github-releases adapters + `sync/gh-client.ts`). DELTA = last mile: (1) no release ROUTE/UI (pipeline unreachable — grep zero hits; mirror repoCreate `+page.server.ts:1610`); (2) real external upload DEFERRED in every adapter ("prepared but NOT executed") → flip behind the confirm token, Thunderstore first; (3) add `verify()` to `PublisherAdapter` + post-publish stage; (4) deterministic version-bump+git-tag (fork). Absorbs the D-037 DEPLOY-ADAPTER gap (framework already built — documented as RELEASE §1). Publish stays operator-gated even under autonomous-to-v1. Tasks RL-1..RL-5. | operator |
| maintain-phase | **MAINTAIN — loop-driven project maintenance** | **SPEC'd → `docs/MAINTAIN-SPEC.md` (2026-07-07), grounded.** Completes the MAINTAIN lifecycle phase. SCANNERS ARE BUILT + already project-facing (`scanner/` security/dep-health/UX over `confineToRoot(project.root_path)` → `security_finding` table → Maintain panel + /reports). DELTA: (1) scans are MANUAL-only (route action `+page.server.ts:1979`) → register a project-scoped `MAINT_PROJECT_SCAN` action wrapping the built `scanProject()`; (2) `MaintenanceLoopEngine` fires GLOBAL rows only (`maintenance.ts:93,218`) → teach it project-scoped (`projectId != null`) firing, gates preserved; (3) findings never propose work → new MEDIUM+ finding → `proposeTask` (D-039), dedup one-open-per-family. Propose-not-fix (never edits project code); confined (D-018); append-only findings (D-015). Tasks MT-1..MT-4. | operator |
| done | repo-creation-finish | **✅ DONE (run `wf_71ef6f1a-19b`, 2026-06-25) — repo-creation feature COMPLETE; both tasks PASS, pushed (origin/v2 `7a4cded`).** RC-4 `89117aa` operator UI ('GitHub repository' card on the project Settings tab — private-only two-step confirm driving the existing RC-3 action; honest created/no-repo/creating/named-failure states; surfaces repo_url + PM brief approve/reject with operatorConfirmed B4 wall; live-verified up to but NOT through the real outward create). RC-H `7a4cded` closed all 5 RC-1/2/3 outward-path findings — **red-team caught a real HIGH** (the stale-origin guard initially covered only the already-exists branch, NOT the `created` push path → fix-loop reconciled the origin URL before EVERY push at root). Landed: stale-origin push guard (sameRepoTarget — derive canonical URL from owner/slug, push only to a matching remote), branch-name validation + `--` separator (no flag-misparse), resolveOwner (well-formed URL), permission-denied wording tightened, error-leak cleaned. **Deferred: LOW only** (parseRepoTarget host-regex breadth, G3). **Repo-creation = DONE (RC-1..RC-4 + RC-H).** | auto |
| (superseded) | repo-creation-finish-detail | **LAUNCHED `wf_71ef6f1a-19b` (2026-06-25) — completes repo-creation.** RC-4 operator UI ('Create repo (private)' control driving the existing RC-3 server action; consent/confirm; honest states; surfaces repo_url + PM brief). RC-H (redTeam) closes the RC-1/2/3 deferred ledger: **stale-origin push guard** (derive canonical URL from slug, never push to a mismatched recorded remote on already-exists), branch-name validation (`-`-prefix flag-misparse), owner-less already-exists URL, isPermissionDenied /scope/ over-broad, gh error-string cosmetic. gh stubbed — no real repo. Chains conversation-layer on green. | auto |
| partial | repo-creation (BL-0) | **⏸ RC-1/2/3 DONE+PUSHED (origin/v2 `64df4de`); RC-4+hardening now running as `repo-creation-finish` (wf_71ef6f1a-19b). Earlier stop note ↓.** STOPPED mid-RC-4 per operator (2026-06-24, run `wf_a96d98d0-a7d`). RC-1/2/3 DONE+reviewed+red-teamed+PUSHED (origin/v2 `64df4de`); RC-4 (operator UI) INCOMPLETE — partial edit reverted, clean tree.** RC-1 `4619f5b` createRepo --private (public unrepresentable, honest outcomes, stubbed gh). RC-2 `7e7af85` repo-creation-gate (D-037 consent+confirm-token → auth → createRepo → remote add/push via dedicated OUTWARD runner → repo_url; assertLocalGit UNCHANGED). RC-3 `4ab3d7c`+fix `64df4de` two trigger paths (operator-create + PM-proposed-via-operator-gate, never unilateral; fix guards brief.status==='open' BEFORE the irreversible create). **RESUME:** `Workflow({scriptPath:".../v2-wave-wf_a96d98d0-a7d.js", resumeFromRunId:"wf_a96d98d0-a7d"})` → RC-1/2/3 return cached, only RC-4 re-runs. **Deferred ledger (RC-1/2/3 red-team, fold into resume/hardening):** stale-origin already-exists push guard (derive canonical URL from slug, not trusted repo_url); branch-name validation at the gate (`-`-prefixed branch); owner-less already-exists URL synthesis; isPermissionDenied /scope/ over-broad; gh error.reason internal-string cosmetic. | operator |
| BL-0 | **Repo creation (private-first) — original spec note** | **SPEC'd + BUILD-READY → `docs/REPO-CREATION-SPEC.md` (2026-06-23, grounded).** Verified seams: REUSE gh-client.ts `GitHubClient`/`runGh` (gh CLI, GH_TOKEN), release-gate.ts D-037 consent+confirm-token pattern, post-task.ts execFileRunner, project.repo_url (exists), §4.1 proposeTask+panel. BUILD NEW: `createRepo(--private)`, repo-creation-gate (mirror release-gate), 'repo-create' proposal kind, operator UI. Tasks RC-1..RC-4. Private-first hardcoded (public unrepresentable); D-037 gate (no unilateral PM create, B4/D-039); local-only git rule (FORBIDDEN_GIT) PRESERVED — outward remote/push lives only in the gated repo-creation runner. Chains after skill-harvest. Original raise ↓. Operator-raised (2026-06-23): the operator can create a repo for a project, AND a PM can create one when it judges the project needs one — **ALWAYS private first** (public is a separate later operator-gated flip, NEVER at creation). Outward-facing (real GitHub resources) → D-037-class gating: operator-create direct; PM-create goes through §4.1 propose → panel → operator gate (PM never creates an external repo unilaterally, like publish/hire). Pairs with workspace-isolation (a per-session worktree wants a real repo) + the autonomous-expansion pillar. **Spec-first, GROUNDED** (no-guessing): find the existing GitHub/git auth seam (token via .env, D-026 screen), private-default + idempotent create + honest failure, who-can-trigger + the consent record. Needs a spec when promoted. |
| BL-R1 | **recovery-harden (crash/restart work-queue recovery)** | Surfaced at go-live (2026-06-25, F-048 incident). The F-048 hotfix (`6fb4d3d`) stopped the dedup-collision CRASH, but two deeper bugs remain (live-recovered by hand via DB this time): (1) **a reaped session doesn't release its work_items** — the boot reaper marks dead SESSIONS failed but leaves their `work_item`s stuck `processing` (claim_token held) + their TASKS stuck `in_progress`, orphaned forever (Continue only re-drives `ready`). Fix: the reaper must, for each reaped session, fail/release its claimed work_items AND reset its task to `ready` (or failed). (2) **claim starvation** — `claimNext` on a dedup collision `continue`s but re-SELECTs the SAME top-priority colliding item every retry; a STUCK-`processing` twin (dead session) starves the whole drain → 0 sessions. Fix: on collision exclude that candidate from the pass (or gcStale stale-`processing` items so the twin frees). Also verify `gcStale` is wired into the drain + its threshold. Structural option (F-048 deferred): collapse the dedup active-window so {pending,processing} dedup against each other (prevent the 2nd enqueue). Belongs to a recovery-hardening wave. |
| BL-RC-FIX | **repo-creation gate push assumes `main` (F-050)** | Live at ROUNDS go-live (2026-06-26): the repoCreate gate created the private repo but its push leg (RC-2) hardcodes `git push -u origin main` → failed on ROUNDS' `master`-default local repo (`src refspec main does not match any`, `failedAt:'remote'`, "repo exists but unbacked"). Worked around live (rename master→main + push + idempotent re-run backed it + set repo_url). **Operator directive (2026-06-26): `main` is ALWAYS the default; `master` is deprecated.** So the fix is NOT "tolerate master" — it's ensure `main` everywhere: the Create-with-AI scaffolder must BIRTH repos on `main` (the root defect — ROUNDS was scaffolded on master), and the gate normalizes any `master→main` before pushing + asserts. Small; BL-0/repo-creation family. F-050 + [[feedback_main-default-branch]]. |
| BL-R4 | **no product re-run path for a `failed` task** | Surfaced live at ROUNDS go-live (2026-06-26). A `failed` task is a dead-end: `restartSession` refuses (409 "task status failed is not re-runnable — the task has moved on… Move it back to ready to restart"), but `failed` is TERMINAL in `ALLOWED_TRANSITIONS` (`tasks/repo.ts:61`) so `moveTask` can't do `failed→ready` either — the restart action's own error message advises an impossible move. The only recovery today is the PM re-proposing the work as a fresh `proposed` task (which it did for 2 of ROUNDS' 6 failed). Gap: an operator-authority `failed→ready` (or a `restartSession` that resets the task) so failed work is re-drivable after the blocker (e.g. missing env) is fixed — without relying on the PM to re-propose. Relates to BL-R2/BL-R3 recovery family + the operator command-center controls. |
| BL-R3 | **task-status recovery completeness (2 deferred MEDIUMs from RH-1 red-team)** | Surfaced by the recovery-harden-2 RH-1 review+redTeam (2026-06-26), out of RH-1 scope. (1) **Success-side twin:** a SUCCESSFUL task_run with post-task DISABLED (the Orchestrator constructor default) still strands its task `in_progress` — ok=true skips BL-R2's failed-write and no terminal writer runs. **Mooted in production** because boot (HB-1) enables post-task, but the default-off path is a latent trap. (2) **Concurrent same-task clobber (F-048 class):** two task_run work_items for the SAME task are reachable (dedup_key keys on status → a 2nd enqueue makes a fresh `pending` while the 1st is `processing`); BL-R2's failed-write on one could flip the task `failed` while its twin is still running. Both fold into the F-048 deferred structural fix (collapse the dedup active-window so {pending,processing} dedup against each other). Small; well-scoped; next recovery pass. |
| BL-GUX-2 | **lifecycle-graph is vertical + no connectors — causality emission + readability** | Live diagnosis at ROUNDS go-live (2026-06-26). NOT a layout bug (layout.ts assigns x=col left→right correctly) and NOT a viewing mode (graph is project-scoped, all sessions, no selection needed). ROOT CAUSE = data shape: ROUNDS has **73 session nodes but only 7/197 agent_events carry `parent_event_id`** (the LG-1 causal link) + **1 continue + 2 pm_tick scene markers + 0 peer_messages** → almost every node is a rootless node piling into column 0 (the "very vertical" pile) and the handful of real edges (≈8 PM→task via proposed_by, a few continue→session) are swamped/likely dropped against 73 roots → "no connectors". TWO fixes: (1) **causality emission** — the runtime must set `parent_event_id` on spawn/completion events consistently so Continue→session→PM chains actually form (7/197 is far too sparse; that's why columns never advance); (2) **readability** — scope the graph to the CURRENT/recent lifecycle window (e.g. since last Continue) or group/collapse historical roots instead of dumping all 73 sessions as column-0 nodes; verify edges referencing in-view nodes aren't filtered by the node cap (lifecycle.ts:581 nodeIds set). Needs server DOWN (build wave) → conflicts with the ROUNDS hands-off drive; do in a lull/after. Folds in BL-GUX-FIX (max-width cap + a11y) below. |
| BL-GUX-FIX | **lifecycle-graph DoD test failures (pre-existing)** | 3 failing tests on the graph UI, introduced by lifecycle-graph work (`1da738e`), caught as collateral by recovery-harden-2's full `npm test` (NOT this wave's code): (1) `layout-gate.test.ts` — `routes/projects/[id]/graph/+page.svelte` declares `max-width:1100px` (banned page cap, full-viewport standard 14.2a); (2,3) `route-head-a11y.test.ts` — graph page `<title>` not ` — Atelier`-suffixed + `projects/[id]/+page.svelte` count-badge missing `aria-hidden`. Real D-038 gaps on a feature we're about to showcase. Tiny (remove/tokenize the cap, suffix the title, add aria-hidden). Fix before or right after go-live. |
| done | BL-R2 | **failed-spawn task left in_progress ✅ DONE as recovery-harden-2 RH-1 (`d11db2f`).** Original note ↓. Surfaced by the BL-R1 red-team (2026-06-26, NOT fixed there — out of scope). A FAILED `task_run` spawn (`orchestrator.ts:777` catch) leaves its task on `in_progress` because post-task never runs — and `task_run` work_items do NOT carry `session` (the session is created AFTER the claim), so BL-R1's `releaseSessionWork` can't reach it; it's recoverable only at the next boot reaper / gcStale window. Fix shape: on a spawn-failure in the `#runItem` catch, transition the task `in_progress→failed` (or `→ready` for retry) directly. Small, well-scoped; fold into the next recovery/hardening pass. Grounded at file:line by the BL-R1 build agent. |
| done | BL-H1 | **headroom-digest ✅ DONE (2026-06-26)** — digested through the cannibalize foundry (`F:\code\cannibalize`, source `headroom` pinned `9362747`, Apache-2.0, now `studied`; 13 findings + 1 playbook + 1 decision + 4 questions captured). v2-facing slice → `docs/HEADROOM-DIGEST.md` (committed v2-main). D-026 clone pre-screen: clean (all flags = placeholders/AWS example key/token-readers). **Key findings:** compression is LIVE-ZONE-ONLY (latest user message's content blocks; whole-message dropping retired) — lossy-on-wire/lossless-end-to-end via CCR (BLAKE3 marker + `headroom_retrieve`, on-disk SQLite, 30min TTL); cache floor keeps everything below byte-identical so the prompt cache still hits; ContentRouter routes JSON/logs/grep/diff to specialized crushers BUT **SourceCode/PlainText blocks are NO-OP** → on v2 code-write traffic the cut is likely far below the 92% headline (their own codebase-exploration bench = 47%, all headline numbers are tool-output-dominated). **Hard D-026 constraint:** CCR writes RAW UNREDACTED tool outputs to disk with NO secret detection → any spike must pre-screen + ephemeral store + loopback + short TTL. NOTE: digest ran against the FOUNDRY db (:8100) — did NOT need v2's server (earlier note was conservative). | auto |
| done | BL-H2 | **headroom-eval ✅ DONE (2026-06-26) — VERDICT: NO-ADOPT now, IDEA-ONLY later** (`docs/HEADROOM-DIGEST.md` §6). Ran a contained, credential-free OFFLINE measurement (more-contained pivot from the live proxy — compressors run in-process, no API traffic/Rust build needed) on real ai-playground tool outputs. Measured (0.9.7 pure-Python era): search 98% / log 89% / JSON 89% / git-diff 8.9% / source-code 0% no-op; aggregate 63–66%. **3 independent no-adopt blockers:** (1) Windows-incompatible as shipped — 0.20.15+ hard-imports the Rust `_core`, no `win_amd64` wheel, sdist build fails on MSVC `link.exe`; v2 is Windows-native; (2) the big cuts are LOSSY down-sampling and the proxy path at our pin uses the NON-CCR variant → no recovery → information loss on agent traffic; (3) `DEFAULT_EXCLUDE_TOOLS` bypasses fresh Read/Grep/Bash outputs → realistic live cut ≪ 63%; source-code is 0%. **Idea-only kept:** live-zone+cache-floor + down-sample-with-anchors, rebuilt v2-native (deterministic, secret-fenced, Windows-safe, CCR-recoverable) ONLY if token spend ever justifies it. Gotcha → F-049. Foundry decision captured. | auto |
| (spec) | BL-H2 | **headroom-eval (bounded spike) — original note** | After headroom-digest. The ADOPT decision: stand up `headroom proxy` (or the npm `headroom-ai` TS lib / MCP) in front of ONE driven code-write session, MEASURE real token reduction on OUR traffic, and produce a FIT report against D-002 isolation (must not break the isolated config / `--settings` / gates / determinism) + D-026 (it caches+rewrites tool outputs that may hold secrets → must sit BEHIND our screen/fence, secret-safe) + CONTAINMENT (third-party proxy over our LLM traffic — contain it, never pipe raw secrets). Adopt ONLY the compression (NOT headroom's learn/cross-agent-memory — we have error-learning + MEMORY-SPEC + skill-harvest). CAVEAT: a true on-our-workload token number wants a live CREDENTIALED driven session (operator env). Output = adopt/no-adopt with real numbers. |
| BL-1 | **Image-generation addition** | Operator-referenced as already backlogged (2026-06-11) but no prior written record found in queue/docs/memory — recorded here so it exists. Scope unspec'd: likely a capability/adapter for generating images (mod icons, store assets, UI art?) consumable by projects + Create-with-AI. Needs a spec when promoted. |
| BL-2b | **API reference / client (scalar) — project-serving, only for API projects** | Operator-evaluated (2026-06-25): scalar/scalar (MIT, TS/Vue, OpenAPI references + REST client). **NOT for Atelier itself** (Atelier is a loopback control plane, not an API product; active project ROUNDS is a C#/BepInEx mod — no API; stack is Svelte not Vue). Recorded ONLY as a future PROJECT-SERVING capability in the BL-1/BL-2 family: IF/when Atelier manages an API or web-service project, auto-generate a scalar API reference as a deliverable FOR that project. No digest, no spike now (relevance-gated — don't ingest irrelevant tooling into the brain). Promote only when a real API project exists. |
| BL-2 | **Marketing features** | Operator (2026-06-11): hook in marketing features for maintained projects that need them — "a massive lift to even consider." Ordered explicitly AFTER BL-1. Natural shape when its time comes: a D-037-style adapter/capability family (channels as adapters, content as artifacts, PM proposes campaigns through D-039 validation) + likely new roles (copywriter?) via WORKFORCE-SPEC. Needs a full spec + deliberation when promoted; do NOT scope-creep it into earlier waves. |
| done | peer-message-bus (G-B / BL-4) | **✅ DONE + HARDENED + PUSHED (2026-06-15, opus, origin/v2 tip `1a860c9`).** Hardening wave wf_d0910cf9-7de drained both MEDIUMs: H1 `d598de3` — D-026 live-leg one-chokepoint screen (delivered turn byte-identical to the persisted screen→fence envelope; secret redacted in delivery+row+transcript) + cross-project drain re-assertion (cross-project pending → EXPIRED, never delivered); H2 `1a860c9` — atomic per-session send budget + sender-namespaced dedup + named IdempotencyError. Both review+red-team PASS. **NEW deferred MEDIUM (NOT chased — operator hold 2026-06-15): concurrent double-deliver — the drain `WHERE status='pending'` CAS is unreliable on the app's SHARED singleton WS connection (runtime-init.ts), and send.ts markDelivered MERGE is unguarded → a rare race yields a DUPLICATE transcript turn / body folded twice into a briefing. NOT a leak/steer/secret-exposure (fenced DATA, origin=agent); pre-existing from PM3 `9659746`. Fix later: a connection-safe CAS (separate connection / retryable-conflict handling) or a unique-delivery guard.** Known LOW placeholder: pm/atelier addressing inert until session.pm seam / D-040. **Original core ↓.** CORE (3 tasks PASS, tip `9659746`, run wf_527de9ea-5fa): PM1 `6e429d3` inbox+resolution+policy · PM2 `94cfddd` send path · PM3 `9659746` drain+delivery+transcript. PM1 `6e429d3` durable peer_message inbox (m0039) + 4-class address resolution + cross-project-forbidden policy · PM2 `94cfddd`+fix `22e5ffc` peer-send capability + gated MCP tool + authenticated send route (sender resolved server-side, screen+fence, bounds) · PM3 `9659746` offline drain-at-spawn + live delivery + TTL/hops + transcript (free via G-A). **HARDENING wave wf_d0910cf9-7de running (drains the ledger): 2 MEDIUM — (1) D-026 screen bypass on the LIVE delivery leg (offline path screened, live path passed raw to interject→fence-only) · (2) dedup_key globally-unique DoS + raw-500 collision → namespace-by-sender + named IdempotencyError; + LOW budget-race + delivery-time project re-check.** Known LOW placeholder: pm/atelier addressing inert until the session.pm seam / D-040 (today such messages can only expire). **Original spec note ↓.** SPEC SIGNED OFF (D1–D5) → `docs/PEER-MESSAGE-SPEC.md`. Operator decisions: D1 = 4 address classes (session/role@project/pm@project/atelier) · D2 = DURABLE inbox · D3 = in-project mesh + PM hub, cross-project ONLY via the atelier (project↔project forbidden) · D4 = build NOW (before ceremony finish) · D5 = full v1 scope. §11 atelier = platform agent on the global-memory brain (≈ atelier_self PM + brain until D-040), origin=agent/non-steering (orchestrates via the gated control plane, not message-steering). 3 tasks: PM1 peer_message table + address resolution + policy · PM2 (red-teamed) peer-send capability + gated MCP tool + authenticated send route (sender resolved server-side, screen+fence, bounds) · PM3 live + durable offline-drain-at-spawn + TTL + transcript (free via G-A). **Original spec-draft note ↓.** SPEC DRAFTED 2026-06-15 → `docs/PEER-MESSAGE-SPEC.md`. Locked invariants inherited (D-035a non-steering, D-026 screen, capability-gated fail-closed, transcript-visible via G-A). Builds on 4 EXISTING seams (channel/bus/capability-tool/fence); must close 4 GAPS (recipient addressing, OFFLINE delivery [critical], authorization-topology, loop/spam bound). Open decisions: D1 addressing · D2 offline=durable-inbox-vs-running-only · D3 topology (recommend project-mesh + PM-hub) · D4 timing (recommend after ceremony, with new-mod-test as first consumer) · D5 v1 scope. Original raise ↓. Operator-raised (2026-06-15): agents sharing findings/communications. The primitive is DESIGNED — `channel.pushToSession` (D-035b) explicitly anticipates "the v0.2 peer_message fleet bus lands as an additive consumer of this same seam" — but it is UNBUILT (one comment, no consumer). Today inter-agent interaction is artifact/file handoff (orchestrator dispatch · PM proposal→`panel_verdict` · gauntlet `findings.json`→scorer), not conversation. Build the peer_message consumer of the channel seam so agents can message each other, STRICTLY under D-035a: `origin=agent` stamped server-side, fenced as DATA (MEMORY-SPEC §10, same fence as recalled memory), NON-STEERING — only an operator-token message steers; making peer comms visible must never become an injection vector. Pairs with G-A (origin-visible transcript) — peer messages then surface as origin-labeled communication turns. **Spec-first** (the binding rule + the bus shape + when an agent is permitted to message whom) before building. Until G-B lands, "agent communications" in a transcript = operator interjects + artifact rows only. |
| done | global-transcript (G-C / BL-5) | **✅ DONE + PUSHED (2026-06-16, opus, origin/v2 tip `fee89fd`, run wf_680ce259-25d — 2 tasks PASS).** GC1 `f98af2b` — `/atelier` merged timeline (message/peer_message/panel_verdict/role_event by timestamp; scope global/per-project; time-windowed + paginated [page 50, `before` cursor]; honest PARTIAL on a failing source; verdict/role_event TurnKinds added to the shared renderer; nav entry under KNOWLEDGE & SYSTEM; live SSE). GC2 `fee89fd` — the §5b INBOX LENS: per-agent + fleet peer_message by status (pending/delivered/expired/quarantined), from→to identity, screened body verbatim, hops/age, full-scope count badges, honest D-040 'awaiting recipient identity' for pm/atelier. Both review PASS, live-verified (both lenses drove clean). **1 LOW deferred (NOT chased, G3): timeline page cursor is per-source strict `at<before` while merge tiebreaks by id — identical-sub-ms-timestamp entries straddling a page boundary could drop; latent (0 skips in tests/live); fix = (at,id) compound cursor.** Original ↓. SPEC DRAFTED → `docs/GLOBAL-TRANSCRIPT-SPEC.md`. Operator-raised (2026-06-15): a unified transcript of the atelier's reasoning + actions + communications, not just per-session. Today there is per-session transcript (`/claude-code?session=`), a fleet LIST (status only), and `agent_event` (analytics) — no cross-agent timeline. Build an aggregate timeline (atelier-wide or per-project) merging session turns + channel comms (G-A origins) + PM proposals/`panel_verdict`s, reusing the shared `SessionTranscript` renderer + the `message`/`agent_event`/`panel_verdict` substrate. Depends on G-A (origins) and is most MEANINGFUL after G-B (peer messages) — until then it aggregates single-agent reasoning + operator comms + artifacts. Honest empties, D-026 screened, bounded/paginated (an atelier-wide stream can be large). Spec the scope (global vs per-project, time window, what event classes) before building. |
| done | cannibalize-in-product (BL-6) | **✅ DONE + PUSHED (2026-06-16, opus, origin/v2 tip `f241d8b`).** Resumed via fresh wave wf_2afd6ab9-a34 (CB2+CB3/UI; CB1 already on disk, dropped to avoid migration/table redo). **CB1** `3439fe2` — ingest_source table + provenance/applied_count on memory write + SSRF-safe capture layer (red-team caught+fixed an IPv4-mapped-IPv6 bypass). **CB2** `907f042` — distill→screen→fence→embed→ingest pipeline writes THROUGH the existing memory store (NO parallel datastore) carrying provenance+license; honest distilling→ingesting→done|quarantined|failed; utilization `markIngestedFindingsApplied` bumps applied_count via recordOutcomes/recordTurnOutcomes (RANKING only, never prunes G3); m0044_memory_license additive OVERWRITE. Review PASS + **red-team PASS** (load-bearing untrusted boundary: planted private-key quarantined-not-ingested, fence sentinels un-escapable on recall, provenance server-side un-forgeable). **CB4/UI** `f241d8b` — `/cannibalize` front door (NL intent + URL/repo/file/text inputs → live pipeline stage progress via onDbChange SSE on ingest_source → provenanced results), tokens/a11y, honest states, sidebar KNOWLEDGE & SYSTEM. Review PASS + **live-verified**. **Deferred ledger → CB-H1 hardening wave DONE+PUSHED (run wf_dc8a1ebb-ce3, opus, origin/v2 tip `41254a9`):** fixed the real MEDIUM — distinct neutral `dropped` terminal (m0045 additive OVERWRITE widens status ASSERT; traced through every +page.svelte consumer: label "Nothing kept", non-error muted tag/stage color) so an all-noise run no longer shows the security `quarantined` badge. **CB-H1's OWN red-team caught a deeper instance** (a screened secret that then fails embed/insert could route to `dropped` → false "no secret detected") → in-script fix-loop closed it (`41254a9`: `isWriteFailureReason` routes write-failure count-0 runs to `failed`, never `dropped`); re-review PASS + live-verified (done=green / quarantined=red / dropped=neutral render distinct, no false security denial). **Residual 1 LOW (G3, not chased):** +page.server.ts:6 status-enum doc-comment omits `dropped`/`failed`-routing (prose-only, no consumer). Earlier CB2 LOWs (ingest.ts:197 dropped-intent '', no per-finding output byte cap, +page.svelte:319 TODO footnote) still advisory → fold when CB5 utilization-stats lands. Spec: `docs/CANNIBALIZE-SPEC.md`. | Original ↓. Original ↓. Operator-raised (2026-06-15): productize the dev `cannibalize` foundry (`F:\code\cannibalize` — capture/embed/ingest/find-relevant/**mark-applied**) as a FIRST-CLASS product capability that grows the atelier's ecosystem. Shape: a **UI front door** (page/panel) where the operator gives **natural-language intent + inputs** (links/repos/URLs/files/pasted text) → the atelier runs the foundry-style pipeline: **capture** (fetch content) → **distill/extract** findings/patterns → **embed** → **ingest into the brain** (global memory, MEMORY-SPEC) → consumable by ALL projects (recall) + atelier self-improvement (§11). **Close the utilization loop** the foundry's `mark-applied.ps1` models (CANNIBALIZE-BRIEF §1 calls it "v2's winning move — all three [hermes/mem0/kongcode] under-use it"): track whether ingested knowledge later helped → feed BOTH ranking AND curator keep/prune. SECURITY (load-bearing): ingested external content is UNTRUSTED → `screen()` (D-026) + `fence()` (DATA, MEMORY-SPEC §10) before it enters the brain — a malicious doc/repo can NEVER inject instructions (same fence as recalled memory); record PROVENANCE per finding (the foundry's source tags + AGENTS.md provenance discipline; respect license — kongcode-style "ideas OK, lifting code needs consent"). Capability-gated (the atelier/operator drives it; later the atelier ingests autonomously as it discovers relevant content — D-040). This is a core **atelier identity** capability (§11 PEER-MESSAGE-SPEC: the layer that "ingests content into its ecosystem"). **Spec-first** (front-door UX, pipeline stages, the utilization-loop data model, provenance/license model) before build. Reference design: `F:\code\cannibalize` + `docs/CANNIBALIZE-BRIEF.md`. |
| BL-7 | **Memory-utilization hardening — close the loop to BEAT kongbrain/hermes** | **PART A ✅ DONE + PUSHED (2026-06-16, opus, origin/v2 tip `140ebb6`, run wf_1ab499db-eb9 — build+review+red-team PASS).** `140ebb6`: strengthened use-and-cite REPORTING directive on the single shared §10 FENCE_NOTE chokepoint (fence.ts) — "when an item below INFORMS your work cite it `[#N]`; pull more via the memory tool; citing REPORTS usefulness, NOT an instruction to follow." **D-026/D-035a held under red-team** (embedded SYSTEM directive + fake close-sentinel stays inert: stripEmbeddedSentinels collapses to one OPEN/CLOSE, payload inside fence, origin stays non-steering [recall]). B8 cite-signal probe added (runCiteSignalProbe): measured lift cite-directive coverage 0→1 over 40 items, [#N] parse rate 1.0 through the shared parseCitations() grammar — **HONESTLY BOUNDED as PROMPT-SIDE signal strength + parse-path integrity, NOT a model-behaviour citation-rate gain** (no live model in the LexicalEmbedder harness). No new schema. **Deferred (NOT auto-chainable — needs live infra, pairs with the live-proof/ceremony track): 1 MEDIUM** — true model-behaviour citation-rate proof (run the strengthened directive against a live qwen3/Agent-SDK path to confirm agents actually emit MORE [#N] — the real D-030 signal gain stays unproven until a live model run). **LOWs (G3, advisory):** B8 ranker-discrimination needs an outcome-bearing/age-spread corpus (weights score identically on the flat corpus); red-team flagged citeIdParseRate is a near-tautological recall-id↔grammar shape check (comment slightly oversells); single-chokepoint directive lands on all fenced sources not just recall (harmless, consult-not-obey preserved). **PART B (B = activate fast tier) ✅ DONE + PUSHED (2026-06-16, opus, origin/v2 tip `50cc3c8`, run wf_b4b21cbd-9d1 — build+review+red-team PASS, live-verified).** `50cc3c8`: wired the dead D-027 fast in-use writer fork at BOTH ends — ENQUEUE hook in launch.ts on the eventToMessage per-turn seam (bumpCounters→dueReview→screen(turnText, D-026)→enqueueReview, best-effort/non-blocking, gated memory.fastTier!==false, per-session dedup coalesces rapid turns to ONE pending review) + DRAIN dispatch in orchestrator.ts #runItem (work_type==='memory_review'→#runReviewItem→runReviewFork via the memory dep + makeWriteSurface, bounded by the EXISTING D-021 cap/claim/stale-GC, fork-failure best-effort never crashes drain, honest-fail on missing dep). ADD-only (fork holds only MemoryWriteSurface — forgets nothing); interview-exclusion holds (engine-level, loop.ts:112); no migration. Red-team PASS (secret-in-turn screened-not-raw, interview-exclusion unbypassable, D-021 throttle holds, ADD-only proven). **MATERIAL (live): a review-LLM fork now fires ~every 5 turns / 10 tools — recurring spend, bounded by the D-021 cap.** **Deferred LOWs (G3): tool_use→toolIters cadence axis + skill-kind path integration-thin; transcriptParts capped at 400 → late-session mines frozen tail; commit-msg test-count miscount (says 21, adds 14 — coverage real).** **↪ BL-8-H1 fix wave DONE+PUSHED (run wf_b0186afc-84d, opus, origin/v2 tip `80abc00`, build+review+red-team PASS): the red test caught a REAL D-026 leak — observability.ts `loadMemoryContent` (utilization-lens content read) had NO quarantine filter → a quarantined row's body could surface on the leaderboard. Fixed (`SELECT … WHERE screen_status != "quarantined"` + display-screen survivors), catalogued LEAK in the census (green for the right reason), red-team test plants a quarantined row + asserts content '' / no sentinel leak. F-036 logged (cross-cutting guard not in the touched-area slice, F-014 family). NOT cosmetic — the census earned its keep.** **C = SLOW curator tier PARKED** (§5.3: D-028 conflict pass, D-031 consolidator, D-015 time-based GC, + operator-confirm soft-delete). **Earlier framing ↓ — Part B was RE-SCOPED ON VERIFICATION (2026-06-16) → `docs/MEMORY-UTILIZATION-SPEC.md` §5. The original "outcome→keep/prune (low-util+age)" model was REJECTED** — a file:line verification pass found it contradicted locked **D-030** (outcome→ranking ONLY, NOT pruning) + **D-015** (pruning is time-based), and it wasn't the real gap. **VERIFIED real gap: the D-027 FAST in-use writer fork is DEAD on both ends** — `bumpCounters`/`dueReview`/`enqueueReview` have zero non-test callers (enqueue side) AND `#runItem` (orchestrator.ts:363-371) only handles taskId/projectId so a `memory_review` work_item never executes; `runReviewFork` has zero callers (drain side). Session-END whole-session extract (TASK 8.3) IS wired; the per-turn fast tier is the disconnected piece. **Operator chose B+C (2026-06-16):** **B = activate the D-027 fast tier NOW** (wire enqueue hook on the live per-turn seam + `#runItem` `memory_review` dispatch→`runReviewFork`; ADD-only/forgets-nothing; screened turnText; interview-excluded; bounded by the D-021 cap; MATERIAL: fires a review-LLM fork every ~5 turns = recurring bounded spend) — §5.2/5.2a, red-team flagged (self-reinjection seam). **C = PARK the SLOW curator tier** (D-028 conflict pass, D-031 consolidator member-selection, D-015 time-based GC, + the operator-confirm soft-delete layer) — recall+ranking carry it meanwhile; spec as its own item when un-parked (§5.3). Original ↓. Operator-raised (2026-06-15): make brain utilization decisively better than kongbrain/hermes/mem0. We ALREADY have both channels (recall injection + B10 pull), Tier-0 directives, the fence(§10)+screen(D-026) safety layer the others lack, and the D-030 retrieval-outcome loop (citations [#N] → recordOutcomes/recordTurnOutcomes → RANKER). Two gaps to close: **(1) Citation-signal strength** — the outcome loop depends on the agent emitting [#N] when it uses a recalled memory, but the fence standing note frames recall softly ("DATA you MAY consult") with no strong use-and-cite directive → weak/under-cited signal. Strengthen the agent-facing instruction ('cite [#N] when you lean on a recalled item; use the pull tool when you need more') WITHOUT breaking consult-not-obey (D-026). Measure the lift via B8 (recall-eval harness). **(2) Outcome → keep/prune** — D-030 feeds RANKING only, deliberately NOT the curator's keep/prune (conservative). CANNIBALIZE-BRIEF §1 wanted outcome to feed BOTH; evaluate letting usefulness influence curator keep/prune CAREFULLY (G2/G3 — operator retires; never auto-delete on a noisy signal). Pairs with B8 (measure) + BL-6 (the ingested-content utilization loop). Spec-first (the cite directive wording + the keep/prune safety model). |
| BL-3 | **Project capability-needs + PM role-matching** | **SPEC DRAFTED 2026-06-15 → `docs/CAPABILITY-MATCH-SPEC.md` (defaults baked, build-ready — builds WITH wave-v2.3 §6/§7; ceremony-gated for real track data).** Original ↓. Operator-raised (2026-06-15): a new project needs roles specific to its language/domain (e.g. C#/BepInEx/Unity for a ROUNDS mod) that differ from this project's; reusable across projects when applicable; **the PM should determine whether existing certified roles fill the need or a new hire is required.** The mechanism is already specced — roles are global catalog entities, projects opt-IN via `project_staff` (§6), PM proposes staffing through §4 + operator gate (D-039), and the gauntlet is the objective fit-proof (a role only "covers" the defect classes its fixtures certify, §3.8). The GAP this records: (1) a project should declare **structured capability-needs** (language/frameworks/defect-classes) so the PM matches against data not prose — projects today have targets/config but no needs declaration; the Create-with-AI §2.1 reference-repo hint covers part of it; (2) the **PM matching logic** itself (score catalog roles' proven coverage vs needs → reuse / extend-and-re-certify a generic role on added domain fixtures / new specialized hire). Folded into **wave-v2.3 §6/§7** (above) and exercised by **wave-create-ai → new-mod-test** (the new ROUNDS mod is the first real case: PM surveys the catalog, reuses generic roles, proposes a C#/ROUNDS hire for the gap). Spec the needs-declaration shape + matching heuristic before building §6/§7. |
| done | observability: brain-observability (BL-8) | **✅ DONE + PUSHED (2026-06-16, opus, origin/v2 tip `32e7e76`, run wf_09e05abb-493).** `3dee6b8`: read-only `observability.ts` listers (listMemoryHistory / listSkills / listCausalChains[ByIds] / listRetrievalOutcomes + a `screenForDisplay` D-026 chokepoint) → three Memory-cluster lenses `/memory/{history,skills,outcomes}` + shared MemoryTabs; recalled-but-never-cited filter derived from real counts (no faked score); watched-tables gained memory_history/skill/causal_chain/retrieval_outcome for live SSE. Review PASS + **red-team caught a NEW in-scope MEDIUM** (the queue `?before=` cursor guard validated ISO *shape* not *validity* → a calendar-invalid date reached the `<datetime>` cast and threw → a HEALTHY DB falsely painted 'disconnected' + raw SurrealDB error leaked to UI, F-008) → fix-loop closed it (`1e68c9d`: `_validCursor` NaN-drops invalid dates), re-review PASS + **live-verified** the previously-failing URL now 200s clean. READ-ONLY proven (grep: zero CREATE/UPDATE/DELETE/MERGE in new code+loaders). LOWs advisory (G3): listRetrievalOutcomes unbounded GROUP-BY screens distinct-memory ids before slice (fine at scale; leaderboard needs global sort). SPEC → `docs/BRAIN-OBSERVABILITY-SPEC.md`. |
| done | observability: work-queue monitor (BL-9) | **✅ DONE + PUSHED (2026-06-16, opus, origin/v2 tip `32e7e76`, same wave).** `a840949` (+ core in `3dee6b8`): read-only `queue-monitor.ts` (listWorkItems/getWorkItem/queueStats reusing workqueue.ts countByStatus/pendingDepth/spawnsSince; STALE count = a SELECT mirroring the gcStale threshold, **reaper NEVER invoked from a loader**) → `/atelier/queue` linked from /atelier; watched-tables gained work_item. Review PASS + read-only-invariant test (queueStats/listWorkItems mutate nothing even over GC-eligible rows). **Test-precision bug fixed + logged F-035** (`32e7e76`: ms-truncated getTime() comparison collapsed SurrealDB's 100ns datetime ordering → false pagination-test fail; compare ISO strings — sibling of F-013). **Deferred MEDIUM → BL-9-H1 hardening wave DONE+PUSHED (run wf_ac16f134-64d, opus, origin/v2 tip `8110441`):** removed the fabricated DEFAULT_DAILY_CAP=50 literal — traced orchestrator.ts (opt-in `dailySpawnCap>0` gate) + boot.ts (wires NONE) + config (no cap key) → live system is genuinely UNCAPPED; queueStats now reports `capped:false`, no denominator, throttled always false when uncapped; UI renders "{n} spawned today · no cap enforced". Build+review+**red-team PASS** (red-team also stress-tested the adjacent `?before=` cursor cast — JS-extreme dates don't throw → no false-disconnect; F-008 holds), live-verified. **1 LOW (G3, advisory):** if an operator later wires a real dailySpawnCap into boot.ts they must also thread it into queueStats({dailyCap}) in the loader (correct today since both are uncapped; queueStats already supports the param). SPEC → `docs/WORK-QUEUE-MONITOR-SPEC.md`. |
| done | create-live-thinking-replay | **LOW UX follow-up (deferred from wf_a61766c4-d03).** A reload MID-generation does not replay the generation session's historical transcript turns — the live pane shows "Waiting for the first turn…" until the next SSE turn arrives (honest, F-008, but incomplete-on-reload). Fix: hydrate the loader with `listSessionMessages(run.session)` so prior turns render instantly on a mid-generation reload. + 2 LOW: done→review happy path not yet live-driven (needs a real successful generation = real OAuth — wiring proven by unit tests + the identical onDbChange/invalidate SSE the failed-flip was driven through); persisted briefing row renders as 'system message' not a 'briefing' turn (pre-existing, shared with /claude-code, cosmetic). | auto |
| done | live-status-badge | **MEDIUM UI follow-up (deferred from wf_a1662fb4-bd3).** live-query-reconnect wired the server-side honest `live_status` (reconnecting/disconnected) + a reactive client `sse.tableLiveness(table)` map, but NO .svelte region renders it yet. Core F-008 already holds (db_change stops + lastEventAt staleness applies — no silent stale-as-live), but surface a visible 'live disconnected/reconnecting' badge on the live regions (home/projects/workflows tickers). + LOW: give the 5-value union an exhaustive typed render-map when the badge lands. | auto |
| done | file-snapshot-harden | **Deferred from wf_5babea6c-6ab.** [MEDIUM] captureSnapshot dedup is NOT concurrency-safe — SELECT-then-CREATE with no UNIQUE guard produced 4 rows from 4 concurrent identical captures (defeats spec §2 "stores ONCE"; now reachable since FS-2 wired concurrent capture: scaffold writes many files, transcript turns). Fix concurrency-safely: a (content_sha,path,project) dedup_key + catch-on-conflict UPSERT. NOTE F-026: on this SurrealDB build a UNIQUE *secondary* index did NOT enforce under concurrency (gauntlet_key_dedup) → that needed a DETERMINISTIC PRIMARY-KEY id; the red-team cites embedding_cache_hash UNIQUE as working — so the fix MUST include a real 4-concurrent→1-row test to prove whichever approach actually enforces on this build. [LOW] validate captured_by ref shape (file-snapshot.ts:328, free-form today); [LOW] composite dedup index for scale; [LOW] add file_snapshot to WATCHED_TABLES if the view-file surface live-updates. | auto |
| done | memory-scene-data | **The living brain — DATA foundation (§7.1-2 of MEMORY-SCENE-SPEC). Forks locked (recommended): scope v1=memory+jobs, retention=rolling, surface=/memory lens.** MS-1 scene_event derived append-only rolling feed + projection writer (reuse the SSE trigger surface) + WATCHED_TABLES; MS-2 live nodes/edges aggregator (memory+jobs, read-only, derived, honest empties). F-008: derived-not-fabricated, node/edge truth derives live (no denormalized copy). The animated viz (§7.3-4: motion.dev force-graph + /memory Scene lens) is the next wave memory-scene-ui. | auto |
| done | memory-scene-ui | **The living brain — VIZ (§7.3-4). The animated Obsidian-bubble force graph.** MS-3 the force-graph component (motion.dev IMPERATIVE core — NOT React; force layout + spring node-spawn/job-pulse/edge-draw-in/retire-fade; SSE-fed from MS-1 scene_event + MS-2 aggregator; reduced-motion safe; 60fps transform-only). MS-4 the Scene LENS on /memory + node-class styling (lifecycle-colored) + activity feed. Adds the `motion` dep. Completes memory-scene v1 (memory+jobs); hires/PMs/ateliers layer next (fork 2). | auto |
