# HANDOFF — 2026-07-07 (resume without regression)

Everything a fresh session needs to continue Atelier work. Read this + `CLAUDE.md` +
`docs/SPEC-COVERAGE.md` first.

## Model policy (NEW — read first)
Operator directive 2026-07-07 ([[feedback_model-roles]], CLAUDE.md §2.5):
- **Fable 5 (`claude-fable-5`) → planning / speccing / creating workflows.**
- **Opus (`claude-opus-4-8`) → building / coding / research / large-context.**
The planning session runs on fable-5 and **delegates all coding/research/large-context to opus**
(subagents/waves). Supersedes the retired "opus-everywhere" note.

## Branches / where things live
- **Code**: branch `v2`, worktree `F:\code\ai-playground-v2` (app at repo ROOT — `src/`).
- **Docs / CLAUDE.md / workflows / this file**: branch `v2-main`, `F:\code\ai-playground`.
- Push: docs → `origin/v2-main`; code → `origin/v2`. Both are ahead-0 as of this handoff (all pushed).
- Gates run with `CLAUDE_CODE_OAUTH_TOKEN` unset (F-029), in the v2 worktree.

## What shipped this session (all committed + pushed)
**v2-main (docs):**
- `CLAUDE.md` rewritten as the Atelier v2 operating manual (`9dbd006`) + §2.5 Model roles (this turn).
- 3 skills: `atelier-data-layer`, `atelier-live-verify`, `atelier-wave` (`04fd5a2`).
- Decision **D-041** (per-hire soul + comms topology) in `DECISIONS.md`.
- 8 specs: PER-HIRE-SOUL, GAME-VERIFY-IN-LOOP, MEMORY-CURATOR, CONCIERGE-BROKER, RELEASE, MAINTAIN, + SPEC-COVERAGE tracker (this handoff's sibling).
- Devlog `2026-07-07-operating-manual-recovery-harden.md`; BUILD-QUEUE rows for every spec (all gate:operator).
**v2 (code):**
- **Wave A `recovery-repo-harden` — 4/4 green, pushed (`9d1019b`)**: RRH-1 graph-UI DoD (no-op, already fixed), RRH-2 BL-RC-FIX/F-050 main-everywhere (`8d1ff08`), RRH-3 BL-R3 dedup active-window (`4e8698b`), RRH-4 BL-R4 operator `failed→ready` (`9d1019b`). 4 LOW deferred (advisory).

## In-flight / where to pick up
1. **Spec campaign ("spec everything")** — backbone = `docs/SPEC-COVERAGE.md` (15 FULL / 21 PARTIAL / 3 NONE). Next specs, ranked: **orchestrator/work-queue → projects → claude-code-harness → adapter-framework → db-runtime → cc-config → scanner/sync/services/auth**. Plus held: **COST-GOVERNANCE**, **SECURITY-MODEL** (both P1). Method: fable-5 authors each spec; **opus subagent scouts the seams first** (no-guessing, file:line), then write. Commit+push to v2-main, queue a BUILD-QUEUE row.
2. **graphify** (codebase → knowledge graph, accelerates spec grounding) — INSTALLED (`graphify` CLI, 0.9.9, + `graphify-mcp`). AST code graph of `src/` built (651 files). **Graph BUILT** (`graphifyy[ollama]` extra installed — F-049): `F:\code\ai-playground-graph\graphify-out\graph.json` = **5,862 nodes / 19,374 edges / 187 communities**, whole `src/`, $0 (ollama local, private). Named-community `GRAPH_REPORT.md` + `graph.html` via `graphify cluster-only`. Query with `graphify query "..."`, `graphify explain "X"`, `graphify affected "X"` — use these to ground specs instead of a fresh scout. **Full-depth `--mode deep` pass** (aggressive INFERRED edges) still optional: `graphify extract "F:/code/ai-playground-v2/src" --mode deep --backend ollama --model gpt-oss:20b --max-concurrency 1 --out F:/code/ai-playground-graph` ($0/local) or add an `ANTHROPIC_API_KEY` for the opus deep pass. ollama is UP.
3. **new-mod-test (the graduation milestone, gate:operator)** — create a FRESH ROUNDS mod via Create-with-AI (CardDrawControl is spent — built manually this session), feed `refRepoUrl` → `F:\code\CardDrawControl` / `F:\code\UnboundLib` as prior art. Full lifecycle → first real publish (D-037-gated). Floor is hardened (Wave A). Operator-driven live-product flow.

## Don't-regress rules (the load-bearing ones)
- **Model split** (above) — plan on fable-5, build/research on opus.
- **Publish/external actions operator-gated** (D-037), even under autonomous-to-v1. Real-spend needs recorded consent.
- **Cross-project wall stays** (D-041/D-035a): project↔project direct forbidden; `atelier` = sole cross-project broker; comms are DATA, only operator steers.
- **Data layer**: idempotent migrations (F-015), ORDER BY in SELECT (F-020), datetime→ISO norms (F-013), real-surreal tests not stubDb — see `atelier-data-layer` skill.
- **v2-wave**: sentinel `BLOCKED:`/`CONFLICT:` stop markers (F-019); load-time syntax traps (F-016/056); StructuredOutput-crash recovery = check git log + push, don't re-run (F-051).
- One builder per worktree (F-052); gates token-unset (F-029).

## Environment state
- ollama UP (gpt-oss:20b, qwen3:30b, qwen3-embedding:0.6b pulled). Dev server DOWN (bring up with `npm run db:up` + `npm run dev` in the v2 worktree when needed).
- graphify output dir `F:\code\ai-playground-graph` (outside both worktrees). Stray `graphify-out/` dirs cleaned from the v2 worktree.
- No uncommitted code on v2. v2-main has this handoff + SPEC-COVERAGE + CLAUDE.md §2.5 pending commit (this turn).

## Auto-loaded context (already available next session)
`MEMORY.md` + memories load automatically — incl. `feedback_model-roles`, `feedback_cost-awareness` (superseded note), the full Atelier project memories. `CLAUDE.md` + `docs/fails.md` load via project instructions.
