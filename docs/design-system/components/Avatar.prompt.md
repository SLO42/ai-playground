Round operator avatar (initials or image) or a square, tier-tinted agent marker for the fleet view.

```jsx
<Avatar name="Operator" />
<Avatar tier="opus" />     {/* gold square */}
<Avatar tier="local" size="sm" />
```

Pass `tier` to switch into the agent marker (rounded square, tinted by tier ramp).
