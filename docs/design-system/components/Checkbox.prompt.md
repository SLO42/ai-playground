Controlled checkbox with inline label — filter lists, multi-select table rows, settings toggles that aren't on/off switches.

```jsx
<Checkbox checked={onlyBlocked} onChange={setOnlyBlocked} label="Only blocked" />
```

Keyboard operable (Space), `role="checkbox"`.
