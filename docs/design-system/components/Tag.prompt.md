Monospace keyword chip — ecosystem detection badges, active filters, MCP/skill/agent ids.

```jsx
<Tag>typescript</Tag>
<Tag>neoforge</Tag>
<Tag onRemove={() => clear('blocked')}>status:blocked</Tag>
```

Add `onRemove` for filter chips. Use `icon` for a leading glyph.
