# Atelier Devlog

A durable, cloud-followable log of what shipped each working session — a second
source of truth alongside git history + `docs/` specs, so future revisions
(far beyond any one session) can follow the "why" and the sequence, not just diffs.

**Convention:** one file per session, `YYYY-MM-DD[-slug].md`, newest first below.
Each entry records: what shipped (with commit hashes), migrations, decisions made,
bugs caught / fails logged, and what's parked/next. Written at session close;
committed to `v2-main` (docs branch). Code lands on `v2`.

## Sessions

- [2026-08-05/06 — Claims that read as complete, and a record id that was never table-scoped](2026-08-06-claims-that-read-as-complete.md) — PAUSE #7 lifted. One spine ran through the stretch: **a claim that reads as finished and is not.** V1R-1 was closed by asserting the CLASS after two failed reviews — its guard had been fail-open by a rule the same commit authored (`if (frozen.has(file)) continue;`), skipping the seven files most likely to hold the defect and reporting green; instance five sat inside that skip. **F-062**: a request-supplied record id validated by SHAPE ONLY reached bare `UPDATE $rid`/`DELETE $rid` — seven instances, `removeTarget(db,'project:<slug>')` **deleted the project row** and answered `200 {ok:true}`, and the last two were found by the red-team *on the page the first fix had just hardened*; promoted to a CLAUDE.md hard rule. The `fails.md` fork was measured read-only (`docs/FAILS-DIVERGENCE.md`): **four ids name different failures** depending on the copy, ~240 code citations are ambiguous, and both numbering systems are MIXED inside one worktree. Worst of all was not a collision but **F-019 silently TRUNCATED** in the fork — missing the recurrence bullet that escalates the stop-marker rule to a sentinel prefix, so it taught the superseded rule while reading as complete. Damage settled: no wave was ever stopped by it — the host is `^`-anchored and exists only on `v2-main`. F-060's `.ts` residual measured **clear** (recommend leaving `.gitattributes` alone); F-061 logged **and then corrected**. No migration; head stays `m0087`. **Lane C merged into `v2` (`7d88a87`) but the merge is 28 commits ahead and NOT pushed, re-gate in flight.**
- [2026-08-04/05 — Atelier starts gating its own work, and a sha stops determining the bytes](2026-08-05-the-self-gate-and-the-sha-that-lied.md) — the pre-commit gate SHIPPED (`4ba14c1`): build/lint/typecheck/test now run BEFORE the terminal transition, and a red gate preserves the branch instead of fast-forwarding it — closing the only correctness finding of the 2026-07-26 review. It then caught its own class six times, incl. a post-task fault that merged **around** an armed gate and tore the worktree down (F-055 + F-007 in one code path) and an ENOENT wedge that blamed the change for a missing SDK. TASK-BOARD P1/P2/P3 shipped with **m0087** (`task.tags`, live db:up 87/87) — three review catches: a forged brief section (D-026), a cross-table `UPDATE $rid MERGE`, an O(n²) dedup over unbounded input. Lane C: the hiring-honesty cluster (the stranded paid run; a header counting 36 over 17 rows), a reconcile TOCTOU closed with a compare-and-swap **race-tested from inside `db.query`**, and **F-060** — `core.autocrlf` means a sha does not determine the bytes on disk; 53 tests had been contributing nothing while the reporter called them "failing files". Plus COUNCIL-SPEC, whose research contradicted the obvious debate design. Lane A pushed (`b4f58f6`); **lane C 4 commits unpushed on a twice-failed tip, never merged.**
- [2026-07-26/27 — Two lanes to completion, and the suite goes honestly green](2026-07-27-parallel-lanes-and-a-green-suite.md) — both build lanes finished their task lists (lane B `e25738b` pushed; lane A 22 commits UNPUSHED). `npm test` on `v2` honestly green for the first time (331 files / 5712 passed / exit 0) via three root causes — a module-scope `bootstrap()` that booted the real server inside tests, ~23 concurrent SurrealDB forks, and an unretried optimistic-commit conflict. The terminality defect class attacked at five depths after a SIXTH fabricating surface was found; a guard test that could not fail (`String.match` without `/g` vs `OVERWRITE` last-wins), fixed and mutation-proven; and the find that matters most — an environment outage was being finalized as a TERMINAL candidate rejection. No migration; head stays `m0086`. F-058/F-059; the wave host now judges a suite against a measured BASELINE.
- [2026-07-26 — The operator review: Wave A completed, 17 findings, and the parallel-lane mechanism](2026-07-26-operator-review-and-wave-a.md) — deferral-sweep 2/2 + COMPLETION-LEDGER Wave A 4/4 GREEN (m0083–m0086); a live operator review turned into 17 grounded findings, five of them one defect (the UI renders the weakest field it can reach); the only correctness finding — Atelier commits agent work with no review and no pre-commit gate; MODEL-LADDER + TASK-BOARD specs; the wave host parameterized for parallel build lanes; a real migration-id collision and the allocate-from-live-head rule.
- [2026-07-23 — The "Atelier Alive" program: full design suite + stabilize chain completed](2026-07-23-alive-program-and-stabilize.md) — pause lifted; north star set (100% floor → wake the brain). Six design specs (observer loop, alive-UI, self-improvement loop, versioning/D-042, completion ledger, nav map) + the plan-refine loop; the released hardening chain COMPLETED green (periphery-svc/security-floor-2/cost-governance-2/cc-config-2, m0081); D-042 locked; 3 deferrals chained.
- [2026-07-11 — The hardening chain: waves 5–9, paused mid-9](2026-07-11-hardening-chain.md) — cost-governance-1b/cc-config/projects/adapter-framework GREEN + periphery 2/3 (operator pause); D-036 stale-catalog spawn hole closed, budget-gate race bounded, one brief dispatcher, dryRun required; F-051 ×3 recovered zero-loss; deferral waves cost-governance-2 + cc-config-2 queued.
- [2026-07-08 — The "spec everything" campaign: 10/10 specs](2026-07-08-spec-everything-campaign.md) — ORCHESTRATOR/PROJECTS/CC-HARNESS/ADAPTER-FRAMEWORK/DB-RUNTIME/COST-GOVERNANCE/CC-CONFIG/SECURITY-MODEL/SCANNER/SYNC/SERVICES specs, all opus-scout-grounded; coverage 15→27 FULL; 9 hardening waves queued gate:operator; headline finds: root-runs-runtime DB gap, null cost_usd, boot re-drain hole, slot-keyed run registry.
- [2026-07-07 — Operating manual + recovery-repo-harden](2026-07-07-operating-manual-recovery-harden.md) — rewrote CLAUDE.md as the Atelier v2 operating manual + 3 skills; recovery-repo-harden wave 4/4 (F-050 main-everywhere, F-048 concurrent-clobber, BL-R4 operator re-run path), pushed origin/v2.
- [2026-07-01 — Cognitive architecture + Concierge + model-switch/benchmark](2026-07-01-cognitive-concierge-model-switch.md) — S0–S4 complete, Concierge Stages 1–3 + consultation (chat + autonomous), local-vs-cloud model switch & benchmark, `/brain` view, 15 features shipped.
